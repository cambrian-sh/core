# CONTEXT.md, cambrian-core (Substrate)

This file is the **manual for the OSS Go kernel**, scoped to `cambrian-core`. It
is the per-sub-repo source of truth for the Substrate: layering, module
responsibilities, current implementation status, and the load-bearing design
decisions that make the runtime run.

For agent-facing rules, see `AGENTS.md` in this directory. For layering and
domain terms, see `docs/ARCHITECTURE.md`. For the reasoning behind a decision,
see the relevant ADR in `docs/adr/`.

---

## Implementation Status

Status values follow the ADR vocabulary in `docs/adr/README.md`, condensed to
the 5 surface tokens used by the F-verifiers. The runtime is at
`v0.6.10-Alpha` (pre-1.0; proto + config are stable, Go API is not).

| Area | ADR | Status |
|---|---|---|
| The Cambrian MCP endpoint — transport, auth, listener (E3) + four read-only core tools (E4) + token CLI, stdio bridge, parity ledger (E5) + five premium tools, identity hop, D6 correlation (E6) | 0126 | **Implemented 2026-08-14 through E6 — phases 1 AND 2 complete.** E6 (premium, plus four small OSS seams): the five premium tools land through `Registry.PublishTool` at Register time over each plugin's EXISTING services — `substrateplugin` publishes `query_knowledge` (the ADR-0118 D1 scoped seam, run as the calling principal, the two typed refusals kept tellable apart), `ask_knowledge` (the same `astFormer`: LLM-drafted AST, name-verified against the real catalog, executed scoped; a refusal is an ANSWER carrying `lane_hint`) and `check_policy` (`policycheck.Check` against `currentBounds`, no LLM); `authzplugin` publishes `explain_access` (`ExplainAccess` asked about the CALLER off the context — an arguments-supplied principal would make a transparency tool an oracle); `receiptsplugin` publishes `get_receipt`. **D4 identity hop:** `mcpserve.Options.Identity` mirrors `Options.Authorizer`, wired from `opts.IdentityResolver` — the SAME registry the chat ingresses use. No resolver ⇒ phase-1 behaviour verbatim (the client name IS the principal, the honest open-core statement); installed ⇒ a `principal` binding re-points it, `Blocked` is a 403 at the door, a group binding keeps the client's own identity (the group's terms reach it through the decision point), and `StrangerPolicyFor` governs anyone unbound. The principal KIND stays `PrincipalAgent` across the hop on purpose: a binding changes who is asking, not how the decision point evaluates them, and an unknown agent principal fails closed. **D6 correlation:** `domain.WithQueryCorrelation` / `QueryCorrelationFromContext` / `NextQueryCorrelationID` (prefix + shared atomic hop counter); `mcpserve.invoke` mints one 128-bit handle per tool call, seeds it, and defaults `ReceiptRef` to it; `emitDecision` PREFERS it over minting and, because it fires once per hop, hops extend it (`<corr>-h1`, `<corr>-h2`) — a 1:1 id would let hop 2 land on hop 1 and the chain would read incomplete without saying so. `receipts.ListFilter.QueryIDPrefix` (LIKE-escaped) is the read side, and `get_receipt` verifies the CONTIGUOUS run spanning the hops, anchored on its predecessor, so unrelated receipts written between hops do not read as lost evidence. **Scoped answer (the ledger's E6 must-do):** `QueryService.Answer(ctx, query, callerID)` is the real caller-scoped counterpart of the system-scoped `AnswerSystem` (one shared body; the two differ only in the seeded scope and the caller id), and `ask_memory` now passes the context principal — under a premium PDP the prose lane narrows exactly as search does. Surface-vocabulary follow-ups landed: premium `authz/proposer.go` `knownSurfaces()` and UI `ExplainPane.tsx` `SURFACE_KINDS` both gained `mcp`; the stale `operator.proto` surface-kind COMMENT is deliberately untouched (contract event, batch with a phase-3 bump). E5: `cambrian mcp token create\|revoke\|list` (`app/mcp_cli.go`; OFFLINE by design — bbolt is single-process, a held lock names the fix; tokens are 256-bit `cmcp_…`, shown once, durable across restarts) and `cambrian mcp bridge` (dumb-pipe stdio relay: SDK client against the HTTP endpoint, token via `CAMBRIAN_MCP_TOKEN` env only, instructions and `_meta` pass through untouched) on BOTH binaries; the D11 parity ledger `docs/published-tool-parity.md` gated by `scripts/check-parity.{sh,ps1}` (which also block publishing `remember` while its ledger row reads deferred). Remaining: phase 3 (an operator console for MCP *clients*, `tools/list` change notifications, MCP prompts + resources, OAuth, a stability statement) — all of it needing an operator-contract bump. `mcpserve/coretools.go` publishes `search_memory` / `ask_memory` / `get_document` / `list_documents` (owner "core", all `EffectRead` + readOnlyHint; `remember` deferred by owner ruling), bound in `app` to the same read sources the operator plane uses — caller-scoped `QueryService.Search`, the ADR-0081 answer lane gated on `AgenticRetrievalEnabled` (no model ⇒ extractive degradation, never sampling), `DocumentLister`, principal-scoped `DocumentGetter`. D12 server `instructions` ship at initialize; D8's `tools/list` is effect-filtered per caller via `AddReceivingMiddleware`; the tool contract is frozen by `mcpserve/testdata/tools_list.golden.json` (regenerate with `-update`; any drift is a public-contract event). E3 status, still true: SDK v1.6.1 → v1.7.0 (no source adaptation needed; new indirect `golang.org/x/time`). New `internal/infrastructure/mcpserve/`: the Published Tool Surface rendered as one MCP server on `NewStreamableHTTPHandler` with `Stateless = true`, DNS-rebinding protection left ON, plus the D4 middleware (constant-time bearer → principal `mcp:<client>` + surface `mcp` → per-credential concurrency cap → per-call effect authorization; a denial is a TOOL error, never a transport error). `ReceiptRef` rides `_meta["cambrian.sh/receipt_ref"]`. Listener in `app/mcp_endpoint.go` through `transportCredentials` + `secureListener` (routable + plaintext + no opt-in still REFUSES), started immediately before `Connector.ConnectAll` because that call blocks boot for 40-45 s. Config `server.mcp{enabled,port,clients,max_concurrent_per_client}`, **default disabled and with no implied port** (owner review: ADR-0126 §8 item 2 reserves the port default). Zero tools is a legal state |
| Q8 latency fix: retrieval-pass instrumentation + batching + concurrency | — (review Q8; record: `docs/research/agentic-retrieval/DECISIONS.md` addendum 3, 2026-08-14) | **Implemented + probe-measured 2026-08-14.** Per-stage pass timer in `searchByType` (INFO breakdown >500 ms — degraded and healthy passes are no longer indistinguishable); `ChunksByIDs` chunks-only batched materialization (adapter + ENFORCED wrapper) replacing per-candidate 4-table GetByID loops in kgExpand/seeding; query-entity seeding via one batched corroboration-ranked lookup (was one SQL per term) with TRIGRAM terms (3-word anchors could never match under the bigram cap) and a 32-term cap; dense∥lexical concurrency; two-stage `ChunksForEntities` SQL (bound candidates by corroboration+recency before distance-ordering — hub entities were costing ~300 ms/hop in per-row cosine). Probe: end-to-end −19% (4-hop −28%); kg_expand stage 230–360→64–151 ms; remaining pass costs are embed HTTP + ts_rank; per-query time now dominated by the LLM calls. Race-tested; live-PG test green. Packet-reorder arm implemented but unmeasured (owner held measurement for this fix; next leg carries both) |
| Retrieval upgrade wave (owner directive 2026-08-13, "no A/B until all three"): embedder swap + GPU reranker + graph mechanics | — (frontier-comparison report "RAG vs the Frontier"; measured as ONE combined battery, record to `docs/research/agentic-retrieval/DECISIONS.md`) | **Implemented 2026-08-13, measurement pending.** (1) **Embedder**: `configs/embedder.json` bge-large → **qwen3-embedding:0.6b** (same 1024-dim ⇒ no dim migration; Qwen instruction-format query prefix; probe showed +35% relevant-vs-irrelevant margin). Store re-embedded via backfill script (57k chunks, legacy `chunks.embedding` column — both ADR-0107 projection flags default off). Rollback = revert embedder.json + re-run backfill with bge. (2) **Reranker**: new `execution.retrieval.reranker_model` key, injected as `RERANK_MODEL` through the `SetEnvExtras` config-derived-env seam (never read from kernel .env); live config pins `BAAI/bge-reranker-v2-m3`; system Python upgraded to torch cu126 — measured **1.27 s / 100 pairs on the RTX 3060 vs ~40 s CPU** for the 2023 setup that got Stage-B shelved. `reranker_enabled` stays default-false until the battery. (3) **Graph mechanics** (HippoRAG-2-inspired, LLM-free ingest unchanged): batched `ChunksForEntities` store capability — ONE corroboration-counting, cosine-ranked SQL round-trip per expansion hop, replacing up to 30 sequential per-entity queries ordered by `extracted_at` (review defects Q8+Q16); `ChunksMentioningEntityRanked` for single-entity callers; query-time alias linking in kgExpand (lexical variants: possessive/plural/hyphen/diacritics + embedding-nearest entity names via `EntityIndex.NeighborNamesOf`, zero extra embed calls); `expandedScore` now hop-decays its survival floor and adds a bounded distinct-entity corroboration bonus (was flat 0.5). Non-displacing invariant untouched. Unit tests + a live-Postgres integration test (`chunks_for_entities_pg_test.go`, PG_TEST_DSN-gated) green; full build+test sweep green |
| Party-scope SQL pushdown for the chunk plane + `execution.ingestion.chunk_context_prepend` flag | — (security fix + measurement-gated flag; record: `docs/research/agentic-retrieval/DECISIONS.md` 2026-08-13) | **Implemented 2026-08-13.** (1) `scopeExpressionsOn` now renders `PartyScopedTags` as NOT-containment terms — the SQL mirror of `TagPredicate.Check`'s party-less semantics (chunks have no parties column; a party-scoped tag on a party-less resource denies). Closes the RAG-review Q2 gap where dense/lexical/entity chunk reads returned rows the authoritative predicate refuses; `TestScopeExpressionsOn_PartyScopedDeniedLikeForbidden` pins it. The substrate plane keeps its row-aware parties disjunct (CheckRow's mirror). (2) New flag `chunk_context_prepend` (default OFF) prepends `<title> › <section breadcrumb>` to chunk text at ingest — both retrieval lanes gain the tokens (stored text feeds the FTS GIN). Measured flat on tau2 banking (titles largely restated in bodies — wrong instrument); the decisive gate is TechQA, pending. Upsert re-ingest is stack-safe (prefix guard, unit-tested) |
| Agentic retrieval bridge-binding fix: second-chance grounded extraction + diacritic-folded grounding guard in `retrieval_agent/planner.py` | — (measured arm; record: `docs/research/agentic-retrieval/DECISIONS.md` monorepo root) | **Implemented + measured 2026-08-12.** The July full-run miss analysis showed 72% of MuSiQue misses were mid-chain breaks where `answer_subquestion` returned "" despite gold in context (`subq_ref_fallback` → unresolved referent text poisons the next hop's query). Fix: when the strict pass yields nothing grounded, a candidate-forcing second prompt commits to the best in-context entity — the mechanical word-set guard (⊆ context tokens, now diacritic-folded) still enforces grounded-only by construction. 60-Q hop-balanced A/B: answer 0.783→**0.867** (+0.083; 3-hop +0.15), fallback hops −62%, +0.8 s/query. Single-seed; full-2417 confirmation is the gate for updating the published 0.703. Same session found two environment traps recorded in the DECISIONS file: a port-collision mode where spawned agents dial `localhost` and reach a FOREIGN kernel (resident stack on 50051) so the whole loop silently fail-opens to single-pass (worth −17 pts and invisible), and the BRAIN-01 `session_id`→`ingest_thread_id` rename silently zeroing support-recall in four benchmark suites (fixed harness-side) |
| Memory-read determinism + ingest-loss observability hardening (RAG review track 1) | — (defect fixes; review artifact "RAG Hardening Review" 2026-08-12) | **Implemented 2026-08-12.** Eight surgical fixes from the end-to-end RAG design review, all the more load-bearing since ADR-0123 made the agentic/hybrid stack default-ON: (1) ADR-0015 exploration slots are now OPT-IN (`SearchOptions.ExplorationRate > 0`) — the adapter's silent 5%-when-unset default (minimum one slot) had every retrieval path returning ~5% uniformly random rows marked as PRIMARY hits, making identical queries non-deterministic and contaminating every A/B; no production caller ever set the field. (2) The `_agentic_control` synthetic row (Score 2.0, LLM-synthesized text + full hop trace) no longer ships to agent callers on `QueryMemory` unless they opt in via the `x-agentic-control` metadata header (the benchmark transport now sends it); agents were grounding on a fabricated "memory" at position 0 by default. (3) The relevance floor tests the pinned cosine (`RawScore`, backfilled once post-dense-search) instead of the stage-rewritten `Score` — any floor > ~0.02 with hybrid-on used to drop every RRF-scored row (~1/61) while 0.5-floored graph injections survived; inert at today's floor 0 but armed. (4) `decompSearch` caps planner fan-out at 6 sub-questions and `finalizeAgentic` truncates the interleaved union to `recall_top_k` (was up to N×topK rows to the wire). (5) `cosineSimilarity` guards mismatched vector lengths (ADR-0107 projection coexistence makes a legacy-column dimension mismatch a reachable data condition — was a query-goroutine panic). (6) Partial ingest loss is loud: `persistChunks` reports non-zero dropped-chunk counts (was silent below 100% loss), and `EmbedBatch` warns when a context-overflow retry halved the input cap for a whole batch. (7) The docling junk-leaf filter counts drops (`dropped_leaves` in the sidecar JSON, logged kernel-side with the sidecar's previously-unchecked `ok` flag), and its word test is Unicode-aware on total letters — the old `[A-Za-z]{2,}`×2 rule silently DELETED every Cyrillic/CJK/Arabic paragraph at ingest. (8) Silent fail-opens log (empty plan rewrite, decompose-enabled-but-non-decompose-planner downgrade, nil-planner-at-finalize, structure-parse nil/no-leaves flat fallbacks). Full `go test ./...` green + separability green; behavior deltas are removal-of-randomness/wire-hygiene, benchmark-gated under track 2's A/B |
| First-class Bun/Node agent runtimes (kernel-side): `RuntimeBun`/`RuntimeNode` + per-runtime interpreter map (`metabolism.bun_executable`/`node_executable`, $PATH fallback), JS discovery shapes (`*agent.ts`/`*agent.js`, JS package = `package.json`+entry, `node_modules` never scanned), standalone sidecars opened to all explicit traits (cognitive normalizes to `""`), JS deps presence check, setup step 5 (bun probe/download + `bun install`, `--skip-js`), PLAT-01 JS twin (`scripts/gen_agent_packages.py` + `agent-packages`/`agent-packages-check`, chained into `agent-reqs-check`) | 0125 | **Implemented 2026-08-12 (owner directive: kernel support now, TS SDK deferred).** The agent-plane contract, boot/health handshake and SEC-01 sandbox were already language-neutral; this closes the spawn/discovery/config/install couplings. No proto change ⇒ no contract bump. Spawn branch shares the Python argv contract (`--socket`/`--substrate-addr`/`--auth-token`/daemon flags) with `NO_COLOR=1`+`FORCE_COLOR=0`; sibling `<id>.manifest.json` is canonical for JS manifests (`ManifestRecord.Runtime` carries the .js-under-bun override), with an ``AGENT_MANIFEST = `{…}` `` template-literal regex as the heredoc-symmetric fallback. `upsertDiscovered` now refreshes ExecPath/Runtime for every record on source change (was tool-sidecar-only). On Linux the SEC-01 memory cap is NOT applied to JS agents — `RLIMIT_AS` vs V8 virtual reservations (D8); Windows Job Objects apply unchanged. `go:embed all:agents` guarded by gitignoring `node_modules/` + unpack filter. Python fleet byte-identical (agentmgr/storage suites + new unit tests for every JS branch green). Not benchmarked, deliberately: no in-tree JS agent exists and no scoring path is touched (recorded in the ADR per change-control §8). |
| Operating config promoted to shipped defaults: 30+ `DefaultConfig()` changes + committed `tuning.json` emptied | 0123 (owner directive 2026-08-11: "everything in my local json files should be defaults") | **Implemented 2026-08-11.** The gitignored local files' effective values became the Go defaults, verified by a defaults-vs-merged-config diff that now shows **exactly three** deliberate exclusions. Promoted: the whole tuned retrieval stack (agentic retrieval ON 4-hop + grounded decomposition + planner `llm:deepseek`, hybrid RRF ON k=60, Stage-A blend ON with weights .4/.2/.05/.1, kg2rag 2-hop/40/10, recall 100/100, reranker_top_k 100 + weight .5 — the reranker FLAG itself stays opt-in, similarity floor 0, hnsw_ef_search 120, query-entity seeding ON), evidence capture ON (the ADR-0105 D6 off-rationale expired when ADR-0108 supplied the outbox consumer), kg_extractor ON (was tuning.json's only real pin), chat pool ON (2/8/`chat_agent`), procedure induction hourly, plan ceiling 10 min + 15 s step buffer, pipeline drainer ON, **`execution.tools.tool_menu_k`** (new key, catalogue-writable, 0 ⇒ SDK default 3 — replaced the rig's `agent_env_passthrough: [CAMBRIAN_TOOL_MENU_K]`, which was REJECTED on the owner rule that a knob must never depend on .env: the kernel now INJECTS the var into agent envs from config via the new `InstanceManager.SetEnvExtras` seam, distinct from the SEC-01 passthrough which only carries what the kernel's own env holds; default passthrough back to empty; ConfigSchemaVersion 1→2), **embedder block defaults to ollama/bge-large/1024** (a fresh install now boots and embeds with no embedder.json — dimensions 0 used to refuse boot), llm_provider health 3/30 s stated where defaults live + max_concurrency 3, mcp.call_timeout 60 s. **Excluded by design (post-promotion security audit, same day):** `tools_unrestricted`/`tools_auto_approve` (bench-rig security bypasses; stay false — every agent + auto-approved dangerous tools is not a shippable default), `llm_provider.default: deepseek` (a default id boot-bricks any deployment declaring generators without it; it stays in providers.json beside the generators), and `trace_pipeline_payloads` (initially promoted, then REVERTED on audit: it ships bounded previews of real item data to the operator plane, which its own doc comment calls an exfiltration surface as a default; `capture_llm_exchanges` was never promoted for the same reason). The rig keeps all four via tuning.local.json. Committed `tuning.json` emptied — every pin equalled the new defaults, and an equal pin silently shadows the next default change (`plan_timeout_ms: 120000` was ALREADY doing this to the new ceiling). Test guards moved with the decision, not deleted: golden-snapshot guard gained a reasoned `promotedExecutionDefaults` allowlist (still armed for unlisted drift), evidence/procedure/known-values tests re-pinned with the new rationale. Full core suite + per-pr green; local files now redundant except: providers.json (generators+default), mcp.json (servers, until migrated into the store), tuning.local.json (the two rig bypasses), config.json (DB bootstrap) |
| MCP server write path: `SaveMCPServer`/`RemoveMCPServer`/`SetMCPServerToken`/`ClearMCPServerToken`/`TestMCPServer` (contract **0096→0097**) + live connector attach/detach | 0123 Wave 3 (write half of ADR-0043; ADR-0101 store) | **Implemented 2026-08-11.** The WRITE half of `ListMCPServers` (read-only since 0072). Store: whole-list key `mcp.servers` (koanf list rule), replace preserves per-tool policy + `auth.token_env` (the plane never carries them). **LIVE, not restart_required:** the MCP substrate is now constructed UNCONDITIONALLY (connector = a map + a client; a zero-MCP kernel must hot-attach its first server), `mcp.Connector` grew per-server watch lifecycle — `AddServer` (teardown-then-arm; the ADR-0043 D8 loop's first act on a sessionless server is to connect, so tools appear via the ADR-0044 sink as soon as the server answers, and a down server retries with backoff exactly like one unreachable at boot), `RemoveServer`, and `Watch` now blocks on ctx (a WaitGroup over the boot list would orphan runtime-added loops); boot arming uses `armWatch` (no teardown — ConnectAll's sessions and tools are already live). `app.mcpRuntime` is the live list shared by the lister and the write path, so a re-read after save shows the new server. **Tokens:** `mcp.SetSecretResolver` mirrors the llm one (env first, store second — `mcp:<id>:token`); the connector's `httpClient` resolves through it at connect time; a token SET **bounces** the server's connection (tokens are injected at connect, a healthy session would keep the old one forever) ⇒ effect live; write refused against an unknown server id. **TestMCPServer** probes the SUBMITTED spec (possibly unsaved) with a throwaway client and returns the advertised tool names + latency. `MCPServerOp` gains the declared half (endpoint/args/auth/tags) + `token_configured`/`token_source`; sse endpoints now render as URL not command. UI: **`/mcp` is the ONE surface** (owner decision same day — an interim settings pane was built then folded into it): cards + editor (transport radios, draft-probe test, write-only token panel, add/remove; `['mcp-server-configs']` invalidated on write). The mock-era `mcp_servers` feed projection — which no kernel ever populated, so the screen said "no servers registered" on every deployment — was deleted end-to-end (TS types, Rust fold field, mock seeds, old detail/filter screens); `IntegrationsSettings` + `MobileRuntime` rewired to the real read. Unit-tested (write source × 8, connector package -race, console × 5); gates green |
| Role-assignment write path: `SetRoleAssignment` (contract **0095→0096**) + `Provider.SetRole` live hot-apply + removal guards | 0101 (write half of ADR-0042 roles) | **Implemented 2026-08-11.** The WRITE half of `ListRoleAssignments`, which had been read-only since 0072 (the console's RoleMap radios invoked a bridge that did not exist). Chain: `SetRoleAssignmentOpRequest` → `operator.Service.SetRoleAssignment` (mutating, audited) → `configWriteSource.SetRoleAssignment` — validates the role against the closed organ vocabulary (planner/verifier/router/interview/memory; `agent_step` deliberately excluded, that choice belongs to the EFE hook) AND that the generator exists (a dangling binding stored durably would silently serve the default while the console shows the typed name), stores **per-role keys** `llm_provider.roles.<role>` (koanf merges maps per key, so stored roles COMPOSE with file-configured ones — unlike the whole-list `llm_provider.generators`), shadow-checked, then hot-applies via `Provider.SetRole` (roles map now RWMutex-guarded, resolution reads it per call) ⇒ effect **`live`**. `generatorRegistry.RoleAssignments` reads the provider's live map so a re-read after save shows the new binding. **Removal guards (same contract):** `RemoveGenerator` refuses the global default (dangling `llm_provider.default` is a HARD boot error discovered at the next restart, by a kernel that then cannot serve the console that caused it) and any role-serving generator. **Validation semantics change:** a role naming an unknown generator is no longer a boot ERROR — the runtime tolerates it by design (ladder falls back to default, `resolved=false`, `NewProvider` warns naming the role); refusing to boot became a brick vector once roles could live in the store (a file edit removing the generator a STORED role points at would need the console to fix, and the console needs the kernel up). `llm_provider.default` + generator-id checks stay hard. UI: RoleMap radios wired end-to-end (outcome surfaced, `['role-assignments']` invalidated — roles are a request/response read, no feed op refreshes them), "Add generator" affordance added (the `SaveGenerator` RPC existed since 0083 with no create path in the pane). Unit-tested kernel-side (write source × 7, provider live-rebind + map-copy, dangling-role tolerance); `make per-pr` + proto gates green |
| Pipeline field schema + payload flow: `field_schema_json` on `GetPipeline`/`ValidatePipeline` (contract **0090→0091**, capability `pipeline-field-schema`), `domain.PipelineGraph.FieldsJSON` + `PipelineValidation.FieldsJSON` | 0116 | **Implemented 2026-08-05.** The kernel projects, per node, the fields available to expressions (walked from the ingress capture profile through map set/drop, split members, aggregate's fixed output), what each node reads (D36)/writes/drops, and named unknowns at the honesty boundaries (an effect's `out` is its RECEIPT; computed fan-outs; loop outcomes). Projection computed by the premium `GraphAuthor` (optional `TriggerSchema` source; the ingress-studio plugin wires its capture store, other authors degrade to named unknowns). Empty means UNCHECKED, never "no fields". Companion premium change: `CompiledPlan.NeedsPayload` — the delivery payload flows through an ingress run's items iff the plan consumes it, refused loudly when an item-reading plan gets no readable payload. UI: canvas inspector becomes a per-kind editor with a field picker + field-lineage highlight. **Contract 0092 (same ADR, D3):** the dry run replays REAL archived bodies (`pipeline.DeliverySource` / `ingressstudio.DeliveryReplay`; redacted captures = labelled fallback via `DryRunPipelineOpResponse.source`) and carries `node_examples` — up to 3 real items per node, value + receipt — so the console shows the actual data through each step; canvas became directly manipulable (palette drag-drop with auto-connect/splice, grabbable/re-routable pipes, draggable steps persisting `Node.Layout`, no-overlap default). `ui` re-pinned `0085→0092`. **Contract 0093:** `TransitionPipeline` — the lifecycle's missing BUTTON (`domain.PipelineLifecycle` port, holder-routed like saves; premium `pipeline.LifecycleGate` over the Registry's own gates; `armed` walks a draft through validate+publish so "make this live" is one intent; `paused` = publish-the-armed-revision). Capability `pipeline-lifecycle`; ui pin `0093`; canvas header gained a real next-step button (Publish / Arm — make it live / Pause), held by refusals |
| Operator-plane domain principal: `authorize()` sets `domain.WithPrincipal` for every operator RPC (`internal/substrate/operator/authz.go`) | 0047 A2 | **Implemented 2026-08-09.** No proto change, no contract bump — the interceptor already resolved `{principal, role}` and kept it in an operator-plane PRIVATE value; it now also publishes the identity the rest of the kernel authorizes on (`domain.PrincipalFromContext`), unary and streaming. Before this every operator RPC reached the domain as `principal:<none>` one frame after authenticating, so ownership, provenance and ADR-0085 chokepoints saw an anonymous caller; the operator ingest handler's hand-rolled stamp (a workaround at one call site) is deleted. Invisible to OSS tests by construction — OSS fails OPEN on an unknown principal, premium fails CLOSED — so `authz_principal_test.go` asserts the principal INSIDE a handler against a fail-closed check, verified to fail on all three assertions with the fix reverted. **Residual:** the core AGENT plane keeps the same hand-rolled stamp in `internal/substrate/network/ingest_memory.go` and still needs it — `internal/authz/surface.go` `seedAgentPrincipal` covers only premium services declared agent-facing |
| Ingress Studio kernel seams: `KernelServices.StageEvidenceContent`/`FetchEvidenceContent` (= `evidence.Ingestor.Stage`/`FetchStaged` — the content-first ordering's step 1, alone, so a raw-delivery signal journals a CID and never a body) + named-secret seam, read half `ResolveNamedSecret` AND write half `StoreNamedSecret`/`ClearNamedSecret`/`NamedSecretStatus` (ADR-0101 store via a late-bound holder set in `Run` beside `llm.SetSecretResolver`, inside the same typed-nil guard; no env form, no list operation, no read-back above the seam) | 0112 | **Implemented 2026-08-01.** Consumed by the premium ingress-studio plugin (transport staging + credential resolution for verifier profiles). All three nil when their substrate is off — consumers degrade loudly, never guess. The reactive-side half (journal-first `OnSignalDurable` + `BindDurableEmitter`) lives in `cambrian-premium/reactive` |
| Typed query plane (Substrate Phase 7): `domain.KnowledgeQuery` closed AST + `PgQueryPlane` + `KernelServices.QueryPlane` | 0111 | **Implemented + gate-passed (10/10 live).** Nine kinds (point/history/as_of/current/contradictions/aggregate/events/traverse/evidence), validated BEFORE execution; `domain.ErrCannotExpress` is the only failure mode and every refusal names its reason; every answer carries its §14 guarantee label. Executor composes the existing typed stores + four new reads (as-of over resolution versions, cross-actor contradictions, closed-map aggregates, hop-capped traversal). Wire surface = premium `SubstrateLane.Query` (rows-as-JSON v1). Gate surfaced a REAL cross-actor contradiction (PO-800) and refused 5/5 malformed queries. Open-ended deliberately unexpressible (two-tier: the corpus lane is the tool). Agent-plane exposure + read-scope = ADR-0118 (next row) |
| Substrate read-scope + agent retrieval (ADR-0118): scope-enforced `QueryPlane` + `KernelServices.QueryKnowledge` + agent-plane premium services + `domain.SubstrateConsultant` | 0118 | **Implemented + gate PASSED (2026-08-06: `substrate-retrieval` suite 16/16 live vs baseline 12/16; all four programme rows flipped, zero regressions).** `QueryPlane.Query` now takes a REQUIRED `*TagPredicate` (nil denies, records-lane discipline); every shape enforces scope matched to what rows carry (evidence/knowledge-item classification directly; events/observations via `evidence_id` join, unresolvable ⇒ dropped fail-closed; traverse never bridges through unauthorized events; aggregate scoped in SQL). `KernelServices.QueryKnowledge` REPLACED the raw `QueryPlane` seam — principal in, never predicate (`internal/authz/query_knowledge.go`; OSS AllowAll ⇒ bypass, unchanged behaviour). Auth: `/cambrian.premium.*` now REQUIRES the operator bearer unless the service was declared agent-facing via `Registry.AddAgentGRPCService` (SurfaceAgent + x-agent-id principal seeding in the interceptors) — closes the unauthenticated pass-through. Fact-lane hook: `domain.SubstrateConsultant` (nil in OSS) appends ONE synthetic `_substrate_citations` metadata row after truncation (never displaces; fails open). Residuals: operator-plane substrate reads run as SystemPrincipal (per-operator scoping future); plane still namespace-hardcoded `default`; `WorkspaceStage` outside the hook |
| Knowledge kind registry (Substrate Phase 6): `domain.KindSpec`/`ValueSpec`/`KindRegistry` + `ResolutionAuthority` | 0110 | **Implemented + gate-passed.** Extracted from three real producers; specs are DATA (plugins via `Registry.AddKnowledgeKinds`, deployments via `CAMBRIAN_SUBSTRATE_KINDS` JSON); boot-refuses duplicate kinds/bad types/orphan policies. Validation at `PutItem` + `RecordObservation` (per-predicate opt-in under reserved kind "observation"); refusals name the constraint and wrap `domain.ErrKindRefused` so async consumers separate permanent (complete-with-log) from transient (retry). `latest_assertion` re-expressed as the built-in `ResolutionAuthority`. Gate: a sensor source added by CONFIGURATION ALONE (zero new Go), 999 refused naming the max, drift 0/244 with the commitment kind declared. Residuals: event roles not declarable; reconciler stays off the authority interface until Phase-2b assessments |
| Quantitative policy knowledge (Substrate Phase 5): premium `policycheck` + policy transformer + `CheckPolicy` | 0109 | **Implemented + §7D MEASURED: 0.667 deterministic (12/18) with 6 abstentions and 6/6 P/R — publish only as the triple, never the fraction alone.** Handbook bounds extracted ONCE per document (outbox transformer, targeting by tag = data); message claims guarded via the exported `drift.AssessClaim` (guards reused, not forked); five-way closed outcome. All pre-registered predictions held; pipeline real end-to-end (3 policy_statement items with typed values verified in pg). Honest limits recorded: synthetic vocabulary-aligned corpus; "most policy is quantitative" needs a REAL handbook |
| Event-shaped knowledge (Substrate Phase 4): `events`/`event_roles`/partitioned `observations`, `domain.EventStore`, the evidence outbox CONSUMER + `EvidenceTransformer` seam | 0108 | **Implemented + gate-passed.** Migration `0014`; roles are ROWS with indexable entity edges (§7B); observations RANGE-partitioned with exactly-one-value CHECK. The ADR-0105 outbox got its first consumer (`internal/evidence.Consumer`: at-least-once, exactly-once-logical completion, unclaimed shapes complete, refuses to exist without transformers; 2026-08-06: **drain-until-idle** — a pass that fills its batch and completes something goes straight round again, so the 2 s tick paces an idle queue instead of capping catch-up at batch/interval; a no-progress pass yields to the tick rather than hot-looping on a stuck head). Premium `substrate` plugin: thin-envelope ingest + `SubstrateLane` (no operator-contract bump). Gate: async envelope→evidence→typed rows, replay deduped end-to-end, point/history/role-traversal exact, `chunks`+`chunk_embeddings` BYTE-IDENTICAL ("nothing embedded") — God Object risk CLOSED per the memo scorecard. Incident recorded: a ctx-taking `Build` silently skipped by the Builder interface — every plugin now carries `var _ app.Builder` |
| Embeddings as a versioned projection (Substrate Phase 3): `chunk_embeddings` + write/read flags + `cmd/embed-backfill` | 0107 | **Implemented through stage 3c; flags default OFF pending the cutover decision.** Dual-write in the same tx as the chunk upsert; read path via per-model partial HNSW over a cast (planner-verified); backfill RTO 2.88 s same-model copy / 15 m 58 s re-embed (33k rows, nomic). Gates: 50/50 exact top-10 fidelity, drift 0/244 across write AND read arms, full model ROUND TRIP (bge→nomic→bge) with zero refusals — **the destructive dim-migration is retired for projection deployments** (`ensureSchema` coexistence: column write skipped on dim mismatch, reads hydrate from the projection). Residuals: per-model query-prefix config; cross-model backfill coverage; cutover proposal in DECISIONS awaits the owner |
| Commitments on the epistemic split (Knowledge Substrate Phase 2a): `knowledge_items` + typed `statement_values` + versioned `resolutions`, `domain.KnowledgeStore` typed boundary on `KernelServices` | 0106 | **Implemented + gate-passed.** Migration `0012`; items are append-only with NO uniqueness on (entity, predicate, validity) — contradiction must be representable; non-overlap exists ONLY on the resolved projection (partial unique `WHERE system_to IS NULL`). Resolutions derive through the PURE `domain.ResolveLatestAssertion` from the FULL item set (permutation-tested order independence; never compare-to-prior). Premium `records.SubstrateCommitmentStore` re-backs the drift lane's existing `CommitmentStore` seam over the boundary — supersession is a projection recompute (every item kept), retraction a recorded negation item. Arm knob `CAMBRIAN_DRIFT_SUBSTRATE_COMMITMENTS` (premium env, default off). **Gate: drift suite row-by-row identical across backings, 0/244 differing** (DECISIONS 2026-08-01; runs 111444Z control / 111536Z substrate / 113819Z default-on); treatment physically verified (33 items / 66 values / 31 current + 2 closed resolution versions). **Phase 2b landed: substrate backing is DEFAULT ON** (premium env `false` = rollback lever), erasure parity shipped (`EraseItems` — the substrate's one true deletion, compliance-only — + `records.EraseOwner` composite, substrate-first ordering), write-cycle cost measured (~2 ms at p95). Outbox consumer / assessments / evidence_id linkage DEFERRED with reasoning (each unwired today; see the 2b DECISIONS entry and the ADR addendum) |
| Evidence foundation (Knowledge Substrate Phase 1): `domain.Evidence`/`EvidenceStore`, content-first commit ordering, transactional outbox, migration `0011` | 0105 | **Implemented + live-validated; flag default OFF.** The substrate's first stage (`Evidence → KnowledgeItem → …`, memo in `docs/research/company-brain/`): every ingested document's original bytes become durable, content-addressed evidence + an outbox work item BEFORE chunking or any semantic step. Ordering is control flow, not convention (`internal/evidence.Ingestor`: Put → Has-verify → atomic insert → only then may the caller ACK; orphan blobs are garbage `ContentStore.GC` collects, never damage). Idempotency decomposed, never summed to "exactly once": insertion idempotent on `(namespace, source_id, source_key, source_revision)`; outbox transition exactly-once-logical via conditional UPDATE row count; delivery at-least-once. Revisions are NEW rows linked by `revises_id` — the port has no update method at all. Wired at the single ingest chokepoint (`IngestionManager.ProcessSync`, all doors) behind `execution.ingestion.evidence_capture_enabled`; when on, capture failure fails the ingest. All four §17 crash/replay gates pass as failpoint tests (`internal/evidence/ingestor_test.go`); live-validated on a running premium kernel: 3 ingests → 2 evidence rows (identical replay deduped, changed body = new revision), 2 pending outbox items, blobs idempotent under CID. Phase-0 baseline recorded first (SUB-00, DECISIONS.md 2026-08-01). Residual: the outbox has NO consumer until Phase 2; `namespace_id` constant `'default'`; Phase-2+ types deliberately absent (God Object contained, not fixed) |
| Evidence Receipts — decision provenance: `domain.DecisionObserver` + `RetrievalDecision` seam, `Registry.AddDecisionObserver`, `ExecutionConfig.RetrievalFingerprint` | 0103 | Partial — **OSS seam only**. The kernel emits a value-copy record of every completed retrieval to registered observers (after assembly, never blocking, no error path — retrieval reads still fail open) and can identify the configuration that produced a ranking. The consumer is built and lives entirely in premium (D4): `cambrian-premium/receipts` + `plugins/receiptsplugin` supply the schema, hash chain, Ed25519 signing, Postgres store, retention and a read-only `ReceiptLane` plane. It is **unbenchmarked** — the ADR's hot-path latency arm has not been run. With no plugin registered the emit site is a nil check and behaviour is bit-identical, so OSS is unaffected. Stage attribution deliberately absent (D8) — v1 carries only the existing primary-vs-injected distinction |
| Access-policy port + extraction: `domain.Authorizer`, `TagPredicate`, kernel-side enforcement points, decision moved to the premium `authz` plugin | 0085 | Implemented (OSS `domain/authz.go` + `internal/authz/`; `domain/scope.go` and `internal/scope/` GONE — `grep ScopeConfig cambrian-core` returns nothing. `execution.scope_enforcement_enabled` retired: the chokepoints run unconditionally and only the answer varies. OSS fails OPEN, the plugin fails CLOSED. Contract 0065→0066 + cap `access-policy`: `ExplainAccess`, `ListClassificationTags`, `AccessDecisionOp`, `QueryMemoryResponse.policy_note`; agent plane gained `MemoryResponse.policy_note`. Skill `ToolGrants` are now INTERSECTED with policy (`skill_grant_clipped`) — that was a live escalation path. Residual: policy persistence, SEC-03 for the surface clamp, no benchmark suite) |
| Plugin identity on the handshake: `SnapshotResponse.plugins` + per-plugin version skew | 0089 | Implemented (contract **0066→0067**). `PluginInfoOp` carries each DECLARED plugin's id, display name, **own version line**, state (`active`/`deps_unmet`/`not_entitled`/`expired`), capabilities, panels, reason, missing deps and entitlement expiry. `contract_version` describes the OSS proto surface only, so it cannot detect a plugin two majors ahead of the console's panels — every RPC would answer normally. Reported for plugins that did NOT register too: a console otherwise cannot tell "no such plugin" from "it declined to register". Kernel still interprets nothing (ADR-0082 D2); the `app.PluginStatus` → `operator.PluginInfo` mapping lives in the composition root. UI side: `PINNED_PLUGIN_VERSIONS`, a four-state verdict (aligned/minor/major/unknown), and a `PluginSurface` shell every plugin panel renders inside so the banner is structural rather than opt-in. Live-validated: 4 plugins on the wire at contract 0067) |
| Ingress and surface identity: `domain.IngressResolver` port, `Session.Surface` writer, Postgres ingress registry with namespaces | 0090 | **D0/D2/D3 implemented** (D4 identity linking and the outbound/duplex path are NOT). An *ingress* is any point where the outside world enters — Telegram, webhook, websocket, inbound API — and chat is one payload type through one; ADR-0080's Chat Manager is renamed the **Chat Ingress** (`cambrian-premium/chat/ingress.go`, lifecycle `chat-ingress`, env `CHAT_INGRESS_ADDR` with the legacy name still honoured so a rename cannot silently disable chat). **An ingress HAS a surface; it is not one, and it never asserts its own** (INV-5) — the mapping is registered out of band and the kernel attaches it. OSS holds only the port (nil ⇒ nothing is an ingress ⇒ every surface stays transport-derived, i.e. unchanged); premium owns `authz_ingress` (write-through, wholesale reload, LISTEN/NOTIFY on `authz_ingress_changed`). `IngressRegistration.Namespace` bounds which external ids an ingress may speak for — the Matrix application-service precedent, so one bridge cannot impersonate another's users. Only **agent** principals can be ingresses (a console operator must never inherit a clamp). **D7 (delivery address):** `domain.DeliveryAddress{IngressAgentID, ExternalID}` on `Conversation` (migration `0006`), bound by the kernel on first inbound and **write-once** — rebinding is a redirect, so the guard lives in the UPDATE WHERE clause rather than a read-then-write (12-way concurrent test: exactly one bind wins). An agent names a CONVERSATION, never a recipient (SMTP envelope split) — otherwise anything producing a message could choose who reads it and a fire-and-forget ingress would deliver it. Namespace enforced at bind time via `IngressRegistration.DeliveryFor`. An unbound conversation is undeliverable, which matches Telegram's own rule. **D8 (outbound delivery):** `internal/ingress` — `DeliveryService` (resolve -> RE-AUTHORISE against the current registration -> idempotency -> send) plus `DaemonTransport` over the existing `CallDaemon` seam. Re-authorising at delivery time is what makes revoking a compromised ingress stop conversations bound while it was trusted. Permanent-vs-transient is the ingress's call (only it knows if a 403 is "blocked" or "rate-limited"); unlabelled = transient, permanent = dead-lettered. `DeliveryJournal` nil-safe = best-effort. **D9 (Turn no longer caps replies at one):** `TurnService.Emit` is the speak primitive — append + deliver-if-bound; `Turn` routes its reply through it, so the console still gets exactly one reply while anything may Emit any number of times, including with no inbound turn (proactive). The message is durable BEFORE delivery, so a delivery failure never loses it and never fails the turn. **D10 (SDK):** `cambrian_agent_sdk.IngressAgent` serves the delivery endpoint AND holds the signal stream — neither `DaemonAgent` (produces, never called) nor `CognitiveAgent` (called, never produces) could express an ingress. No API to declare a surface or choose a recipient (INV-5 by omission). **D11 (inbound):** `internal/ingress.InboundService` + a check in `Server.SignalStream` that runs BEFORE the Watcher — an ingress message is a conversational turn, so it goes to the chat lane and the planner never sees it (ADR-0080 D4 enforced for external traffic); an unregistered sender returns `ErrNotAnIngress` and falls through untouched. First contact opens a conversation with its address bound in the same write; owner is `ingress:<agent>:<external-id>` (per-sender pseudonym, isolates without proving identity); a closed conversation is not silently reopened. `Conversation.FindByDelivery` + migration 0006 index serve the lookup. **D12 (the port):** SDK `HTTPChatIngress` serves the same /open /turn /close contract as the Go ingress, with request/response correlation living IN the ingress (it is a property of that external protocol, not of Cambrian); premium `agents/airline_chat_ingress.py` is that class with the airline namespace. **Both paths coexist — the swap is UNMEASURED and the Go ingress is still the validated one until an airline run compares them.** **D13 (admin surface):** `ListIngresses`/`RegisterIngress`/`DeregisterIngress` on the premium `AccessPolicyAdmin` plane — registration MINTS a surface, so it sits behind the same operator auth as policy. This closed a gap that made the whole feature unreachable: the table and Go API existed but nothing exposed them, so nothing could ever become an ingress. Validation errors are InvalidArgument (inert registration, wildcard-looking namespace); unconfigured answers Unimplemented. Live-validated over the wire: registered, refused an inert spec, listed back, durable in `authz_ingress`. Live-validated: tables created at boot, `authz-ingress-sync` lifecycle non-blocking, migration 0006 applied through the real runner. |
| Closed tags + the crippled grant: deny-by-default per tag, and the one term that ADDS access | 0091 | **Implemented + live-validated.** ADR-0087 makes every fold an intersection, which buys "order affects the explanation, never the outcome" but cannot express *"only this path may reach this data"* — an unprofiled principal is unrestricted and `ForbiddenTags` is absolute, so a forbid broad enough to cover everyone also covers the principal you meant to allow. Default-deny + explicit grant is the dominant practice elsewhere (AWS IAM vs SCPs, K8s NetworkPolicy/RBAC, Zanzibar, MLS clearances); the narrowing-only model is the unusual one. **A tag may be declared CLOSED** (`CAMBRIAN_CLOSED_TAGS`, premium env, must be in the vocabulary): nobody reaches it unless granted. **`ScopeConfig.GrantedTags` reopens a closed tag** at a container and is crippled on purpose — it may name nothing but a closed tag (refused at write time otherwise), it can never override a forbid, and grants UNION while everything else intersects (still order-independent). **No global default-deny switch, ever** — SELinux is the cautionary tale, so a deployment that closes nothing is byte-identical in behaviour. Enforcement adds NO kernel concept: `closed − granted` is folded into `ForbiddenTags` at resolve time, so the absolute-forbid invariant IS the mechanism, and "a grant cannot beat a bound" falls out for free. Denials explain themselves (`AccessDecision.GrantedTags` + a closure note) because `forbidden_tag` for a tag nobody forbade is true and useless. Prerequisite: MCP tools became taggable (dynamically discovered tools arrived untagged, so no predicate could act on them). Two silent traps recorded in the ADR: `IsZero()` ignoring a new term drops grant-only policies, and `Build` rebuilding the Vocabulary discarded the closed set while the boot log claimed closure was active. |
| Live LLM generator registration: SaveGenerator/RemoveGenerator hot-apply — atomically-swapped broker table (`llm.Provider.table`), `generatorRuntime` (console reads/guards), streaming-gateway clients + auction TraitModel reconcile in one apply closure; first-generator auto-default; ladder membership gate | 0124 | **Implemented + tested (owner directive 2026-08-12).** A console-saved generator is routable, assignable, listable and testable on the next call — no restart; outcome `live` (restart_required only with no live provider). Auto-default closes the 2026-08-11 boot-brick (stored generators without `llm_provider.default` refused boot; ADR-0123 constraint 2). Latent ladder bug fixed on the way: rungs 1–3 resolved UNKNOWN ids (breaker is unknown-optimistic) then hard-errored in Acquire, breaking `GeneratorForModel`'s documented unknown→ladder fallback. Race-tested (200 reloads × 4 concurrent resolvers under -race); full core suite green. Residuals: gateway keeps stale streaming clients for removed ids (inert); metabolism default-cost seeding stays boot-only; boot validation of `llm_provider.default` still hard (unreachable via console now). |
| Self-installing kernel: `setup` subcommand (deps → config → migrate → run + health-verify), embedded agents/config templates at the module root, `status`/`stop` lifecycle | 0122 | **Implemented + E2E-verified, OSS AND premium.** The binary is the installer (owner decision: the Bun CLI is retired completely; the premium binary dispatches the same subcommands, so a premium install = run the premium binary's setup). Unit tests green (embed keeps `__init__.py`, filters `__pycache__`; config merge idempotent, preserves hand-edits, strips BOM, and REFUSES an unparseable config rather than clobbering it). Full E2E on an isolated prefix + scratch DB: fresh `migrate up`, kernel started and DB-gated SERVING confirmed, `status`/`stop` correct in both states — both binaries. The first E2E caught two shipped-then-fixed defects: BOM'd config silently clobbered, and "port open ⇒ already running" claiming success while a FOREIGN kernel held the port (now pid-file first, then health probe). `setup --yes` honors pre-set `CAMBRIAN_DATABASE__*` env for unattended installs. Step 7 seeds the operator-console login (ADR-0047 D13: env is the ONLY credential source, so a fresh install was unloginnable) into `<prefix>/.env` (0600): prompts with env defaults, generates + prints-once a random password under `--yes` (never a fixed default), never rewrites existing entries; the plaintext `StaticIdentity` compare is ADR-0047's V1 posture, flagged as residual, not changed here. Cross-compiled linux/amd64 + windows/amd64 CGO-free (~28 MB OSS, ~37 MB premium). Residuals: no service registration (systemd/launchd/Windows), install scripts not yet reduced to fetch-wrappers, secrets prompting deliberately absent, premium agents (airline benchmark artifacts) deliberately not embedded. |
| Row-level entitlement — the MECHANISM (ADR-0121): `TagPredicate.PartyScopedTags`/`PartyIdentities`, `CheckRow`/`AllowsRow`, `ReasonNotAParty`, `evidence.parties` + GIN (migration 0015), the SQL implication clause and the Go-side row filters | 0121 | **Mechanism implemented and tested; the FEATURE is not usable and no deployment behaves differently.** The tag algebra decides on the RECORD ALONE, so it cannot express "the orders that are yours" — the reader never appears in the predicate — and an operator's only options for a sales rep were every customer's orders or none of their own (123 of 328 graded asks, `cambrian-worldsim/docs/class-vs-row-entitlement.md`). Party-scoping is a QUALIFIER on a tag the policy already grants, rendered as a material implication `(NOT (classification && $scoped) OR parties && $identities)` — it can only ever REMOVE rows, so INV-1 holds and the algebra stays an intersection. `Check` (party-blind) DENIES a party-scoped tag rather than ignoring it, so a party-less resource fails closed AND correctly. `IsZero` counts the term at BOTH layers — omitting it on `TagPredicate` would suppress INV-3's `policy_note` at `querymemory.go`'s `!pred.IsZero()` gate, the silent empty the design exists to prevent. `ToTagSet` REFUSES a party-scoped predicate rather than truncating, because a truncating projection keeps the tag grant and drops the restriction (a widening by struct literal). Verified against live PostgreSQL: a scoped reader sees their own rows plus every unscoped row, a reader with no identities sees nothing. **Residuals — the first two are what make it a feature:** nothing populates `evidence.parties` (the ingress needs a derivation rule beside `ClassificationFrom`); no authoring path, so `PartyScopedTags` cannot be set over the admin plane and `ResultantPolicy`/What-If do not render it; `policy_note` not wired for party denials; INV-1's 3000-case property test does not generate party terms; no policy benchmark suite  **2026-08-10:** `policy_note` now distinguishes the two party cases — "no party identities resolved for this principal" (misconfiguration) from "you are a party to none of the records that match" (working) — satisfying INV-3 on the memory lane. **Residual:** the substrate query plane has no note mechanism at all, so a party-filtered substrate read stays silent; a pre-existing INV-3 gap, now named.  **2026-08-10:** an ingress stamps parties onto a record (`parties_from`) and the premium plugin resolves a principal to identities (`SelfPartyResolver` + a durable `authz_party_identities` table with three admin RPCs on premium's own plane — contract **0093→0094**, opted in with `CAMBRIAN_PARTY_PREFIXES`).  **2026-08-11 — COMPLETE, the authoring path landed (contract 0094→0095):** `party_scoped_tags` on `ScopeRule`, round-tripped through Save/ListPolicies, and **in `allTags` so the coinage check covers it — which matters more here than for any other term because a typo fails OPEN** (the term is an implication, so a tag no record carries makes the premise permanently false and the restriction VANISHES rather than narrowing). Deliberately NOT refused: a party-scoped tag no policy grants, since the restriction is routinely authored in one policy and the grant in another. D1c satisfied on both previews — `ResultantPolicy` returns the term + resolved `party_identities` + a `row_scope_note` (zero identities gets its own sentence: a misconfiguration that reads as an empty database; no party term ⇒ no note, or the qualification stops meaning anything), and What-If gained one for the OPPOSITE failure: the journal records tags but never parties, so a replayed party-scoped question fails closed and `newly_denied` is now stated as an UPPER BOUND rather than a prediction. **Pre-existing bug found and fixed on the way:** `PolicyStore.Clone` used a 3-of-5-field struct literal, so What-If had been simulating every ADR-0091 closed-tag policy as granting nothing; `ScopeConfig.clone()` now copies the value and replaces slices, mutation-checked. Residuals: no D8 policy suite; substrate query plane still has no `policy_note` mechanism ||
| Natural-language policy authoring: an assistant that PROPOSES and cannot enforce | 0092 | **Implemented; not yet exercised against a live model endpoint.** Authoring one boundary means holding the ADR-0087 tag algebra, ADR-0091 closure/grant rules, four container kinds and the report-only rollout path at once — and a wrong policy in this model usually does NOTHING rather than something visible (ADR-0085 D11's coined tag, `IsZero()` dropping grant-only policies), which makes authoring help valuable and a writing assistant dangerous in the same breath. So: **the model has no write path.** `ProposePolicy` is read-only; applying means the operator calling the ordinary `SavePolicy`/`LinkPolicy`, so approval cannot route around a validation, an audit record or the rollout default (an `ApplyProposal` RPC was rejected — one round trip is not worth making the model's output a write). Proposals are validated with the checks the WRITE path uses (disagreeing with the store teaches operators to ignore warnings): blocking on a coined tag, a grant on an open tag, a link to a nonexistent group, no links, an empty rule; warning on any grant, an organisation link, and an unresolvable principal (a human operator is deliberately not in the agent registry). An approved proposal **lands report-only** — the model cannot ask to be enforced — while the blast radius is simulated as **enforced**, because simulating report-only reports 'no effect' for every proposal ever made; `SimulationBasis` distinguishes 'nothing to compare' from 'no effect'. The request is nonce-fenced as data (ADR-0063), which is defence in depth and NOT the load-bearing protection — validation plus human approval is. No LLM ⇒ `Unimplemented` and a convenience is lost, never a check. |
| The document as an ENTITY: one table split into six | 0093 | **Implemented + verified against a real database; NOT yet benchmarked.** `documents` held everything with an embedding — 14 `document_type` values sharing one HNSW index, from document chunks to the seeded tool/skill/agent_profile descriptors rebuilt on every boot. The serious defect was not the index: **a document was not an entity.** Nothing represented the source document a chunk came from — parentage was an id string convention (`{docID}-chunk-{n}`) plus a metadata key — so classification tags were COPIED onto every chunk at ingest with no authoritative row. Re-tagging was N updates with no transaction boundary, and a partial failure left a document **half-classified**: some chunks reachable, some not, nothing able to report the disagreement. Now: `documents` (new entity, authoritative `tags`), `chunks` (every vectorised recall unit, `document_id` **nullable** — an agent's memory of its own work has no parent), `document_sections`, `tools`, `skills`, `agent_profiles`. **All recall types stay in ONE table** so the split cannot have narrowed what a search finds. The chunk tag copy is KEPT as a derived cache (the filter is on the hot path behind a GIN index; a join would trade correctness for latency) but gains a single writer: `RetagDocument` moves the document row and every chunk in one transaction, and deleting a document cascades. `document_id` is written through a SUBSELECT so a missing parent yields NULL rather than failing the write — losing a memory over bookkeeping is worse than losing the link. Migration is ADDITIVE: `documents` is renamed to `documents_legacy`, never dropped. **The trap:** `update_activation_strength`/`apply_ebbinghaus_decay` name `documents` in their bodies and plpgsql resolves that at CALL time — renaming without redefining them would have left decay silently operating on the new empty table, every call succeeding and nothing happening. Both redefined against `chunks`, with a test. **Three follow-on migrations, all found by a LIVE BOOT after 9 integration tests and 50 green packages — every one of them lived in the seam between the schema and the code, which the tests exercised separately:** `documents.version + 1` in the ON CONFLICT target (a tool upsert emitting it while inserting into `tools`); **0008** adds `section_path` to the descriptor tables (0007 claimed "same column set as chunks" without checking it against the adapter's SELECT list, so every agent's interview-vector check failed); **0009** drops `document_edges.source_id`'s foreign key, which PostgreSQL had carried along with the rename so it pointed at the FROZEN legacy copy — ~700 silent `SaveStructuralEdges` failures per ingest, non-fatal only because that write is best-effort, losing the whole ADR-0060 structure graph. An edge endpoint may now be a chunk OR a section, so no single FK can express it (the baseline had already dropped the `target_id` twin for that reason). The row shape was an UNASSERTED CONTRACT between migration and adapter; `row_shape_test.go` now runs the adapter's exact SELECT against every table `GetByID` consults, with the column list deliberately duplicated rather than imported (importing would make it agree by construction). **Benchmarked, no regression:** locomo 0.310→0.380, document-qa 0.500→0.500, tau2-knowledge recall 0.435→0.486 / mrr 0.339→0.378 / hit_any 0.959→1.000. Read as "nothing got worse" rather than a measured gain — locomo gave 0.410 pre-0009 and 0.380 post-0009 on the same code path, so a single run-pair cannot support a precise delta. **A FIFTH bug lived in the measuring instrument:** tau2-knowledge scored a flat 0.0000 because its scorer read `metadata["session_id"]`, a key a prior change had renamed to `ingest_thread_id` — pre-existing (the frozen `documents_legacy` snapshot has zero rows with it) and unrelated to the split. Repointed at `metadata["document_id"]`, recall went 0.000→0.486. A broken scorer and a catastrophic regression produced the IDENTICAL row, which cost two wrong conclusions in one session; rows now carry `hits_returned`/`docid_unrecoverable` and a distinct `failure_kind` so a dead instrument cannot masquerade as a bad score. |
| Labelling documents from the console: vocabulary-driven ingest labels, bulk labelling, `ListDocuments`, and the identity/classification split | 0099 (new); contract **0069→0070** | Implemented; **not benchmarked** (see below) and the new RPC **not yet exercised over a live gRPC connection**. Everything here follows from one fact: **access policy acts on labels and never on a document by name** (ADR-0085), so an unlabelled document is not denied — it is *invisible to the policy model*. **(1) Ingest labels were free text** (`IngestPane.TagChips`), the exact ADR-0085 D11 defect the scope console already solved: `internal-only` for `internal_only` is not a restricted document, it is a mislabelled one, and the two are indistinguishable from the console. Replaced with the shared `TagPicker` + `useVocabulary`, keeping free entry (labelled as unsafe) when no vocabulary is configured — an unscoped OSS deployment must still be able to tag — and warning that a CLOSED label makes the document deny-by-default for everyone, the operator included. **(2) Labelling was one-at-a-time**; the Labels pane now multi-selects and applies one label across the selection under one reason. `TagMemory` is per-document, so a bulk run is N calls — atomic on each document, never across the selection — and the result is reported as a COUNT ("labelled 7 of 9") with failures named, because a blanket success is a claim the UI cannot make. **(3) There was no way to browse documents at all**: the pane searched semantically, which answers "find the document that says X" and cannot answer "which of my documents have no labels?" — a question with no query text. New OSS `ListDocuments` RPC (`ListDocumentsOpRequest`/`ListDocumentsOpResponse`/`DocumentSummaryOp`, capability `document-listing`, `memory.DocumentLister` implemented by `PgVectorAdapter`) reads `documents` directly with an `unlabelled_only` filter, **keyset** paging (a concurrent ingest must not shift an OFFSET window and skip the newest row — the one most worth seeing) and a `total_matching` that ignores paging, because "422 of 1163" says how much of the corpus no rule can reach and "50 shown" does not. Documents are an OSS concept, so this is on the OSS plane, NOT the premium authz plane. **(4) ADR-0099 — the root cause.** `ExternalDocument.Tags` carried a classification AND an identity: `externalDocumentID` rule 1 reads the tag after `source_document` and makes it the document id. Six suites pass it deliberately (it is what stops rule 1b collapsing N documents onto one id). **A handoff prescribed deleting that tag from the tau2-knowledge ingest; that is wrong and was not done** — `metadata["document_id"]` *is* that tag, so removing it renames every document to `tau2-knowledge:<content-digest>` and scores a flat 0.0 recall, a renaming that reads as a catastrophic retrieval regression. Identity now stays on the wire and is stripped from the CLASSIFICATION at the write chokepoint (`memory.classificationTags`, called in `persistChunks` after the id is resolved): it removes the `source_document` marker and a tag equal to the RESOLVED id, preserving everything else in order, so a caller whose id came from a thread/URI/digest keeps every tag it sent. One change, all six suites, none of them edited. **Live result:** 726 → **15** distinct tags across 1163 documents; `cmd/tag-repair --apply` (dry-run by default, writes through `RetagDocument`) repaired **710 documents, 0 failures**; 0 self-id tags remain, document ids unchanged, and all **741** labelled documents agree with their derived chunk caches (0 half-labelled). 422 documents remain unlabelled — that is the operator worklist the browse lane now exposes, not a defect. **Not benchmarked, deliberately:** no code is added to any retrieval, routing or ranking path — the kernel change removes two strings from a label list at ingest and cannot move a recall or routing score; document ids, chunk ids and `metadata["document_id"]` are all unchanged. Recorded rather than silently skipped (change-control §8). The cheap confirmation is a `tau2-knowledge` run, whose recall must be unchanged. **Residual:** no live gRPC round trip for `ListDocuments` (a kernel already held port 50051 and the bbolt lock); its SQL is validated read-only against the live database but has no automated integration test; ADR-0099 is `Proposed`. |
| Tool effect classes: closed `read` / `write` / `egress` / `spend` / `admin` set, `ClassificationTags`, registration validation | 0086 | Implemented (`domain/tool_effects.go`; `TOOL_MANIFEST` gains `classification_tags`/`effects`; `InMemoryToolRegistry.Register` is the normalization chokepoint. Un-migrated tools get DETERMINISTICALLY INFERRED effects flagged `effects_inferred` on `ToolOp`, because shipped manifests live outside this repo; `execution.tool_effects_strict` (default false) makes absence fatal once migrated. An effect outside the closed set is fatal in both modes) |
| Policy composition: groups, policy objects, links, LSDOU-analogue precedence, Block/Enforced, report-only, What-If, audit export | 0087 | Implemented (premium `authz/policy.go` + `simulate.go`; administration on the premium proto plane `AccessPolicyAdmin` per ADR-0073, so groups/policies/links never enter the pinned OSS contract. Cycles rejected at write AND survived at read; unsatisfiable rules refused at SAVE time; deny survives Block Inheritance. Groups/policies/links are durable in Postgres per the ADR-0087 D9 amendment — write-through durable-first, wholesale reload, LISTEN/NOTIFY across replicas — with the in-memory map as the read model. Residual: no authoring-history table) |
| Operator answer lane: grounded, `[n]`-cited answers (`AnswerMemory`) | 0081 | Implemented (proto contract `0060`; capability `memory-answer`). New RPC runs the agentic retrieval loop on the operator plane — a deliberate, capability-gated exception to the single-pass invariant (read-only, no agent selection, no `x-agent-id`). `QueryService.AnswerSystem` reuses `agenticSearch` for evidence then re-synthesizes over the evidence-only order via the optional `CitedSynthesizer` (`RetrievalDispatcher.SynthesizeCited` → Python `synthesize_cited`, which numbers chunks `[1..N]` and cites inline) so marker `n` aligns to `evidence[n-1]`. Distinct Python op keeps the benchmark synthesize (whose answer text the scorer substring-matches) marker-free. `operator.Service.AnswerMemory` maps evidence → `MemoryCitation[]`. Wired + capability advertised only when `execution.agentic_retrieval_enabled`. Builds + operator/network unit tests green. Residual: benchmark gate (operator-answer coverage on the `agentic-retrieval` suite) + live end-to-end run |
| DAG parallel execution, value-copy plan freeze | 0001 | Implemented |
| Self-healing, replan, negative edges, loop detection | 0005, 0010 | Implemented |
| Mid-execution semantic checkpoint (H1 gate) | 0013 | Implemented |
| Neuromodulator cost-aware routing (GatekeeperScore cost term) | 0011 | Implemented |
| Synaptic bridge (episodic write-into-readout) | 0012 | Implemented |
| Thalamic gating, workspace priming (cold-start fallback) | 0014 | Implemented |
| Engram engine: `activation_strength`, floor-multiplier re-rank, Tier-1/Tier-2, `DocType` taxonomy | 0015 | Implemented |
| Global Workspace Stage (SCENE+FACT, contradiction guard) | 0016 | Implemented |
| Spreading activation (GraphRAG over `document_edges`, `0.75^depth`) | 0017 | Implemented |
| Managed cognitive resource allocation (LLM gateway streaming) | 0018 | Implemented |
| System quality measurement (PlanEvent, RetrievalSession, ContradictionResolution) | 0021 | Implemented |
| Koanf config engine (11-layer pipeline, curated `tuning.json`) | 0024 | Implemented |
| Memory reform (retire `DocTypeMemory`, XML-tagged planner injection, live graph edges) | 0025 | Implemented |
| Plan template generalizer (Hippocampus promote/blacklist) | 0030 | Implemented |
| Universal input router + reactive rule engine plug | 0031, 0033 | Implemented |
| Tag-based isolation (Phase 1: agent-scope read/write enforcement) | 0034 | Implemented (residual: Phase 2 `caller_scope` wiring is HITL-gated, see Known Gaps) |
| Working-memory context hygiene (recall same-session filter, `summary` column, tool-card offload, trajectory cards, cid hydration) | 0048 | Implemented (residual: D2 spreading off by default, no `resolve` for `content_cid`, truncation heuristic, embed-summary-vs-full a flagged choice) |
| Verbal self-reflection (SDK-side, gated on the LRW recurrence gate) | 0052 | Implemented |
| Agent trait classification (Cognitive / Model; retired TraitTool) | 0058 | Implemented |
| Centralized LLM provider (health-guarded broker, circuit breaker, price ledger, failover ladder) | 0042 | Implemented (residual: model-failover requires ≥2 generators in config, see Known Gaps) |
| MCP tool provider (MCP server discovery, agent-unreachable approval, injection-scanned responses) | 0043 | Implemented (residual: tool price in menu + per-tenant budget + OAuth deferred) |
| Local Recurrent Workspace (typed working memory + recurrence gate in the SDK) | 0041 | Implemented (typed working memory + gate live in the Python SDK; ADR-0048 amends the prompt-condensation half) |
| Two-tier tool disclosure (`toolSummary` / `describe_tool`) | 0045 | Implemented (extends 0044) |
| Agent skills (system + agent-local; `RunGrantOverlay` for system-skill grants) | 0046 | Implemented (residual: `RunGrantOverlay.Clear` on session-end is unwired, see Known Gaps) |
| Operator transport plane (UI gRPC surface: feed, cursor resume, projection, auth, audit, HITL, capability handshake) | 0047 | Implemented (all 13 slices live in `internal/substrate/operator/`; producer-side emit/dispatch wiring in non-operator organs is the residual) |
| Routing traces on the selection event (Gatekeeper L1/L2/L3 candidate funnel + winner margin + requirements on `AuctionEventOp` — the proto name outlived the auction, see ADR-0100 P3) | backlog ROUTE-02 | Implemented (behind `execution.routing_trace_enabled`, default true; funnel is a `json:"-"` output on `DispatchTask`, mapped to proto in `operator/mapper.go`; consumed by the `orchestration` bench suite; residual: full-120 `routing_trace` on/off latency arm → DECISIONS.md) |
| Capability contract (`Step.RequiredCapabilities` + planner emission from manifest vocabulary + L1 hard-gate `required ⊆ manifest.Capabilities`) | backlog ROUTE-03 | Implemented (arm `execution.capability_contract`, **default ON since ADR-0100 P1** — capability-typed dispatch gates on it, so with it off L1 is a no-op and the D5 ladder never fires; was default off = byte-identical; planner uses `buildCapabilityClusterFromManifests`; fixed the `ManifestRecord` DTO that dropped `capabilities`; `InterviewWorker` now sources manifest caps — single source; the LLM CapabilityClusterer was RETIRED in ROUTE-04/ADR-0067. Offline eval: 100% emission, 0.95 routable, 0.62 gold-overlap. Residual: default-on pending plan-rubric no-regression) **CORRECTED 2026-07-28:** this arm was measured as inert and was not — `configs/tuning.local.json` set `resource_selector: "efe"`, which short-circuits `AssignVariant` regardless of `efe_traffic_percent: 0`, so 100% of sessions ran the EFE selector; `GatekeeperEFESelector.Select` rebuilt the `AuctionTask` from `{ID, Description}` only and dropped `RequiredCapabilities` one call before the gate that consumes it. L1 filtered 0 agents across whole runs. Fixed by carrying the contract on `domain.Intent` and into the rebuilt task in `gatekeeper_efe.go` + `ab.go`: L1 filter events 0 → 66 (`runs/rt_diag4` → `runs/rt_fix1`). **Any routing number taken before this is void.** The trap itself cannot recur: ADR-0100 P3 deleted the EFE selector and `resource_selector` is a retired config key, so there is no longer a setting that can silently take over selection. When reading an ARCHIVED routing run, still check whether `resource_selector` was set in the config it captured. |
| Native tool-calling (ADR-0097, Phases A+B) | 0097 | **Both phases implemented 2026-07-28.** *Phase A (kernel):* optional `domain.ToolCallingGenerator` leaves `domain.Generator` (22 files, 9 implementers) untouched; `OpenAIClient` implements it; health/concurrency/purpose decorators forward BOTH the call and the capability report; stop reasons normalize onto `domain.StopReason` where `IsFinished()` is a whitelist (only `end_turn`) and an unmapped value is `StopUnknown` = NOT finished; `ModelTurn.ShouldContinue()` checks both signals. Capability DECLARED via `native_tools` (providers.json), carried through FOUR field-by-field config copies. *Phase B (agent plane):* unary `GenerateWithTools` RPC + `ToolDefinitionProto`/`ModelToolCallProto`; contract **0067→0068**; `native-tool-calling` capability; gateway method reusing CONWIP/health/budget; SDK `substrate.generate_with_tools` + ReAct loop preferring it, latching to the prompt-encoded fallback on `UNIMPLEMENTED`/`FAILED_PRECONDITION` or a substrate lacking the method. **Wire constraints that cost a run each (ADR-0097 D7):** tool names must match `^[A-Za-z0-9_-]{1,64}$` (`mcp:filesystem/write_file` → 400) so the SDK sanitizes with a reverse map; argument schemas need a top-level `"type":"object"` (the registry stores `{"properties":{...}}`, which a provider rejects though the prose menu never cared); and the JSON tool_call action must be withdrawn from the menu when tools are attached natively, or the model takes the encoding that does nothing. |
| **Capability-typed dispatch** — agent selection as a pure kernel function; the auction is deleted (`internal/metabolism/dispatch/`) | 0100 | **P0+P1+P3 done; dispatch is unconditional** (there is no `bid_round` key to set — it is retired). Selection costs **zero RPCs, zero LLM calls, zero speculative agent boots**: `Gatekeeper.FindCandidates` already returns a merit-ranked slate, so dispatch is the auction with the bid round deleted — eligibility (L1 capability contract) → ranking (`ScoreMerit`) → pick → call, and ONLY the winner is booted. Motivation: the bids were hand-written keyword tables returning one of `{0.9,0.85,0.5,0.3,0.2,0.1}` (`propose()`; agents without an override bid the constant `0.5`), and soliciting one BOOTED the agent (`auctioneer.go:392→133→163`) — up to 5 cold starts per step, 4 discarded. `Dispatcher` implements `domain.StepDispatcher` (named `domain.Auctioneer` until P3), so it is a drop-in at the DAG call site; `MetabolismStack.Selector` picks it, while privileged organs (kg_extractor/docling/reranker — the Scout was retired 2026-08-07) call the agent transport directly through `domain.AgentCaller` — they bypass selection by construction. **D4 per-step policy**: a CHEAP (`MaxEnergy`) + VERIFIED (`CheckpointAfter`) step takes the cheapest competent agent, everything else takes merit-argmax (`dispatch_cheap_energy_max`, default 0 = argmax everywhere until the A/B lands; `dispatch_merit_floor` 0.5 stops "cheapest" meaning "worst"). **D5 resolution ladder** (`execution.capability_resolution`, default true): `NormalizeCapability` → authored alias map (`execution.capability_aliases`, DATA not embeddings — ADR-0067 rejected fuzzy matching because `read`/`delete` are embedding-close) → generalist tier (`general_purpose`) → typed `NoCapabilityMatchError` naming the unmatched tag AND the live vocabulary. Resolved caps come back in the fleet's DECLARED spelling because L1 compares verbatim unless `canonical_vocab` is on. The dispatcher still emits `AuctionEventPayload` (winner + slate, `Rationale="merit-rank (dispatch: no bid solicited)"`) so the `orchestration` suite can score both arms identically. Vocabulary review passed: every keyword table maps to an already-declared capability. **Verified unit-level only (33 new tests); the orchestration-suite gate has NOT run.** **P2 harness built**: `AuctionEventOp.selection_latency_ms` + `selection_boots` emitted identically by BOTH arms (contract **0071**, cap `selection-cost`; boot delta from `InstanceManager.BootCount`), aggregated by the suite into `selection_latency_ms_mean/_p95` + `agent_boots_per_task/_total`; A/B protocol in `cambrian-benchmarks/docs/orchestration.md`. Note the dispatcher's EventBus is wired at the composition root (the bus is built AFTER the metabolism stack — wiring it at construction captures nil and the arm goes silently unmeasurable). **P3 DONE 2026-08-07 — auction + EFE selector EXCISED.** The bid round, `ConductAuction`, the EFE selector, `ResourceSelector`, the A/B variant assignment and the yield subsystem are all deleted; `execution.bid_round` / `resource_selector` / `efe_*` / `auction_bid_timeout_ms` are retired config keys. Dispatch is unconditional. What was `internal/metabolism/auctioneer` is now **`internal/agentplane`** holding `Transport` — the gRPC connection pool plus `CallAgent` / `VerifyOutput` / `RequestProposalFrom` (that last one survives for the INTERVIEW worker, not bidding). Vocabulary renamed with it: `domain.Auctioneer` → `StepDispatcher`, `AuctionTask` → `DispatchTask`, `AuctionResult` → `DispatchResult`, `AuctionEventPayload` → `SelectionEventPayload`, `BidEntry` → `CandidateEntry`, `bypass_auction` → `bypass_selection`; `domain.AgentProposal` deleted. New narrow port `domain.AgentCaller` separates invocation from selection, so the privileged organs no longer depend on a mechanism they never used. **Proto is unchanged** — `AuctionEventOp` and its fields keep their names, so the contract stays `0093` and the mapper carries a deliberate `SelectionEventPayload → pb.AuctionEventOp` seam. Yield was NOT ported onto dispatch (P4): it had been inert since the selector was never wired in any shipped config, so it was removed rather than revived. **P2 is CLOSED as unrunnable, not pending** — it was specified as an A/B against the auction arm, and P3 deleted the arm, so the comparison can never be made; ADR-0100 carries a dated amendment recording that and the basis the decision actually rested on. What replaces it is an **absolute** dispatch baseline on the `orchestration` suite (`routing_accuracy` / `misroute_rate` / `no_candidate` as a regression floor), which has not run — it is blocked on LLM provider quota, not on engineering. Residual: that baseline, and P5 typed-failure cascade. |
| Explicit agent pinning (`Step.PreferredAgent` + `Step.AgentPin`; soft = L2-exempt + 0.25 merit boost, hard = bind directly) | 0096 | **Implemented, default ON** (`execution.agent_pinning`). Gives the planner a first-class channel for naming an executor: its prompt already invited "you may reference a specific agent ID" with no field to put one in, so under the capability contract it emitted `required_capabilities: ["terminal_agent"]`, which nothing can declare — all candidates filtered, step dead. Soft never bypasses L1 (priority ≠ eligibility) and never strands a step; hard skips the auction and errors on an unavailable or undispatchable name. Default-on because this is directive following, not a learned component. Carried on Step, AuctionTask, Intent AND `ExecutionPlan.Clone` — every field-by-field rebuild between planner and gate. Live E2E `runs/pin_live`. Cost: a hard pin returns one candidate, so the inter-step fallback loop has no runner-ups. |
| Benchmark-mode kernel flags: `execution.plan_preview_only` (return plan JSON, skip DAG), `execution.disable_interviews` (skip graded LLM interview), `execution.disable_scout` (skip pre-plan discovery) | backlog ROUTE-03 | Implemented (all default false, eval-only; power the ROUTE-03 offline routing eval) |
| ~~Scout usefulness logging: per-session `ScoutUsefulnessOp` on the operator feed~~ | backlog ROUTE-08.A | **FULLY RETIRED 2026-08-08.** It shipped as designed (logging only, zero behaviour change: `domain.ScoutUsefulnessEvent` emitted after execution in `server.Execute`, `DiscoveryReferencedByPlan` entity-id overlap, `DAGExecutor.ReplanCount()`; operator.proto `ScoutUsefulnessOp` oneof 24, contract 0049→0050; `scoring.scout_summary` → `scout_cost_share`). The Scout's deletion on 2026-08-07 took the emitter, leaving a metric that could only ever read empty — the "measured zero" trap, which reads as a measurement of nothing rather than an absent measurement. Now closed on all sides: **`scout_summary`/`scout_cost_share` are deleted** from the orchestration suite along with the feed parser and row field that fed them (an op that cannot arrive should not have a parser implying it might), the dead `DAGExecutor.ReplanCount()` accessor is gone (its `replanCount` field stays — it has 12 internal uses), and `ScoutUsefulnessOp` + oneof field 24 are marked **`deprecated = true`** with a comment stating nothing emits them and nothing will. The message and field number are RETAINED deliberately: the contract holds at `0093` and a reused number would make an old client mis-decode a new op. `proto-check` and `proto-breaking` both pass — a deprecation annotation is not a breaking change — and `ui/proto` + `cli/proto` are re-vendored byte-identical. Phase B (the invoke/skip gate) is cancelled, not deferred: there is no organ to gate. **Two claims in the 2026-08-08 truth pass were wrong and are corrected here:** the `scout-usefulness` capability was NOT still advertised (it is absent from `operatorCaps` in `app.go`; only a stale proto *comment* listed it, now fixed), and `EventTypeScoutUsefulness` was already gone. Both were asserted from a grep that matched comments — the same failure mode that row was documenting. |
| Watch observability: per-watch metrics + dry-run mode + journal backtesting | 0071 (backlog REACT-05) | Implemented (Prometheus/OTel export = residual). Premium `reactive/metrics.go` per-watch atomic counters (signals/fires/suppressions/dry-run/dead-letters/latency, incremented in `process`); `WatchConfig.DryRun` (evaluate + record would-fire, NEVER act); `reactive/backtest.go` `Backtest(cfg, afterSeq)` replays the journal (REACT-01) through a candidate condition without acting. Operator surface: RPCs `GetWatchMetrics`/`BacktestWatch` + `WatchConfigOp.dry_run`, via ports `domain.WatchMetricsReader`/`domain.WatchBacktester` (engine-satisfied, wired by type-assertion on the injected signal receiver; nil in OSS ⇒ Unimplemented). Contract 0053→0054 + cap `watch-observability`. Unit-tested. Residual: native Prometheus/OTel per-watch scrape; durable metric history |
| `schedule` watch source (cron-driven synthetic signals) | 0072 (backlog REACT-06) | Implemented (24h soak-drift = residual). Pure-Go 5-field cron parser `reactive/cron.go` (`@`-shortcuts, `*`/`*/N`/`A`/`A-B`/`A,B`, standard DOM/DOW OR-semantics, `Next`/`Prev` bounded-scan) + self-rescheduling scheduler `reactive/scheduler.go` held by the engine: per active schedule watch a `time.AfterFunc` at each cron instant emits `domain.Signal{FromAgent:"scheduler", Payload:{_scheduled_time,_occurrence,_cron}, Timestamp:scheduled}` via `OnSignal` — flows through the UNCHANGED condition/action pipeline. Wired via `RegisterConfig`(schedule-only)→upsert / `DeleteConfig`→remove / `SetConfigActive`→arm-disarm / `Start`→arm-all / `Stop`→stop-all; armed only between Start/Stop. `WatchSource.Cron`/`Timezone` + `WatchConfig.MissedFirePolicy` (`fire_once` catch-up on restart, exactly-once via REACT-01 schedule-time idempotency key, journal-gated; `skip` default). `WatchHandler` excludes schedule type from daemon ref-counting. Operator/proto: `WatchConfigOp.source_cron/source_timezone/missed_fire_policy`, contract 0054→0055 + cap `watch-schedule`. Cron + catch-up + delete/inactive tests pass. Residual: 24h soak-drift check; ui/cli re-vendor |
| Compile-time plugin architecture: `app.Plugin` + `Registry` + `Lifecycle` | 0074 | Implemented + reactive-lane live-validated (7/7). `app/plugin.go` generalizes the fixed `Options` hooks into a registry: **replace-one** points (`SetSignalReceiver`, `SetAuthorizer`, `SetIngressResolver`, `SetIdentityResolver`, `SetSubstrateConsultant` — conflict = startup error; `SetResourceSelector` was one of these and went with the auction in ADR-0100 P3) vs **add-many** (`AddTraceWrapper`/`AddGRPCService`/`AddLifecycle`/`SetAgentCallLogger` — composed). `applyPlugins` folds `Options.Plugins` into the effective Options + ordered `Lifecycle` set (started at boot, stopped reverse on shutdown). Tiering policy (ADR-0074): Tier-1 replace-one (Generator/Chunker/ResourceSelector/SignalReceiver), Tier-2 add-many, **Tier-3 never-pluggable** (scope/approval/budget gates, dispatch merit integrity, grounded-only, audit — pluggable security = privilege-escalation). NOT Go `.so` (CGO+Windows+version-lock ruled out); out-of-process gRPC reserved for untrusted plugins. Fixed a latent bug: the reactive engine was **never `Start()`ed** in a live kernel — now started via the plugin `Lifecycle`. Unit-tested (`app/plugin_test.go`: fold/conflict/lifecycle). Residual: widen extension points beyond reactive+selector (Chunker map needs threading through the memory stack; Generator) |
| AgentSource seam: unify agent registration behind a provider interface | 0075 | Implemented (plugin agents) + unit-tested. `app.AgentSource` (`Name()`+`DiscoverAgents(ctx) []domain.AgentDefinition`) is the seam agent registration flows through; `Registry.AddAgentSource`/`AddAgent` (regular, Tier-2 add-many) / `AddSystemAgent` (privileged — **explicit + logged grant**, `System=true` stamped, vs `AddAgent` forcing `System=false`). `registerPluginAgents` (app.go) discovers plugin sources and upserts via the same `reg.SetAgent` path as `registerModelAgents` — plugin agents participate in dispatch/scope/merit normally. No change to the bbolt filesystem seeder. Phase 2 (delivered): `BBoltAdapter.Seed` **inverted into discover-then-persist** (`storage.DiscoverFilesystemAgents` + `upsertDiscovered`; behavior-preserving — live-validated same 17 agents); reusable `app.FilesystemAgentSource` so a plugin contributes its own agents dir (health-gated: missing exec path skipped); **sidecar-preference** (Python agent prefers a sibling `<id>.manifest.json` over the `AGENT_MANIFEST` source-regex); **plugin MCP servers** (`Registry.AddMCPServer` → `MCPServerSpec`, connected alongside config servers, ADR-0043). Phase 3 (fully unified): the seam carries the **manifest** — `DiscoveredAgent{Definition, Manifest}` + idempotent `SetAgentWithManifest`/`UpsertDiscoveredAgent` (SourceHash idempotency preserved so no re-interview on reboot; unit-proven). The built-in scan is now **literally a `FilesystemAgentSource`**: `BootstrapStorage` uses `NewBBoltAdapterNoScan`, and the composition root registers the agents dir through a system-aware `FilesystemAgentSource` before the reconciles. Live-validated: fresh boot registers all 12 filesystem agents with `manifest=true` + correct system flags, no wrongful prune. `Seed` kept for tests/calibration-report. |
| Premium transport-plane extension: `app.Options.ExtraServices func(*grpc.Server)` | 0073 | Implemented + live. An inert OSS seam lets a downstream (premium) binary mount ADDITIONAL gRPC services (defined in its OWN proto) on the kernel server after the core services and before Serve, inheriting the server-level operator auth interceptors — the OSS `operator.proto` contract stays untouched. First consumer: the premium `ReactiveControl` plane (`EmitSignal`) that lets the benchmark harness inject synthetic signals to exercise the reactive guarantees. Composed through the ADR-0074 registry (`AddGRPCService`). Reconciled with invariant #2 (that governs the UI contract; this is a premium-only, authenticated, non-UI plane). |
| Additive licensed plugins: manifest, capability ownership, Build phase | 0082 | **Phase 1 implemented** (of 5; ADR is `Proposed`). `Plugin` declares a data `PluginManifest{ID, DisplayName, Version, Requires, Capabilities, Panels}`; `Manifest()` **replaces** `Name()` so a plugin has exactly one identity (its ID is also the future entitlement key). **D2 — capability ownership:** plugin-declared capabilities are collected by `applyPlugins` (deduped, order-stable) and appended to the operator handshake; the seven premium `watch-*` strings moved OUT of `app/app.go` into the reactive plugin's manifest, so the OSS kernel no longer names — or knows the meaning of — any premium feature. **D10 — dependencies:** `Requires` is topologically ordered (dependencies register first) and an unmet dependency **skips** the plugin non-fatally (`PluginStatus{State:"deps_unmet"}`, cascading to dependents) rather than failing boot — with subscriptions an unmet dependency is a billing combination, not a crash. Cycles/duplicate IDs remain hard errors. **D12 — Build phase:** optional `Build(KernelServices)` runs after the stacks exist and before serving, in dependency order, removing the pointer-capture hack where the reactive engine was built inside the signal-receiver hook and stashed for the gRPC registrar to find. **D7 (staged):** `KernelServices` added as an alias of `ReactiveServices` so plugins can migrate to the neutral name before the fields are split. **D11:** premium reorganized into isolated `plugins/<name>/` packages, guarded by `check-plugin-isolation.sh` (no plugin may import a sibling). Unit-tested (`app/plugin_test.go`: capability collection/dedupe, dependency order, non-fatal + cascading unmet deps, cycle + duplicate-ID errors, Build ordering). **Phase 2 implemented:** `EntitlementProvider` seam + chokepoint in `applyPlugins` (D3) — consulted BEFORE a plugin registers and BEFORE dependency resolution, so an unentitled plugin contributes nothing (no caps/lifecycles/agent sources/gRPC) and its dependents cascade to `deps_unmet`; nil provider ⇒ allow-all (OSS default); a provider error **fails closed**; an expired-but-in-grace plugin still activates and reports `expired`. `KernelServices` is now the real bundle type with `ReactiveServices` a **deprecated alias** (D7 rename half). **Chat extracted into its own plugin** (`cambrian-premium/plugins/chat`) — it takes `Manager`+`AcquireLLMToken` from the Build bundle instead of riding reactive's hook — and `execution.chat_manager_addr` was **removed from the OSS config**, closing a standing ADR-0057 D5 violation (the premium chat plugin reads `CHAT_MANAGER_ADDR`). **Not yet built:** Ed25519 licence verifier + issuance (D5, deferred until billing), business-package layer (ADR-0083), `ListPlugins` RPC (D9), plugin-owned operator protos (D8), `PluginStore` (D7 persistence half). **Sequencing note:** `PluginStore` is blocked *behind* D8 — it cannot replace `WatchStore`/`Journal` until `domain.WatchConfig` leaves OSS `domain/`, which ~11 operator-plane references prevent until the watch RPCs move to a premium-owned proto. |
| Conversation model + session pool + pluggable manager | 0084 | **Phase 1 of 5 implemented** (ADR is `Proposed`; **amends ADR-0080**, whose planner-bypass rule is unchanged and must not be weakened). Phase 1 = the data model that was simply missing: the kernel had **no `Message` or `Conversation` entity at all**, `domain.Session` is a *task* container despite its doc comment, operator `SendMessage` is `Unimplemented`, and the premium manager kept an in-memory transcript that died on restart. Shipped: `domain.Conversation`/`Message`/`ConversationProfile` + `ConversationStore` port; `PgConversationStore`; migration `0002_conversations.sql`; `Kernel.Conversations` (nil + warn when unmigrated — chat surfaces disable, the kernel still boots). `Seq` assignment is race-free by row lock (proved by a 20-goroutine concurrency test), and `ClientID` gives retry idempotency without burning a seq. Verified live against Postgres (6 integration tests) plus 6 pure-domain tests; the integration test applies the **real migration file** so it cannot drift from shipped DDL. **Phase 2 implemented:** `internal/agentpool` (bounded stateless worker pool, admission-gated shed, load-balanced dispatch, lost-vs-execution failure split) and `internal/chat` (`TurnService`), wired behind `execution.chat_pool_size` (**default 0 = disabled**) with `chat_pool_agent_id`/`chat_pool_queue_size`/`chat_pool_acquire_timeout_seconds`; the pool runs as a kernel-contributed `Lifecycle` (started at boot, drained on shutdown). Chat is now an **OSS** capability, so these keys name no premium feature (ADR-0057 D5 holds). 16 new tests, race-clean. The OSS chat worker ships as `agents/chat_agent.py` (the pool's default agent id), so OSS chat is self-contained. It is deliberately GENERIC — domain behaviour arrives as `policy` data, plus generic tool-posture flags (`tools_full`/`max_tool_rounds`) read off the turn — so it carries no benchmark/domain specifics. The premium chat manager keeps dispatching to the premium `chat_session_agent` (the airline/customer-service agent), so the benchmark's agent stays OUT of OSS and the τ²-bench airline path is unchanged by this work (no re-run required; the benchmark does not exercise the Phase-2 pool). **Phase 3 (operator half) implemented:** operator-plane conversation surface (`OpenConversation`/`SendTurn`/`CloseConversation`/`ListConversationMessages`), **contract 0060→0062** (0061 conversation surface + 0062 `ListConversations` sidebar), capability `chat`. `SendTurn` routes through the worker pool, NOT the planner — the old `SendMessage`/task-session path stays on the planner by design (task work vs chat turn = the D2 distinction). Client-supplied `conversation_id` ⇒ idempotent Open; principal-stamped ownership enforced per call; migration `0003` adds optional per-conversation policy. 7 handler tests + store integration green (both migrations); codegen idempotent. **Contract skew recorded:** UI pins 0060, CLI 0047 — not re-vendored; safe because the RPCs are additive and `chat` is capability-gated. **Not yet built:** Phase-3 D6 agent-plane `SendTurn` + daemon-agent manager + UI surface; Phase 4 session-noun refactor (`SessionToken`→`BudgetLease`, `streamID` split); Phase 5 `caller_scope` Phase-2 wiring. |
| Daemon supervision: auto-restart with exponential full-jitter backoff + flap quarantine + recovery/quarantine events | 0070 (backlog REACT-04) | Implemented (crash policy; heartbeat = residual). `agentmgr/restart_policy.go` `DaemonRestartPolicy` (per-stream sliding-window attempts; `Base·2ⁿ` capped at `Max`, full jitter; MaxAttempts→quarantine). `handleDaemonExit` captures spawn params → restarts after backoff or quarantines + emits `DaemonQuarantinedEvent`; success emits `DaemonRecoveredEvent` and the premium ReactiveEngine re-marks the stream available. Default-on via `execution.daemon_restart_*` (`max_attempts=0` disables = pre-REACT-04). Unit-tested + **live kill chaos test** (`daemon_chaos_test.go`: real subprocess kill→restart→quarantine via a `DaemonBootHook` seam, 5× stable). Residual: hung-daemon heartbeat liveness (SDK + monitor + signal-path), degraded-watch flag in `ListWatches` |
| Per-capability merit + bounded provisional exploration | 0069 (backlog ROUTE-06) | Implemented (arm `execution.per_capability_merit`, default off = byte-identical). D5 fix: `TaskEvent.Capability` stamped from the step's first required cap (`dag_executor`), aggregator `computeCapabilityStats` → `AgentProfile.CapabilityStats` (per-tag EWMA), L3 `computeMeritBreakdown(ctx, agent, requiredCaps)` reads tag-scoped success/trust with global fallback. Unit-tested (per-tag stats, tag-scoped merit). **The bounded-exploration half was DELETED 2026-08-08 — the arm is now tag-scoped merit and nothing else.** As shipped it added `domain.ExplorationBudget` (N provisional wins/capability/window) which the Gatekeeper consulted before granting the provisional L2 bypass, plus `ExplorationBudgetExhaustedEvent`. `RecordWin` was called from exactly one place, the Auctioneer, and ADR-0100 P3 deleted it — after which `wins` stayed empty, `Allowed` always returned true, and the event could never fire. The bound had therefore been inert for months, masked because the read sat behind this same default-off arm; it would have *armed itself* as an unlimited bypass the moment ROUTE-06 was switched on. Removed rather than re-wired to the Dispatcher, on the owner's call: **a bound that cannot bind is worse than no bound, because the config key reads as a guarantee.** Deleted: `domain/exploration_budget.go` (+test), `ExplorationBudgetExhaustedEvent`, `EventTypeExplorationBudget`, `Gatekeeper.ExplorationBudget`, the `app.go` wiring, and the `provisional_exploration_budget` / `provisional_exploration_window_seconds` keys (recorded in `retiredExecutionFields` with the reason; the field-loss guard is mutation-checked against both). The provisional L2 bypass is now unconditional in both arm positions — the behaviour that actually ran all along. If exploration needs bounding again it belongs on the **Dispatcher**, which is the component that now knows a candidate won. Residual: benchmark gate + per-tag data accrual; v1 keys by a single capability |
| Learned gatekeeper scorer | 0076 (backlog ROUTE-07) | 🔶 Partial (arm `execution.learned_scorer`, default off = byte-identical). `internal/metabolism/routescorer/`: inspectable pure-Go **logistic regression** over the EXACT `meritBreakdown`/ROUTE-02-funnel fields `[success_rate, trust_score, latency_term, cost_term, provisional]` (so a training sample is a direct funnel read — no reconstruction), standardized features, GD+L2, JSON-persisted with schema-drift guard. Offline pipeline `cmd/route07-scorer` {`extract` runs→samples, `train` → learned-vs-hand **AUC** on held-out split + `adopt` gate verdict}. Online: `Gatekeeper.RouteScorer` replaces the hand-weighted score (+ skips the cold-start penalty — provisional is a feature) when armed + `learned_scorer_model_path` loads; missing/invalid model ⇒ hand weights (never a silent 0). Unit-tested (train/AUC/save-load/schema/gate on synthetic). Residual: the offline WIN over the hand-weighted baseline needs accrued funnel+verifier runs — collected from ordinary dispatch runs now, since ADR-0100 P3 deleted the auction and `resource_selector` with it (an older note here said to run with `resource_selector=auction`; there is no such setting); then online A/B + DECISIONS.md; GBT/retrain-job are follow-ups |
| ~~Bid calibration (ROUTE-05)~~ | 0068, superseded by 0100 | **RETIRED 2026-08-07 with the auction.** It calibrated a BID toward expected verified quality; ADR-0100 P3 deleted the bid round, and the only consumer was `Auctioneer.Calibrator`. Removed: `cmd/calibration-report`, `internal/kernel/calibration.go` (`BidCalibrationSamples`, which had no caller), and the `calibrated_bids` / `bid_calibration_min_samples` config keys — recorded in `retiredExecutionFields` so the flat→nested field-loss guard states WHY rather than just tolerating the gap. The pure algorithm package `internal/metabolism/calibration` (isotonic PAVA) is left in place with **no consumer**: it is self-contained and reusable if a later scorer wants monotone calibration, and unlike a half-wired subsystem nothing can accidentally appear to depend on it. Delete it if that bet expires. |
| Capability vocabulary: retired the LLM CapabilityClusterer + deterministic capability normalization | 0067 (backlog ROUTE-04) | Implemented (reframed per user: the embedding/fuzzy canonicalizer was REJECTED — fuzzy cosine merges risk wrong merges, e.g. `file-read` ↔ `file-write`, which misroute worse than the variance they fix. Delivered: **deleted `internal/supervision/clusterer`** + its `SupervisionStack`/`SweepTrigger` wiring; capabilities are the ones agents declare. `domain.NormalizeCapability` (lowercase/trim/collapse `-`,`_`,space) applied to BOTH sides of L1 `PassesDeclaration` + the planner vocabulary under `execution.canonical_vocab` (default off = ROUTE-03 verbatim). Format/typo variance matches with **zero wrong-merge risk**; cross-word synonyms (`browser`≡`web-navigation`) deliberately out-of-scope → curated vocabulary. Unit-tested. Residual: A/B confirm + L2 threshold sweep) |
| Container distribution: multi-stage `Dockerfile` + top-level `docker-compose.yml` + GHCR publish workflow (CPU + `-cuda`) + opt-in model pre-fetch | 0066 (backlog PLAT-04) | Partial (artifacts authored: `cambrian-core/Dockerfile` (golang:1.25 → python:3.12-slim, kernel+agents colocated, non-root, PLAT-01 venv, PLAT-03 `/healthz` HEALTHCHECK, PLAT-02 migrate-on-boot), `Dockerfile.dockerignore`, monorepo-root `docker-compose.yml` (db+kernel+pagerank+optional ollama, health-gated), `.github/workflows/publish-image.yml`. **Build context = monorepo root** (the SDK is the sibling `sdk/` repo). GPU = `TORCH_INDEX_URL` wheel swap; model pre-fetch = `PREFETCH_MODELS=1`. **Validated:** the Go builder stage compiles both binaries in-container. **Release-time (not validated here):** runtime stage (multi-GB torch/docling), clean-machine `docker compose up`, image sizes, GHCR tag push) |
| gRPC health service: standard `grpc.health.v1` on the main listener + DB-gated readiness probe + drain-before-stop + optional `/healthz` HTTP shim | 0065 (backlog PLAT-03) | Implemented (`internal/health.Checker` wraps grpc's `health.Server`; readiness = `pgxpool.Ping` (default 10s, `server.health_check_interval_seconds`), NOT gated on agents; sets `""` + `cambrian.OperatorConsole`. Registered on `k.GRPC`; `Kernel.Shutdown` flips sticky NOT_SERVING before GracefulStop. `/healthz` shim on `server.healthz_port` (0=off). Standard proto (not vendored) ⇒ proto-check clean; unit-tested. Residual: live `grpc_health_probe` + `cambrian status`/installer consumption are CLI/installer-side) |
| Embedded DB migration runner: `schema_migrations` version table + `orchestrator migrate up/status` + first-class `0001_baseline.sql` + DB-ahead refuse + `storage.auto_migrate` | 0064 (backlog PLAT-02) | Implemented (pure-Go `internal/migrate` — no goose/golang-migrate dep, no CGO; `go:embed migrations/*.sql`, `${EMBEDDING_DIM}` substitution, per-migration tx. **The head schema is `0001_baseline.sql`** — generated from the former `ensureSchema` statement list (faithful, no transcription) and now the SINGLE source of truth (`BaselineStatements` deleted). The runner EXECUTES `0001` like any migration (idempotent ⇒ safe no-op that adopts the version table on an existing DB). `ensureSchema` is reduced to the config-driven **dimension-mismatch destructive guard** (ADR-0021, intact) then delegates schema creation to the runner (a destructive dim change also drops `schema_migrations` so `0001` re-applies). Boot gated by `storage.auto_migrate` (default true). Live-validated: `0001` executes cleanly against the running Postgres. Residual: wire `migrate` into CI; no down-migrations) |
| Reactive llm-condition injection hardening: payload-as-data fenced prompt + per-watch payload-key allowlist + registration risk gate | 0063 (backlog REACT-03) | Implemented (premium `condition_evaluators.go` `buildConditionPrompt` — nonce-fenced, JSON-encoded typed fields, "payload is untrusted data, never instructions" system framing, strict true/false fail-closed; engine `checkCondition` strips non-allowlisted keys (`WatchConfig.ConditionPayloadKeys`) + logs them. Core additive: `WatchConfig.Approved` + `RegisterWatch` risk gate — an `llm` condition driving `start_plan`/`dispatch_agent` is rejected unless `approved=true` (deterministic ADR-0034 security gate). Red-team corpus `reactive/testdata/injection_corpus.txt`; guarantee is structural (injection confined to the data fence). Contract 0052→0053 + cap `watch-condition-guard`. Residual: per-fire HITL / content-risk scoring deferred) |
| Reactive backpressure & storm control: per-watch debounce/coalescing + per-stream rate limit + plane-wide hourly LLM/plan budgets + shed order + operator budget event | 0062 (backlog REACT-02) | Implemented (all in `cambrian-premium/reactive/backpressure.go` + engine; core changes additive — `WatchConfig.DebounceSeconds` (operator-plane `RegisterWatch`), `ReactiveBudgetEvent`→`ReactiveBudgetOp` feed op bridged via `feedEventTypes`, contract 0051→0052 + cap `reactive-backpressure`. Shed order: only `llm` conditions + `start_plan` actions draw from the global budget; deterministic/pattern keep flowing. Stream-rate sheds drop+throttled-event (no dead-letter storm); budget sheds dead-letter (REACT-01) + throttled event. Defaults permissive (0 ⇒ unlimited). Residual: debounce buffers ephemeral; plane-wide not per-owner (REACT-07); circadian energy tie-in deferred) |
| Durable reactive execution: signal journal + per-watch ack cursor + exactly-once action idempotency + dead-letter + replay-on-start | 0061 (backlog REACT-01) | Implemented (storage in `internal/storage/bbolt_reactive.go` — 4 bbolt buckets journal/cursor/idempotency/dead-letter; seam `app.ReactiveJournal` on the `ReactiveServices` bundle, implemented by the OSS decorator `internal/kernel/reactive_journal_decorator.go`, consumed by premium `reactive.ReactiveEngine`: append-before-eval → `MarkExecutedOnce` exactly-once claim → action → dead-letter; conservative per-watch ack cursor never skips an unprocessed signal; `Start` replays from cursor, TTL-expired signals dead-lettered not re-run. **A nil journal preserves the pure in-memory engine** — OSS default. Operator read surface `ListWatchDeadLetters`, contract 0050→0051 + cap `watch-deadletter`. **GOV-02 (2026-08-01) closed the GC residual**: `reactive.JournalGC` runs bounded passes on a timer, floored at the lowest cursor across every registered watch, announcing each pass on the feed. Residual: ui/cli proto not re-vendored (skew recorded); a `stress`-suite forced-*process*-restart scenario is a follow-up — exactly-once + no-loss are unit-proven) |
| Reproducible agent envs: auto-generated per-agent `requirements.txt` + union `requirements.lock` + drift check + manifest `python_deps` dependency self-check | backlog PLAT-01 | Implemented (`scripts/gen_agent_requirements.py` / `make agent-reqs[-check]` — AST import analysis + `importlib.metadata`, no pip-tools; deterministic drift gate. `agentmgr/python_deps_check.go` `find_spec` pre-check names a missing dep at boot instead of a silent ImportError crash. Residual: system agents don't yet declare `python_deps`; clean-venv install is the CI/PLAT-04 gate) |
| Agent sandboxing Tier 0: env allowlist (agents no longer inherit kernel secrets) + memory resource caps | backlog SEC-01 | Implemented (`agentmgr/env_allowlist.go` deny-by-default env in `buildAgentCmd`, secrets stripped; `process_caps_{windows,linux,other}.go` pure-Go **no-cgo** memory cap — Windows Job Object kill-on-close, Linux RLIMIT_AS — behind **per-agent** `manifest.memory_limit_mb` (declared limit wins; **system organs exempt from the global default** so docling/reranker/torch are never killed by a fleet-wide number) with `execution.agent_memory_limit_mb` (default 0) as the default for user agents; `execution.agent_env_passthrough` for non-secret extras. Live-verified agents spawn+run with keys stripped. Residual: memory-bomb live-kill test, per-agent scratch dir, Unix pgroup grandchild-kill) |
| Experiential memory: typed records, online graph, scenes-as-world-model, precedent lanes | 0049 | Implemented (issues 001-014 + A1.1/A1.2 entity drift signal; residual: API endpoint-set is last-observed, LLM contradiction-edges deferred) |
| ~~Grounded planner: pre-plan discovery Scout (privileged `run_think` agent over `ToolExecutor.RestrictedTools`)~~ | 0051 | **RETIRED 2026-08-07.** It was implemented and shipped (issues 001-006 closed 2026-06-23; the Go `awareness.Scout` organ detour was reversed and the agent-side path became the authority), then removed whole: `internal/discovery/`, the `scout_agent` principal, its `discovery_safe` tool ceiling, and nine config keys — all listed with reasons in `retiredExecutionFields`. What went with it: the ROUTE-08.A usefulness signal (see that row — the proto field and the bench metric outlived the emitter) and `<DiscoveryLTM>` in the planner prompt. The planner is no longer pre-grounded by a discovery pass; grounding now comes from memory priming and, for knowledge questions, the ADR-0118 retrieval path. |
| KG²RAG retrieval: chunks + per-chunk triplets + tiered extraction (frozen `kg_extractors/` pipeline, Tier-1 metadata + Tier-2 spaCy patterns, LLM demoted to opt-in Tier-3) | 0053 | Implemented (D2 revised + wired 2026-06-25: vector seed + one-hop `kgExpand` shipped; `kg_extractor_agent` is a privileged system organ, behind `execution.kg_extractor_enabled`) |
| Multi-signal ranking: stage-A blend weights (cosine / lexical / coherence / confidence / pagerank / recency / activation) | 0054 | Implemented (LoCoMo recall@10 0.673/0.662 → 0.763/0.766; `SetRuntimeConfig` hot-swap; reranker deferred) |
| Multi-strategy chunking pipeline (pluggable `domain.Chunker` port; 5 chunkers `option_c` / `recursive_character` / `ast_go` / `markdown_header` / `late`; data-driven registry with `SourceType → ext → default` precedence; `chunk_relations` 512B budget; `source_document` entity via `DocTypeMnemonicEntity`, GC-exempt; `ChunkDocument` back-compat shim) | 0060 | Implemented (the `option_c` arm is the back-compat floor; `late` is gated on `chunker.late.enabled` + `embedder.supports_long_context` + `chunker.late.max_doc_tokens=8192`; over-budget docs fall back to `option_c` with a `late_fallback` log) |
| Semantic tool retrieval (embed-rank, `find_tools`, hybrid push+pull) | 0044 | Implemented (residual: HITL benchmark + floor/k tuning deferred) |
| Global Workspace context (ContentStore push/pull, `ContextRef` cid offload) | 0022 | Accepted (design) (the prime/pull surface is wired; full-derivation rollout follows ADR-0048 follow-ups) |
| Kernel-derived write classification (DefaultWriteTags, agent may only narrow) | 0035 | Accepted (design) |
| Trait-aligned cognitive SDK v2 (CognitiveAgent, DeterministicAgent, DaemonAgent) | 0036 | Accepted (design) |
| Central-Executive Planner / EFE selection | 0037 | Accepted (design) — **and the EFE-selection half is now dead.** Phase-1 modules sit in `internal/centralexec/`, wired nowhere; the `CapabilityBelief` loop is unwired by design. ADR-0100 P3 deleted the EFE selector and the `ResourceSelector` port it bound through, so the A/B spike this was gated on cannot be run as specified. Revisiting it means gating against dispatch instead. |
| GAIA benchmark instrumentation (PRD lives, instrumentation surfaces pending) | 0050 | Accepted (design) |
| Open-core boundary (separate premium module, BSL 1.1, OSS = source of truth) | 0057 | Accepted (design) |
| Kernel-owned tool registry (System Tool reference monitor: grant → resource → data scope → approval → budget) | 0039 | Proposed (gated on the falsification spike; legacy `internal/tool/` will land here) |
| Hermes capability migration (one-shot consumer migration to the kernel-owned registry) | 0040 | Proposed (depends on ADR-0039) |
| Memory answer agent (synthesis LLM call over KG²RAG results; another privileged system organ) | 0055 | Proposed in this shape and not built. Its *purpose* was met elsewhere: the ADR-0081 operator answer lane (`AnswerMemory`, row above) and the ADR-0118 deterministic consultant both produce grounded cited answers over retrieval. Read this row as "ADR-0055's specific organ design was not what shipped", not as "Cambrian cannot answer from memory". The privileged-organ roster is now kg_extractor, docling and reranker — the Scout, which used to head that list, was retired 2026-08-07. |
| Community detection (Layer 3 of the KG²RAG roadmap) | 0056 | Proposed (ADR-0053 Layer 3; design space explored, not yet proposed) |
| Deep Kernel migration (Wasm sandbox + WASI-HTTP interception) | 0004 | Cancelled (2026-05-06; agents run as OS processes over UDS; the Wasm walls are gone) |
| ~~Pre-plan Scout: deterministic-first discovery (probe registry filesystem/system/http; LLM demoted to opt-in; selection matches request words against real names, Aider-style)~~ | 0078 | **RETIRED 2026-08-07 with the Scout** (see the ADR-0051 row). It amended 0051 and lived in `internal/discovery/`, which no longer exists. Note what the retirement did NOT take: R2 parametric fan-out (row below) is independent of discovery and still shipped. |
| Parametric fan-out steps (discover-N → do-N; deterministic append-expansion of the plan, no replan) | 0078 R2 / 0051 D10 | Implemented (`domain/fanout.go`; DAGExecutor + `max_fanout_width`; planner emits `fan_out_over`) |
| Config-load robustness (configs/.env/agents_dir/data_dir resolved against the binary, not cwd) | — | Implemented (`config.ResolveBaseDir`; the bug that motivated it was the Scout silently disabling under a benchmark supervisor — the Scout is since retired, but the fix was never Scout-specific and still governs `agents_dir` / `data_dir` / `.env` resolution for every organ) |
| Roster latch (agent→capability roster seeded from manifests + heartbeat) | 0047 | Implemented (`internal/substrate/operator/roster_latch.go`; survives `disable_interviews` + late `--no-supervise` subscribers) |
| Agent-loop observability (`AgentStepOp` on the feed: memory_query thrash + retrieval-provenance poisoning) | 0047 | Implemented (contract 0058, cap `agent-steps`) |
| LLM-exchange provider tap (`AgentLLMExchangeOp`: full prompt+completion of every managed-proxy agent turn on the feed; the ordered sequence reconstructs an agent's whole ReAct loop for benchmark review) | 0079 | Implemented (contract 0059, cap `llm-exchange`, gated `execution.capture_llm_exchanges` default off; live-only/never-replayed, fire-and-forget, zero behavior change) |
| Experiential memory (step-result / plan-scene / procedural / negative-edge / episodic / scope-promotion write-back) | 0015/0025/0029/0030/0034/0049 | **REMOVED** (unwired 2026-07-18; implementations retained; corpus ingestion kept). Redesign proposed: **ADR-0049 §A2** — abstraction-only writes, prediction-error write gate, lane-aware non-displacing truncation, corpus/experience trust split; batch consolidation now permitted (§A2.5 reverses the original rejection). Gated on a memory benchmark (§A2.7 Phase 0). |
| Experiential memory Phase 1-2 (outcome records + surprise-gated failure precedents + lane-aware truncation) | 0049 §A2.2/A2.3/A2.4 | **Implemented, ON BY DEFAULT (2026-07-28).** `execution.experience_records_enabled` (**true**) writes ONE abstracted record per plan — the D7 projection + engaged refs + action path + outcome — parented to an `experiences` row and born-tagged. `execution.experience_surprise_floor` (0.5) gates the extra failure precedent on \|merit expectation − actual\|, a SURPRISE gate not a failure gate. Recall truncation is lane-aware (`assembleLanes`): experience never displaces a corpus primary in a knowledge query, nor corpus a precedent in a precedent query. The raw path (`experiential_memory_enabled`) stays false permanently — that, not this, is what was removed in July. Enabled because `foreign_share` cannot detect displacement until experience has volume, and volume only accrues with the arm on; pre-enable baseline `runs/lg_1`/`lg_2` is the drift reference. See `docs/research/experiential-memory/DECISIONS.md`. |
| Procedural tier (induced, capability-tagged, advisory routines; re-keys the ADR-0030 generalizer off plan text onto the ADR-0049 D7 situation projection) | 0094 | **Implemented, default-off, never exercised on real data.** `execution.procedure_induction_interval_hours` (0 = disabled) schedules the offline induction pass ADR-0049 §A2.5 permits; `procedure_min_samples` (2), `procedure_max_episodes_per_pass` (500), `procedure_deprecate_below` (0.3). Deterministic clustering on capability-shape + situation; one bounded LLM naturalisation call that fails open. Steps name CAPABILITIES, never agents — an agent-named routine would be a learned routing table. Cannot induce anything until plans accumulate under the `capability_contract` arm. **AMENDED 2026-07-28 (ADR-0094 A1):** the D3 clustering key could not match two runs of the same task — measured on the first real corpus (19 stored scenes, 15 successful) it produced 19 clusters and ZERO promotions at min_samples=2. Causes: the trigger embedded the planner's free-text goal with per-run sandbox paths/filenames/quoted literals and volatile engage-counts, and the capability SEQUENCE varied run to run for one task (step count 1..3, tags regrouping). Fixed by clustering on the capability SET (ordered sequence kept on `ProcedureCandidate.Sequence` for the procedure body), a properly normalized trigger (strip paths/filenames/literals/digits, drop stopwords, stem, compare as sorted token set), and `canonicalGoal` stripping paths+literals from the stored projection. Measured 0 → 2 clusters / 5 episodes; both halves attributed and asserted in `procedure_realdata_test.go` against the real scenes, with a guard that the two task families do NOT fuse. **D8 CO-EVOLUTION LOOP CLOSED 2026-07-28:** `PlanRecord.FollowedProcedures` was declared and READ (the memory agent feeds each named routine's confidence from the outcome and deprecates it below `procedure_deprecate_below`) but written by nothing, so the branch was unreachable and no routine's confidence ever moved — the tier could influence a plan and never learn whether it helped. Three hops now carry it: `internal/awareness/planner.go` records the IDs of the routines `ltmEnrichment` supplied, `ExecutionPlan.Clone` deep-copies them through the plan freeze, and `dag_executor.go` passes them to the `PlanRecord`. Each hop is separately mutation-checked — the executor hop was found untested because `TestProcedureFeedbackLoop_ClosesEndToEnd` builds its `PlanRecord` directly and passes with that hop deleted. Confidence is still only measurable at volume: one plan moves one routine by one sample. |
| `experiences` entity table (parent row per episode, mirroring `documents`→`chunks`; born-tagged surface+`internal` classification; cascade delete; `experience_derivations` link table) | 0095 | **Schema implemented** (migration `0010_experiences`, applied to dev 2026-07-27; scratch-verified incl. cascade delete; LoCoMo smoke clean). Writers NOT built — D3 retag-transaction, D4 born-tagged stamp and D6 retention ship with them, gated on ADR-0049 §A2.7 Phase 0. Table lands **empty**: no `plan_id` back-fill while the write path is unwired and its record shape is changing. Extends ADR-0093 to a second entity class and narrows its D3. |
| Config + secret store in bbolt, with per-key provenance (`internal/config/provenance.go`, `internal/config/store.go`, `internal/storage/bbolt_config_store.go`, `app/config_store.go`) | 0101 | **Implemented 2026-07-29.** The 11-layer Koanf pipeline becomes 12: the embedded store sits ABOVE every JSON file and BELOW `CAMBRIAN_*`, so an operator's write outlives a restart while a deployment configured by environment still wins unchanged. `LoadConfigWithStore` also returns a `config.Provenance` — which layer supplies each flat key — gathered by observing what each layer CONTAINS rather than by diffing the merged result. That distinction is load-bearing: a diff-based tracker reports "default" for a key a file explicitly pins to its default value, telling the operator to edit the wrong thing. Secrets live in a separate bucket, AES-256-GCM under a key from `CAMBRIAN_SECRET_KEY` or a 0600 file beside the store; the only decrypting method is unexported and off the `SecretStore` interface, so no read RPC can reach a credential. Store lives at `configs/config.db`, NOT the data dir — a corpus reset truncates the data dir and would otherwise take every durable setting and credential with it. `CAMBRIAN_CONFIG_STORE=off` reproduces the pre-0101 pipeline exactly (benchmark arms). A store that exists but will not open FAILS THE BOOT rather than degrading, because booting with silently-wrong config is the failure this ADR exists to prevent. |
| Operator console Wave 1 — contract **0072** (`internal/substrate/operator/wave1_reads.go`, `generators.go`, `config_schema.go`; `app/wave1_sources.go`, `app/config_schema_source.go`) | 0101 + `operator_ui_refactor/KERNEL-REQUIREMENTS.md` | **Implemented 2026-07-29.** Eight additive changes in ONE bump: `ListSessionCheckpoints` (run-keyed — checkpoints are stored per RUN and a step index is meaningless without one, so every row carries `run_id`+`plan_id`), `ListMCPServers` (with `configured`, separating "no servers" from "no MCP subsystem"), `GetEmbeddingConfig` (`vector_count` -1 = uncountable, never 0), `ClassifyInput` (the FIVE-value router vocabulary — `ingest` must not be collapsed, it WRITES to memory), `ListGenerators`/`ListRoleAssignments`/`TestGenerator` (`model_served` is the echoed model, the only way to catch an endpoint answering with a different build), `RetryWatchDeadLetter`, plus `PlanStepOp.required_capabilities` and `PluginInfoOp.last_check_unix_ms` + the `licence_unverifiable` state. `GetConfigSchema` completes the pair: `SetRuntimeConfig` could write a tunable since 0047 and nothing could read one, so the form showed documented defaults labelled as a guess. Every source is nil-able and answers **Unimplemented, never an empty success** — a console cannot otherwise tell a healthy deployment from a broken one. Capability strings are derived from which sources are wired. `domain.RouterDecision` gained `Why`/`Confidence` (layer 3 already computed both and discarded them). |
| Durable configuration + credential WRITE path — contract **0073** (`internal/substrate/operator/config_write.go`, `app/config_write_source.go`) | 0101 D3/D5 | **Implemented 2026-07-29.** `SetConfig` / `DeleteConfig` / `SetGeneratorKey` / `ClearGeneratorKey`, mutating + audited. Deliberately distinct from `SetRuntimeConfig`, and both survive: that one is ADR-0054's automated-tuning seam (ephemeral, keyed by param name, written by a tuner that must not leave permanent marks), this one is a human saying "make it this and keep it this way". Returns **per-key** `ConfigWriteOutcomeOp` rather than one ack, because a partial set can partly succeed and a single ok/fail hides which field did not take. Four effects: `live` (applied + stored), `restart_required` (stored; read only at boot), `shadowed` (stored, but a higher layer supplies the live value — **reported AT WRITE TIME naming the variable**, which is ADR-0101 D3 and the reason the response has this shape), `rejected` (unknown key or out of range; skip-and-warn so one bad key cannot lose the valid writes beside it). Validation is range-checked against the same catalogue `GetConfigSchema` publishes. Credentials: the key never enters the audit record — only the fact that one was set — and a key filed against a non-existent generator is REFUSED rather than stored invisibly. `DeleteConfig` exists because "put it back" has no other expression: writing the old value over the top leaves the key pinned by the store, so a later edit to the file underneath silently does nothing. Capability `config-write`, advertised separately from `config-schema` — a kernel can report its configuration without being able to persist a change to it. |
| Operator console Wave 2, plane A — contract **0074** (`internal/substrate/operator/submit_plan.go`, `plan_validation.go`, `watches_a2.go`, `reads_a2.go`, `roster_latch.go`) | `operator_ui_refactor/KERNEL-REQUIREMENTS.md` §3 | **Implemented 2026-07-29.** Four items. **(1) `SubmitPlan`** — operator-authored DAGs. `AuthoredStepOp` is `domain.Step` field-for-field including the ADR-0096 soft/hard pin split. Enters execution through `network.AuthoredPlanMetadataKey`, shaped like `ResumeMetadataKey` rather than as a second entrypoint, so session binding, run persistence, checkpointing and the feed are all the EXISTING code — a parallel path would be a second thing to keep in step, and whichever was used less would drift first. Validation reuses `executer.TopologicalSort`, the function the executor itself runs, so a dry run cannot pass a plan the executor then rejects. `dry_run` creates/starts/spends nothing. Issues split fatal (cycle, out-of-bounds dep, empty query, hard pin on a missing agent) from advisory (soft pin on a missing agent, fan-out without a dependency) — conflating them either blocks valid plans or runs broken ones. **(2) Watch liveness** — `source_stream_state` + `source_stream_refcount` on `WatchConfigOp`, closing the reactive plane's quietest failure: when a stream's daemon crashes every watch on it stops evaluating with no error, no dead letter and no event, while each still reads "active". State defaults to `unknown`, never `live`. **(3) `last_fires`** + `GetReactiveBudget` (running totals vs caps; the existing `ReactiveBudgetOp` is a SHED event, i.e. the line already crossed). All counters -1 when untracked, distinct from 0. **(4) Contract-0057 detail fields** — `ToolOp.schema_json` + resource-arg lists, `SkillOp.body`, and nine `AgentReadyOp` fields folded by agent id, so six half-rendered panes fill from the ordinary feed rather than needing the removed per-entity getters back. **Bug found and fixed in passing:** `toWatchConfigOp` silently dropped `MissedFirePolicy`, so a REACT-06 schedule watch edited through the console reverted to `skip` and stopped catching up after a restart. |
| `KernelServices.Agents` — agent enumeration for plugins (`app/options.go`) | 0074 | **Implemented 2026-07-29.** `AgentExists` answered "is this one real?"; a plugin needing "which ones ARE" had no route, which is why the premium `ListPrincipals` could not tell a live principal from one only a policy link mentions. Deliberately the read-only `domain.AgentLister` and nothing wider: a plugin that could MUTATE the registry could mint principals for itself, and the entire value of the orphan check is that it compares against a set the plugin does not control. |
| Contract **0075** — token series + propose-only planning (`internal/telemetry/token_series.go`, `internal/awareness/clarify.go`, `internal/substrate/operator/{token_series,propose_plan}.go`) | `operator_ui_refactor/KERNEL-REQUIREMENTS.md` §3 #15 + Wave 3 T3.1 | **Implemented 2026-07-29.** **`GetTokenSeries`** — hourly buckets behind the spend sparkline. A DECORATOR over `domain.TaskEventWriter` rather than a new emission site: every path that records step usage already goes through `WriteTaskEvent`, so wrapping the one chokepoint cannot drift from what the kernel recorded, whereas a parallel emission site would need adding at each caller and the first one anybody forgot would silently under-report. One accumulator shared across executions (the writer is resolved per `Execute`, so a per-call accumulator would reset the series every plan). Idle hours are returned as ZERO points, never omitted — a sparse series compresses idle time and draws a quiet night as continuous activity. Zero-token events are not counted as calls, so the figure measures spend rather than steps. Tokens only; there is no cost field anywhere. **`ProposePlan`** — plans WITHOUT committing: no session, no run, no spend. Reaches the planner directly rather than through `Server.Execute`, because Execute is the committing path and suppressing its side effects afterwards would make the promise depend on every future edit to that function remembering this caller. A vague goal returns QUESTIONS **instead of** a proposal (never alongside — offering a runnable plan next to "I need to ask you something" invites approving the plan and skipping the question). Proposal steps come back in exactly the `AuthoredStepOp` shape `SubmitPlan` accepts, including the -1 fan-out sentinel and the soft/hard pin, so "Run it" is a straight handoff. `access_consequence` is returned on EVERY branch including the question branch — an operator narrowing a scope should already know what the broad answer would expose. **`internal/awareness/clarify.go` is deliberately NOT in `internal/metabolism/interview/`**, which is the AGENT examiner (ROUTE-03): one word, two subsystems, and colocating them would read as reuse. The clarifier FAILS OPEN — an LLM error yields no questions rather than blocking the proposal. |
| `DocumentFilter.Tags` (`internal/memory/document_entity.go`, `internal/infrastructure/postgres/document_list.go`) | 0075 | **Implemented 2026-07-29.** Tag-intersection filter on the already-GIN-indexed `documents.tags`, via `@>`. Added so a clarification option can say "61 documents" against the SAME listing the documents screen reads — two counts from different queries would eventually disagree about the size of a scope the operator is choosing between. Intersection rather than union on purpose: a scope is a narrowing, and OR would make selecting a second label WIDEN it. **Consequence:** `DocumentFilter` is no longer a comparable struct; tests comparing it must use `reflect.DeepEqual`. |
| Contract **0076** — Wave 3 part 1 (`internal/substrate/operator/{access_policy,blast_radius}.go`, `domain/signal.go`, `cambrian-premium/reactive/reactive_engine.go`) | `operator_ui_refactor/KERNEL-REQUIREMENTS.md` Wave 3 T3.7/T3.8/T3.9 | **Implemented 2026-07-30.** **`ExplainAccessBatch`** (T3.9) — exists to DELETE a client-side re-implementation. A console rendering an NxM grid cannot make NxM round trips, so it evaluated the policy set itself; that is a second implementation of access-control logic where a wrong "allowed" cell is a security-shaped bug. Every cell now goes through the same `ExplainAccess` the enforcement path asks. Answers are POSITIONAL and a malformed cell holds its slot with a DENIAL — skipping would shift every later answer, and a matrix silently off by one is worse than one that refuses to render. Bounded at 400 (a 20x20 grid, larger than anyone reads) with `truncated` reported so a partial grid is never drawn as complete. **Multi-action watches** (T3.8) — `WatchConfig.Actions` carries arms 2..N alongside the retained singular `Action`, so every watch stored before 0076 loads unchanged; `EffectiveActions()` is the accessor. Arms run in order and INDEPENDENTLY: a failing arm does not cancel later ones, because a notification that already went out has been seen and aborting would leave the pipeline half-applied while reporting a clean failure. **`GetBlastRadiusPreview`** (T3.7) — the effect of a scope/grant mutation on AGENTS and IN-FLIGHT PLANS, distinct from the document-count blast radius the scope screens already compute. Unknown mutations are REFUSED rather than previewed as no-change; an unwired kernel returns Unimplemented rather than an empty radius, because an empty preview reads as "safe to apply". `complete=false` + `incomplete_reason` when something could not be inspected. `scopeDirection` errs toward **widened** when ambiguous: narrowing breaks a task and someone notices, widening breaks a boundary and nobody does. |
| Identity bindings — contract **0077** (`domain/identity_binding.go`, `internal/ingress/inbound.go`; `cambrian-premium/authz/{identity_registry,admin_identity}.go`) | Wave 3 T3.2 | **Implemented 2026-07-30.** Closes the state where **the SURFACE IS THE IDENTITY**: a policy links to `chat:telegram`, so every sender who finds the bot has identical reach — `@unknown_4471` is governed by the same rule as `@afsin`, not because they are different people but because nobody is. **Placed on the AUTHZ plane, not the telegram one** (a correction to `KERNEL-REQUIREMENTS.md` §2.5, which said plane C): a binding maps an external id onto a PRINCIPAL, which is this plane's vocabulary; the reach figure needs this plane's resolver; and the mapping is surface-generic rather than specific to one bridge. Bindings key on the platform's **stable numeric id**, and `looksLikeUsername` ENFORCES that rather than documenting it — a username-keyed binding follows whoever claims the name next. Three targets (`principal` / `group` / `room_group`); the third is a delegation kept as its own kind so an audit can tell a delegated room from a named person. **The block drops at the ingress**, in `InboundService.Accept` BEFORE a conversation is resolved: a later drop would still have opened a conversation and left a transcript, so dropping here is what makes "a blocked sender never reaches a policy or a plan" true rather than aspirational. `refuse_until_bound` returns `ErrSenderNotBound` so the caller REPLIES once — silent ignoring is indistinguishable from a broken bot. The unbound worklist fills itself from the resolve path (an operator cannot bind someone nobody told them about) and is bounded at 500, because an entry point is addressable by anyone and an unbounded worklist is a memory leak with a spam vector attached. `readable_documents` is -1 when uncomputable and 0 only under `refuse_until_bound`, where zero is a fact rather than a failure to measure. |
| Telemetry administration plane — contract **0078**, premium-only (`cambrian-premium/api/proto/telemetry/`, `cambrian-premium/langfuse/{admin_service,test_trace}.go`) | Wave 3 T3.4 | **Implemented 2026-07-30.** A NEW premium gRPC plane, mounted through the same `ExtraServices` seam as `AccessPolicyAdmin` and `TelegramAdmin` (ADR-0073), advertising capability `telemetry-admin`. **Deliberately NOT on `operator.proto`:** ADR-0057 D5 removed Langfuse from the OSS config schema because the open-source module names no premium feature, so putting these RPCs on the OSS contract would re-add exactly what was taken out and make an OSS kernel advertise a surface it cannot serve. **`SetTelemetrySecretKey` is its own RPC, not a field on `SetTelemetryConfig`** — folding it in would make the config round-trip carry a credential and force the console to resend the key on every unrelated save (a sample-rate change, a switch flip); separating it keeps `TelemetryConfig` safe to read, log and diff. The three disclosure switches default to **structure-only**, because defaulting content ON would ship prompts to a third party merely because somebody enabled tracing; enabling `send_prompts_and_outputs` warns at the moment it happens, and combining it with `send_from_enforcing_surfaces` warns harder — anything the access policy protects inside Cambrian is NOT protected once it is a trace. An env var shadowing the stored key is reported AT WRITE TIME naming the variable, and clearing the store warns when the environment still supplies one (an operator who believes they revoked tracing has not). **`SendTestTrace` earns its place** because the tracer DROPS what it cannot send rather than blocking a plan — correct behaviour, and an invisible failure, so a misconfigured endpoint is indistinguishable from a working one from inside the product. It is synchronous, carries the endpoint's own words on failure, echoes the host so a success against the WRONG project is visible, and sends no content of any kind. |

---

## Core Philosophy

Cambrian is a **Deep Kernel Substrate**: the Go kernel treats agents as
untrusted cells in a managed sandbox, owns the composition root, and refuses
to let routing, scoring, or selection live in Go conditionals. Three rules
thread through every organ:

- **Strict hexagonal separation.** `domain/` is the importable lingua franca
  (zero external dependencies, every security-critical decision lives here).
  Adapters (Postgres, gRPC, BBolt, LLM clients, MCP) sit at the edges and
  contain no business logic. The boundary is compile-time-enforced; a breach
  is a build failure, not a runtime check.
- **Capability-typed dispatch (ADR-0100).** Work is not hard-routed to agents.
  The Planner emits a natural-language `query` plus `depends_on` indices plus
  `required_capabilities` drawn from the manifest vocabulary; the Gatekeeper
  filters to a small merit-ranked slate (L1 capability hard-gate → L2 trust →
  L3 merit); the Dispatcher takes the argmax and calls it. Selection is a pure
  kernel function: **zero RPCs, zero LLM calls, and only the winner is booted.**
  The Zero-Hardcode Rule forbids any Go `if/else`/`switch` on agent identity,
  with three deliberate exceptions: system-shell commands, the reflexive path
  (latency), and security gates (scope, approval, budget). Those exceptions are
  the only type-aware code in the routing path.
  > **This replaced the Auction model on 2026-08-07 and the auction is deleted,
  > not disabled.** Until then the Auctioneer solicited a bid from each candidate
  > and picked on `GatekeeperScore`. The bids were hand-written keyword tables
  > returning one of a fixed six values (agents without an override bid the
  > constant `0.5`), and soliciting one BOOTED the agent — up to five cold starts
  > per step, four of them discarded. So the market was paying real latency for a
  > signal that carried almost no information. The consequence to keep in mind:
  > selection now rests entirely on merit, with no market averaging over a bad
  > score, which is why ROUTE-06/07 became more important rather than less.
  > The names went too — `domain.Auctioneer` → `StepDispatcher`, `AuctionTask/Result`
  > → `DispatchTask/Result`, `BidEntry` → `CandidateEntry`. **The proto did not**:
  > `AuctionEventOp` keeps its name so the operator contract holds at `0093`.
- **The open-core boundary (ADR-0057).** Premium code (reactive rule engine,
  langfuse tracing) lives in a separate Go module and plugs in via
  `app.Options`. The OSS module never imports premium code; the CI script
  `scripts/check-no-premium.sh` enforces it, with the *policy* owned by
  `cambrian-premium/` per ADR-0057 D13. The kernel's value is the OSS
  composition root, the gRPC/OperatorConsole surface, and the
  memory/scoring/dispatch machinery, all public under BSL 1.1.

The biological model is incidental to the rules: long-term memory (pgvector
Engram engine with `activation_strength`), cellular metabolism (agent
process lifecycle + resource quotas), prefrontal awareness (LLM Planner with
workspace priming), and nervous regulation (Watcher proactive signals +
PauseController HITL). The mapping is in `docs/ARCHITECTURE.md` §Design
principles; the rules above are the load-bearing ones.

---

## Module Breakdown

The kernel is **strictly layered**. A breach (a `domain/` file importing
`internal/infrastructure/`, an adapter importing `internal/awareness/`) is a
build failure caught by `scripts/check-no-premium.sh` and the
`make separability` target. Every layer below `domain/` is replaceable from
inside `domain/`'s ports.

| Path | Role |
|---|---|
| `domain/` | Pure domain types and ports (importable, public). All security-critical decisions (Gatekeeper, TrustScore, scope, classification) live here, with zero external imports. Includes `decision_observer.go` — the ADR-0103 provenance seam (`DecisionObserver`, `RetrievalDecision`, `RetrievedChunk`), which names only concepts OSS already owns so the word "receipt" never enters the core. |
| `domain/tool_calling.go` | ADR-0097 native tool-calling vocabulary: `StopReason` (+`IsFinished`), `ToolDefinition`, `ModelToolCall`, `ModelTurn` (+`ShouldContinue`), the optional `ToolCallingGenerator` and `ToolCallingReporter` interfaces, and `SupportsToolCalling`. Named `ModelToolCall`/`ModelTurn` because `domain.ToolCall`/`ToolCallResponse` already mean kernel tool EXECUTION (ADR-0039) — the model requests, the kernel executes, and the two must not share a name. |
| `app/` | Composition root. `app.Run(ctx, opts)` wires every subsystem; `app.Options` is the open-core extension seam (premium injects via hooks). `app/plugin.go` (ADR-0074/0082) generalizes the hooks into a `Plugin`/`Registry`/`Lifecycle` system with a data `PluginManifest` (id/version/requires/capabilities/panels); `Options.Plugins` are dependency-ordered and folded by `applyPlugins`, then constructed by `buildPlugins` (the ADR-0082 D12 Build phase). |
| `cmd/orchestrator/` | Thin `main` shell over `app.Run`. |
| `internal/kernel/` | Subsystem assembly: four stacks (`MemoryStack` / `AwarenessStack` / `MetabolismStack` / `SupervisionStack`) and `ProvideServer`. The only cross-subsystem wirer. |
| `internal/awareness/` | Planner / Cortex. Produces `ExecutionPlan`; owns the Zero-Hardcode rule at the LLM layer; `ReplanHandler`, `ConsolidatorAgent`, `XMLParser`. |
| `internal/metabolism/` | AgentManager, Gatekeeper, verifier pool, interview worker, DAG executor, A2A connector, and `dispatch/` — the selection machinery. There is no bidding machinery: the `auctioneer` package became `internal/agentplane` (`Transport` — the gRPC connection pool and the typed calls over it, with no selection in it at all) when ADR-0100 P3 deleted the auction. |
| `internal/memory/` | LTM: pgvector store adapter, hippocampus (procedural templates), KG²RAG retrieval (`kgExpand`), scene/edge writers, `WorkspaceStage`, `SpreadingEngine`, `ProfileStore`, `ArtifactVault` (CAS), `MemoryManager` / `MemoryAgent` / `MemoryWorker`. **Chunking pipeline (ADR-0060):** `chunker_registry` (data-driven `SourceType → ext → default` routing, Zero-Hardcode clean), `chunk_relations` (per-chunk 512B parent+linear+sibling metadata), `chunkers/` subpackage (the 5 implementations: `option_c` / `recursive_character` / `ast_go` / `markdown_header` / `late`), `OptionCChunker` (the 115-line back-compat floor), `ChunkDocument` shim over `OptionCChunker`. **Structure-aware pipeline (ADR-0060 default):** `structure_graph.go` (leaves-as-chunks + `BuildStructureGraph`), `structure_retrieval.go` (`applySectionConstraint`), `neighbor_window.go` (off by default), `anchor_query.go` (document-local anchor constraint), plus the `docling_agent` parse RPC and `postgres/structure_store.go` writer. **Document entity (ADR-0093):** `document_entity.go` defines `SourceDocument` + the optional `DocumentStore` port (a document has no embedding and is never a search result, so it is deliberately NOT part of the vector store); `postgres/document_store.go` implements it, `postgres/document_retag.go` carries `RetagDocument` — the transactional re-classification that makes a half-classified document impossible — and `tableFor()` in `pgvector_adapter.go` routes every read and write to the table that owns the type. |
| `internal/evidence/` | The knowledge substrate's evidence write path (ADR-0105): `Ingestor` owns the content-first commit ordering (bytes → verify → atomic evidence+outbox → only then ACK). `Stage`/`FetchStaged` (ADR-0112) expose step 1 alone for the raw-delivery lane — bytes durable, CID out, evidence row later via `ingest_raw`. Imports `domain/` only; the Postgres adapter is `internal/infrastructure/postgres/evidence_store.go`, the schema is migration `0011_evidence.sql`, and the capture seam is `IngestionManager.SetEvidenceCapture`. |
| `internal/supervision/` | Gatekeeper, verifier pool, `ProfileAggregator`, `SynapticWatcher`, `Watcher` (signal-to-inspiration), `MemoryLifecycleManager` (event-driven session lifecycle, replaces `CircadianRhythm`). |
| `internal/substrate/` | gRPC server (`network`), session, OperatorConsole plane (`operator/`), synaptic event log, knowledge-graph extractor dispatch. |
| `internal/authz/` | Access-control ENFORCEMENT points (ADR-0085). `EnforcingVectorStore` (fail-closed read chokepoint), `EnforcingStoreWriter` (write classification), artifact filters, and the gRPC surface interceptor. Contains no policy: every question is delegated to `domain.Authorizer`. Not pluggable — a missing plugin must mean "no policy", never "no check". (Replaces `internal/scope/`, whose DECISION half moved to `cambrian-premium/authz`.) |
| `internal/infrastructure/` | Adapters: `postgres/` (pgx + pgvector), `llm/` (LLM client + `LLMProvider` broker), `mcp/` (MCP connector — OUTBOUND), `mcpserve/` (the MCP endpoint — INBOUND), BBolt storage DTOs. |
| `internal/infrastructure/mcpserve/` | The inbound **Cambrian MCP endpoint** (ADR-0126 D3/D4): renders `domain.PublishedToolSurface` as one `mcpsdk.Server` over the SDK's streamable HTTP transport with `Stateless = true` (spec 2026-07-28; the SDK negotiates down for older clients), behind one middleware that establishes everything before any protocol handling — constant-time bearer compare against the ADR-0101 secret `mcp:client:<name>`, then the D4 identity hop (`Options.Identity`: a `domain.IdentityResolver` when a plugin installed one, otherwise the client name IS the principal), surface `mcp`, a per-credential concurrency cap answering 429, then per-call effect authorization through `domain.Authorizer` with `ResourceKind "tool"`. Each call also mints a D6 correlation handle onto the context and returns it as `ReceiptRef`, which is what makes an answer’s receipts fetchable by whoever received the answer. Zero published tools is a legal state (`tools/list` answers empty). The direction is the whole point: nothing from the sibling `mcp/` connector is ever re-published outward. Its listener is built in `app` (`app/mcp_endpoint.go`) through `transportCredentials` + `secureListener`, so SEC-03's refusal holds for it too. |
| `internal/tool/` | System-tool lifecycle (Python modules, `ToolRegistry`, confined `ProcessHandler`, jail sweep into CAS). Will be superseded by ADR-0039's kernel-owned registry. |
| `internal/skill/` | System-skill discovery and registry; agent skills live in the Python SDK (`agent.local_skills`). |
| `internal/storage/` | BBolt adapter (DTOs only, zero `domain/` imports). `AgentRepoDecorator` wraps storage as the domain interface; `BootstrapStorage` is the only constructor. Durable reactive execution lives in `bbolt_reactive.go` (ADR-0061: journal/cursor/idempotency/dead-letter buckets); the decorator surfaces it as `app.ReactiveJournal` via `internal/kernel/reactive_journal_decorator.go`. |
| `internal/mapper/` | Proto-to-domain translation; the only bridge between `pb` and `domain` types. |
| `internal/agentplane/` | `Transport` — the agent-plane gRPC connection pool plus the typed calls over it (`CallAgent`, `VerifyOutput`, `RequestProposalFrom`, which survives for the INTERVIEW worker and not for bidding). **This is `internal/metabolism/auctioneer` renamed by ADR-0100 P3, with the selection removed**: it decides nothing about which agent runs, it only reaches the one it is told to. `domain.AgentCaller` is the narrow port privileged organs hold, so they depend on invocation without depending on a selection mechanism they never used. |
| `internal/metabolism/calibration/` | Pure isotonic-PAVA calibration with shrinkage. **No consumer since 2026-08-07** — it was ROUTE-05's bid calibrator and ADR-0100 P3 deleted the bid round along with `Auctioneer.Calibrator`, `cmd/calibration-report` and `internal/kernel/calibration.go`. Kept as a self-contained algorithm, not as wiring: nothing can accidentally appear to depend on it, and ROUTE-07 may want monotone calibration of its scorer output. Delete it if that bet expires. |
| `internal/health/` | gRPC `grpc.health.v1` checker (PLAT-03 / ADR-0065): DB-gated readiness probe + drain-before-stop + optional `/healthz` HTTP shim. Registered on the main gRPC listener. |
| `internal/migrate/` | Pure-Go DB migration runner (PLAT-02 / ADR-0064): `schema_migrations` version table, `go:embed migrations/*.sql` (head schema is `0001_baseline.sql` with `${EMBEDDING_DIM}`), per-migration tx, DB-ahead guard. `postgres.ensureSchema` (dimension guard only) delegates to it; `orchestrator migrate up/status` is the CLI. |
| `embedded.go` (module root) + `app/setup*.go` + `app/lifecycle.go` | Self-installing kernel (ADR-0122): `setup` subcommand bootstraps prefix layout, self-copy into `<prefix>/bin`, local Postgres via generated compose (pgvector-checked), uv-provisioned Python agent runtime (uv downloaded on demand), embedded-agents unpack (`all:agents` — keeps `__init__.py`, filters `__pycache__`), merge-written config bundle, in-process `migrate up`, detached start + DB-gated grpc.health.v1 readiness poll. `status`/`stop` are the minimal lifecycle (pid file `orchestrator.pid`, logs under `logs/`). LLM providers deliberately NOT configured here (operator console / ADR-0101). Replaces the retired Bun CLI's `cambrian init` + `start/stop/status`. |
| `domain/conversation.go` | ADR-0084 D1: `Conversation`, `Message`, `ConversationStatus`, `ConversationProfile` (+`Recall()`), and the `ConversationStore` port. Pure domain — validation only, no persistence. |
| `internal/infrastructure/postgres/conversation_store.go` | `PgConversationStore` (`domain.ConversationStore`). `Seq` is assigned by `UPDATE conversations SET next_seq = next_seq+1 ... RETURNING` **inside the append transaction**, so it takes the conversation row lock and concurrent turns cannot collide (a `SELECT MAX(seq)+1` would race). The same statement carries the open/closed gate. Unlike the older audit store it does **not** create its own tables — schema is owned by migration 0002 (ADR-0064) so the DDL exists once. |
| `internal/migrate/migrations/0002_conversations.sql` | ADR-0084 D1 schema: `conversations` (+`next_seq` counter) and `conversation_messages` (`UNIQUE (conversation_id, seq)`, partial unique index on `client_id` for retry idempotency). |
| `internal/agentpool/` | ADR-0084 D4: a bounded pool of interchangeable daemon workers over the `SpawnDaemon`/`CallDaemon`/`StopDaemon` seam. `Size` workers on synthetic stream ids; an admission gate of `Size+QueueSize` sheds **immediately** with `ErrPoolBusy` rather than queueing without bound; `AcquireTimeout` sheds a waiter. Dispatch is load balancing, **not** sticky — any worker serves any turn because the turn carries its own context. Start is all-or-nothing (a partial pool would silently serve at reduced capacity). On failure it distinguishes a **lost** worker (not live ⇒ respawn + `ErrWorkerLost`, the turn never ran) from an **execution** failure (worker still live ⇒ surface as-is, no respawn) — the pool never retries, because only the caller knows whether re-running a side-effecting turn is safe. Free of chat concepts, so reusable. |
| `internal/chat/` | ADR-0084 D4 OSS turn path: `TurnService.Turn` loads the conversation, appends the user message, threads a bounded history window, dispatches ONE turn to the pool, and appends the reply. Retry-safe — a replayed `ClientID` returns the original reply without dispatching again. Emits the ADR-0080 per-turn handoff contract (`_conversation_id`/`policy`/`transcript` metadata + `_session_token_id` lease) so `chat_session_agent` works unchanged, plus `profile`/`recall_lookup` so the conversation's posture travels with the turn (D7). The planner is not involved. |
| `internal/substrate/operator/conversation.go` | ADR-0084 D9: the operator-plane chat lane — `OpenConversation`/`SendTurn`/`CloseConversation`/`ListConversationMessages`. `SendTurn` routes through the pool (`TurnService`), NOT the planner. Ownership is stamped from the resolved principal and enforced per call (non-owner ⇒ PermissionDenied). `conversation_id` is client-supplied ⇒ Open is idempotent. Capability `chat`, gated on `k.ChatTurns != nil`. |
| `app/conversation_ops.go` | Composition-root adapter binding `operator.ConversationOps` to the conversation store + `TurnService` (keeps the operator package free of kernel handles). |
| `internal/migrate/migrations/0003_conversation_policy.sql` | ADR-0084 D9: optional per-conversation `policy` column (threaded into every turn; set once at open). Append-only migration (0002 already applied). |
| `agents/chat_agent.py` | ADR-0084 D4: the OSS conversational worker the chat pool runs (id `chat_agent`). Owns ONE turn in a single ReAct loop — the planner is reachable only via an explicit yield, never by decomposition (ADR-0080). Stateless per call: policy/transcript/posture all arrive on the task, which is what lets interchangeable pooled workers serve any turn. Honours `recall_lookup` (D7) so a customer-facing conversation cannot reach shared memory, and reads `tools_full`/`max_tool_rounds` per turn so one agent serves both light chat and tool-grounded customer service. A spoken-only guardrail replaces any internal error/reasoning/trajectory leak with a non-committal fallback — never a fabricated action. |
| `internal/telemetry/` | The only package importing OTel. `Bridge` translates `TelemetryObserver` calls to Prometheus + OTLP. Runtime-config-gated, no build tags. |
| `internal/config/` | Koanf **12**-layer loader (ADR-0024); merges built-in defaults, `tuning.json`, `tuning.local.json`, `config.json`, `config.local.json`, `embedder.json`, `embedder.local.json`, `providers.json`, `providers.local.json`, `mcp.json`, **the ADR-0101 embedded store (layer 11 — where durable operator writes land)**, then `CAMBRIAN_*` env (layer 12, highest). |
| `internal/centralexec/` | Phase-1 modules for the Central-Executive Planner (ADR-0037): `capability_catalog`, `ladder`, `precision_shaper`, `seed_precision`, `verifier_signal`, `belief/`. **Wired nowhere, and its stated gate can no longer be met** — it was gated on an A/B spike against the auction, and ADR-0100 P3 deleted the auction along with the EFE selector this code was to bind through. So the gate is not "not yet run", it is unrunnable as written, and `domain.Intent` survives mainly for these types. If active-inference selection is ever revisited it needs a new gate against **dispatch**; until then treat the package as a design artifact, not a dormant feature. |
| `internal/router/` | Universal input router (ADR-0031); classifies inbound traffic into reflex / cognitive / signal lanes. |
| `internal/reactive/` | Reactive rule engine plug surface (OSS side; premium `cambrian-premium` provides the implementation via `app.ReactiveServices`). Durability (ADR-0061) is injected through the bundle's `Journal app.ReactiveJournal` field — the engine appends before eval, claims exactly-once, dead-letters failures, and replays from the cursor on start. |
| `internal/service/` | Cross-cutting services (PII masker, audit ledger, scope resolution glue). |
| `internal/benchmarks/`, `internal/testing/` | Internal benchmark and test fixtures (kernel-side; the public harness is `cambrian-benchmarks`). |
| `api/proto/` | The gRPC/protobuf contract: `cambrian.proto` (legacy Orchestrator), `operator.proto` (OperatorConsole, the held-stable UI surface per ADR-0047). Generated Go stubs sit alongside. |
| `agents/` | Production Python agents auto-discovered by BBolt (`*agent.py` + `AGENT_DESCRIPTION`/`AGENT_MANIFEST`): `code_generator`, `code_executor`, `terminal`, `summariser`, `analyst`, plus the system organs `scout_agent` (ADR-0051), `kg_extractor_agent` (ADR-0053 D2 revised), `docling_agent` (ADR-0060 structure-aware ingestion; lazy Docling backend), and `reranker_agent` (ADR-0054, built + deferred, lazy model load). |
| `pkg/` | Reusable internal packages with stable enough semantics to import across the `internal/` wall (currently `util/`). |
| `scripts/` | Build, test, CI helpers: `check-no-premium.sh` (premium-leak audit), `check-separability.ps1` (hexagonal boundary), `run-tests.ps1` / `run-all-tests.ps1`, `setup-python-runtime.{ps1,sh}`, `chaos-compose.yml`, `toxiproxy-config.json`. |
| `configs/` | Committed starter config: `tuning.json` (curated power-user starter, 13 fields), `*.example.json` templates for config/embedder/providers/mcp. The real `*.json` files are gitignored (live secrets). |
| `db/migrations/` | Postgres migrations 002-011 (`add_document_type`, `activation_strength`, `hnsw_cosine`, `ebbinghaus_decay`, `document_edges_update`, `add_summary_column`, `chunk_triplets`, `chunk_triplets_confidence`, `chunk_pagerank`, `fts_index`). |
| `data/` | Runtime data: `content_store_blobs/` (CAS for `ArtifactVault` offload), plus the `vault/` tree (content-addressed agent products). |

---

## Memory & context model

Memory is an **Engram engine**, not a flat vector store. The kernel reads
typed documents by `DocType*` (Fact / Action / Scene / Entity / Episodic /
AgentProfile / ProceduralTemplate / NegativeEdge / Tool / Skill), writes
through a two-tier pipeline, and primes the agent with a bounded,
relevance-ranked bundle. Eight decisions are load-bearing:

- **SCENE + FACT pairing (ADR-0015).** A completed step writes up to two
  coupled documents: a `DocTypeMnemonicFact` (the structured result) and a
  `DocTypeMnemonicScene` (a snapshot of `masterContext` at step completion).
  A FACT is knowledge; a SCENE is the conditions under which that knowledge
  is true. `WorkspaceStage` fetches both. The pair decoupled scene creation
  from the Tier-2 FULL threshold (ADR-0025 D4), which in practice was never
  reached and left the SCENE corpus empty.
- **Two-tier write pipeline (ADR-0015).** A new memory lands first in the
  in-memory Tier-1 channel (immediately queryable within the same run). A
  background Tier-2 LLM-as-Judge scores it and commits to LTM as FULL
  (FACT+SCENE), FACT_ONLY, or DROP, with a heuristic fallback on timeout.
  `activation_strength` ∈ [0,1] is the lifecycle metric; effective relevance
  at read = `cosine × (α + (1−α)·activation) × e^(−λ·age)` (floor-multiplier
  + temporal decay). The exploration slot (Matthew-effect guard) is OPT-IN as
  of 2026-08-12: a caller must set `SearchOptions.ExplorationRate > 0` to get
  it. The old adapter default (5% when unset, minimum one slot) meant every
  retrieval path — including the RAG fact lane — had random rows injected into
  its primary pool and non-deterministic results; no production caller ever
  opted in, so nothing enables it today.
- **Graph layer (ADR-0017, expanded ADR-0025).** `document_edges` carries
  `closes` / `specifies` / `contradicts` / `discussed_in` edges. The
  `SpreadingEngine` does GraphRAG BFS, attenuating `0.75^depth`. Live
  execution writes `specifies` (scene_N → scene_{N-1}) and `discussed_in`
  (committed fact → producing scene) deterministically; only `closes` and
  `contradicts` need LLM semantic reasoning and remain the Consolidator's job.
- **Global Workspace, push/pull (ADR-0022 + ADR-0048).** The kernel pushes
  context, the agent pulls more. `WorkspaceStage.PrimeForStep` assembles a
  bounded, relevance-ranked bundle (SCENE+FACT + episodic + spreading
  neighbours), past a contradiction guard, offloads heavy payloads to the
  content-addressed `ContentStore` (CIDs), and populates
  `Handoff.WorkingMemory`. The agent reasons over that bundle and pulls more
  on demand via `memory_query` (the same pattern `find_tools` and
  `find_skills` reuse). This replaced the old O(N²) context growth with a
  push + on-demand pull model.
- **Context hygiene (ADR-0048).** Recall no longer re-injects the run's own
  step output: `QueryService.Search` drops same-session `step_N` records (D1)
  and applies a cosine `RecallSimilarityFloor` (an all-irrelevant query
  returns empty, the agent answers from its own knowledge, knowingly). Every
  promoted memory carries a one-line `summary` (Tier-2 descriptor,
  `documents.summary` column, migration 007); recall serves the summary as
  the agent-facing surface with the full body behind
  `metadata["content_cid"]`. A `{"$cid":"…"}` tool arg is hydrated
  kernel-side so existing content is referenced, not re-emitted. The agent
  loop renders executed calls as condensed `<step>` cards inside a numbered
  `<Trajectory step="N">`.
- **Experiential memory (ADR-0049) + KG²RAG (ADR-0053).** Memory is typed by
  intent: a side-effecting tool call is a `mnemonic_action` (recorded
  directly, bypassing Tier-2 keep/drop); a read is a `mnemonic_fact`. Each
  plan writes one immutable scene at completion (two-faced: a reconstruction
  with engaged refs + baseline CIDs, and an abstracted projection for
  situational match). Touched things become first-class `mnemonic_entity`
  records keyed by canonical `kind:id` (mutated-only minting, field-LWW
  rebuildable cache, action-driven supersession). The world model carries a
  **precedent lane** (push via `PrimeForPlanning` `<PrecedentLTM>`, pull via
  `recall_precedents`), failure-weighted and similarity-gated; the LLM only
  reasons over precedents. KG²RAG complements this: chunks become
  `documents`, triplets are written by a frozen `kg_extractors/` tiered
  pipeline (Tier-1 metadata + Tier-2 spaCy patterns, no LLM; confidence and
  `sources[]` columns per migration 009). The `kg_extractor_agent` is a
  privileged system organ — like `docling_agent` and `reranker_agent`, and like
  the Scout before it was retired — so it bypasses selection/Gatekeeper/interview
  by construction, reached through `domain.AgentCaller` rather than the Dispatcher.

- **Multi-strategy chunking pipeline (ADR-0060).** External knowledge ingest
  is no longer a single Go function. A pluggable `domain.Chunker` port
  (`cambrian-core/domain/chunker.go`) declares
  `Name() / Supports() / Chunk()`; the `Chunk` value carries `Body` + a
  free-form `Metadata` map (the `chunk_relations` subkey is reserved
  for the IngestionManager and is set after `Chunk()` returns). Five
  implementations live in `internal/memory/chunkers/`: `OptionCChunker`
  (the 115-line paragraph-or-sentence back-compat split; the floor;
  the `ChunkDocument` shim in `internal/memory/chunker.go` delegates
  to it), `RecursiveCharacterChunker` (LangChain-style recursive
  separator with `["\n\n", "\n", " ", ""]`, default `chunk_size=200`
  per Chroma 2024), `ASTGoChunker` (pure-Go `go/ast` decl-level
  splitter for `.go` files; no cgo, no tree-sitter dependency),
  `MarkdownHeaderChunker` (heading-based split; retains
  `section_path` in metadata), and `LateChunker` (Günther et al.
  long-context encoder + per-chunk mean-pool over masked token
  embeddings, gated — see below). The `chunker_registry`
  (`internal/memory/chunker_registry.go`) routes `(sourceType, ext)`
  in strict precedence
  `match(SourceType) → match(ext) → default("option_c")`; the default
  name is a config value, not a Go constant — `Resolve` is a pure map
  lookup with no `switch sourceType` / `switch ext` (the
  **Zero-Hardcode Rule**). Each ingested document is materialised as
  one `DocTypeMnemonicEntity` row (ADR-0049 D8) with discriminator
  `kind: "source_document"`, carrying `SourceURI`, `SourceType`,
  `Title`, `Author`, `Timestamp`, and the offloaded `ContentCID`
  (the byte-oriented `domain.ContentStore.Put` offloads the full body
  once per document, not once per chunk). Source-document entities
  are **GC-exempt** (ADR-0060 D8) because they are the drill-down
  targets for chunk recall, not the chunk-level recall targets
  themselves — the `parent_entity_id` in `chunk_relations` resolves
  to them. `IngestMemory` forwards the authenticated author, session,
  tags, and importance into this path. Contract `0057` adds the
  **binary upload lane**: `content`+`filename` carry raw file bytes,
  which `app.operatorIngestDoc` turns into an `ExternalDocument` whose
  `SourceType` is derived from the filename extension (`pdf`, `docx`, …)
  and whose new `Data []byte` field carries the bytes. `persistChunks`
  base64-encodes `Data` into `StructureParseRequest.DataB64`, which is
  what opens the docling_agent's `_BINARY_TYPES` gate and reaches the
  Docling backend. Before `0057` **no production caller ever set
  `DataB64`**, so the Docling binary path was unreachable and a PDF
  could only ever be ingested as flattened text. The `context` field is
  folded into the body as a `## Context` section (not metadata) so it is
  chunked, embedded, and citable. `ProcessSync` returns only after
  the chunk bodies have been embedded as a batch, then saved as durable
  `DocTypeMnemonicFact` rows in one store batch, using deterministic
  `{document_id}-chunk-N` IDs and raw chunk text. Each chunk's
  `Metadata["chunk_relations"]` carries the
  JSON-marshaled
  `ChunkRelations { ParentEntityID, PrecedingChunkID, FollowingChunkID,
  SiblingContext { ParentTitle(80B), ParentSummary(120B),
  ParentScene(120B), PrecedingSnippet(96B), FollowingSnippet(96B) } }`
  with a strict 512B total budget (`SiblingContext.MarshalJSON`
  enforces it; over-budget fields are trimmed at the right, never
  mid-rune). The retriever follows
  `chunk_relations.parent_entity_id` → source-doc entity →
  `content_cid` → full body via `ContentStore.Get`, so drill-down
  from a recalled chunk to the source document is deterministic.
  `LateChunker` is **opt-in**, not the default: it is selected only
  when `chunker.late.enabled = true` AND
  `embedder.supports_long_context = true` (default `false` until the
  embedder-selection ADR resolves) AND the body is within
  `chunker.late.max_doc_tokens` (default `8192`, matching
  `nomic-embed-text`); over-budget docs fall back to
  `OptionCChunker` with a `late_fallback` log + run-manifest metric.
  `domain.Embedder` gains an additive
  `EmbedBatch(ctx, []string) ([][]float32, error)` so the late
  chunker drives the vectorized Ollama `/api/embed`
  `input: texts` endpoint in one request rather than N (the batch embedder
  was fixed 2026-07-06 to use `/api/embed`; the legacy single-vector
  `/api/embeddings` ignored the `input` array and silently fell back to a
  slow per-chunk loop).

- **Structure-aware ingestion pipeline (ADR-0060 deferred items, now the
  default).** This implements three items ADR-0060 v1 explicitly deferred —
  *PDF structure-aware parsing*, *hierarchical chunk relations (`section_id`)*,
  and *chunk-level graph edges in `document_edges`* — as the **default**
  chunking path (`execution.structure_graph_enabled`, default **true** in
  `internal/config/config.go` + `configs/tuning.json`; opt-out). On ingest the
  `IngestionManager` RPCs the `docling_agent` sidecar
  (`agents/system/docling_agent/`) to recover the document's real hierarchy:
  a dependency-free Markdown/text parser for born-digital text, and a guarded
  **Docling** backend (RT-DETR layout + TableFormer) for PDF bytes
  (`docling_backend.py`; OCR off by default, `DOCLING_OCR=1` forces it for
  scanned pages). The parse returns a normalized `StructuredDocument`
  (`structure.py`) of `StructNode`s, with an **OCR-junk filter**
  (`is_junk_leaf`) that drops image placeholders, config-echo, and low-alpha
  fragments at parse time. On the Go side (`internal/memory/structure_graph.go`)
  the parser's **leaves *are* the chunk set** (`ChunksFromLeaves`, with
  size-controlled merging of consecutive same-section leaves toward ~500 chars),
  so `section_path` stamps are correct by construction. Sections are persisted
  as `documents` rows with `document_type='doc_section'` (no embedding, so the
  `document_edges` FK holds); every leaf chunk is stamped with `section_path` /
  `section_ltree` (Postgres `ltree`, GiST-indexed for `<@` subtree queries) /
  `parent_section_id`; typed structural edges (`part_of`, `next`) are written
  to `document_edges` (`internal/infrastructure/postgres/structure_store.go`,
  wired through `DoclingDispatcher`). Retrieval adds a **section-scoped
  promotion** stage (`internal/memory/structure_retrieval.go`):
  `applySectionConstraint` in `searchByType` promotes chunks in the
  `section_ltree` subtree of a section the query names (`extractSectionTerms`
  prefers hierarchical numbers like `3.2` over topic words). When a document has
  no heading hierarchy (flat plain text, or a broken PDF text layer) the section
  graph is empty and the pipeline falls back cleanly to the flat chunker. Two
  adjacent levers ship built but **off by default**:
  `execution.neighbor_window_enabled` (`internal/memory/neighbor_window.go`,
  append-after context expansion over `chunk_relations` neighbors) and the
  Stage-B `reranker_enabled` (see Known gaps). Ingest perf was fixed alongside:
  per-item scene generation on the ingest path is gated by
  `execution.scene_gen_on_ingest_enabled` (default **false**) — with the batch
  embedder fix this cut a 2-PDF / 236-chunk ingest from ~37 s (RPC-deadline
  timeouts) to ~6.7 s.

---

## Security & isolation

The **Deep Kernel security model is documented but not fully implemented.**
The Wasm sandbox (ADR-0004) was cancelled 2026-05-06; agents run as standard
OS processes communicating over UDS, not as Wasm cells. The WASI-HTTP
interception layer was never built. What holds today is the **hexagonal
architecture + DAG immutability** enforced at compile time. Four invariants
are non-negotiable:

1. **Hexagonal separation.** `domain/` has zero external imports. All
   security logic (Gatekeeper, TrustScore, scope, classification) is
   unit-testable without infrastructure. Adapters import `domain/`; the
   reverse is forbidden, and the build catches it.
2. **Fail-closed reads.** `ScopedVectorStore` refuses unscoped Search. The
   pgvector adapter mirrors `Allows` as a jsonb predicate over
   `metadata.tags`. An agent only sees documents its `EffectiveScope`
   (caller_scope ∩ agent_scope) permits.
3. **DAG immutability.** Once `DAGExecutor.Execute` begins, the
   `ExecutionPlan` is frozen (value-copy semantics). A compromised agent
   cannot redirect future steps; the executed plan equals the approved plan.
4. **Kernel composition-root monopoly.** `internal/kernel/` is the only
   cross-subsystem wirer. DI changes are bounded to one package; nothing else
   constructs subsystems.

`ToolExecutor` is the single reference monitor for tool calls (grant →
resource policy → data scope → approval → budget → dispatch → audit). The
`discovery-safe` grant (ADR-0051) is the ceiling that overrides unrestricted
access for read-only discovery agents: a principal in `RestrictedTools` may use
ONLY its allowlisted tools, enforced *before* the `Unrestricted` dev bypass so it
holds even there. The **mechanism is live and generic; it currently has zero
registered principals** — its only one was the `scout_agent`, retired 2026-08-07
along with the `discovery_safe_tools` key that populated the map. An empty map
means "no restricted principals", not "nothing allowed", so the fail-closed
behaviour is unchanged and the ceiling is ready for the next read-only organ.
MCP tools
(`mcp:<server>/<tool>`) are operator-trusted but their responses are
untrusted: every response is injection-scanned, remote args are an audited
egress.

Access control is split (ADR-0085). The kernel owns the **enforcement
points** (`internal/authz/`) and always asks `domain.Authorizer` before it
acts; the **decision** — the tag algebra, principal resolution, groups,
policy objects, links, precedence — lives in the premium `authz` plugin.
There is no flag: the chokepoints run unconditionally, and only the answer
varies. `execution.scope_enforcement_enabled` is GONE; it turned the
chokepoint off rather than the policy, so an unscoped deployment had no gate
at all.

**The failure direction inverts at the seam, deliberately.** OSS fails
**open** (`AllowAllAuthorizer` — unrestricted is the correct and only
semantics for a single-tenant open-source deployment); the plugin fails
**closed** (an unresolvable principal is denied). Getting it backwards makes
OSS unusable or premium insecure.

That asymmetry has a cost worth naming, because it hid a real defect for weeks:
**a missing principal is invisible to the OSS test suite by construction.** Every
OSS test passes on an anonymous caller, so a path that never establishes identity
looks correct here and fails closed only on the deployments that enforce access
control — the ones where it matters. Any change to identity plumbing needs a test
that asserts the principal is present INSIDE a handler against a fail-closed
check, not merely that the RPC returned OK.

**The operator plane supplies the logged-in principal** (2026-08-09). The auth
interceptor (`internal/substrate/operator/authz.go` `authorize`) resolves the
bearer token and now sets `domain.WithPrincipal` alongside the operator-plane
private `principalInfo` it already set, for every operator-plane RPC, unary and
streaming. Before this it set only the private value, so the rest of the kernel —
which reads `domain.PrincipalFromContext` — saw `principal:<none>` one stack frame
after the caller authenticated, and anything that authorized, recorded ownership
or wrote provenance treated an authenticated operator as anonymous. It had been
patched at a single call site (the operator ingest handler), which is a workaround:
the next handler that wrote anything had to remember the same four lines. That
workaround is deleted; `authz_principal_test.go` pins the property. **Still
outstanding:** the core AGENT plane has the same hand-rolled patch in
`internal/substrate/network/ingest_memory.go`, and it is still load-bearing —
`internal/authz/surface.go` `seedAgentPrincipal` seeds the principal only for
PREMIUM services declared agent-facing (ADR-0118 D3), so nothing sets it for core
agent-plane RPCs.

Four resource kinds are governed by one algebra: memory (`metadata.tags`),
skills (`Skill.ScopeTags`), agents (`AgentDefinition.ClassificationTags`), and
tools (`SystemTool.ClassificationTags` + closed-set `Effects`). A tag says
what a resource is ABOUT; an effect says what the invocation DOES to it, and
both must pass (ADR-0086). Write classification stays **kernel-derived** from
operator-set write tags; a principal may only narrow, never broaden.

Two properties are load-bearing and easy to lose:

- **Skill grants are INTERSECTED, never unioned** (ADR-0085 D4). A skill is
  gated on retrieval by its tags and loading it activates tool grants, so
  without the intersection skill VISIBILITY becomes a privilege-granting
  operation. A tool policy refuses is clipped (`skill_grant_clipped`), the
  rest of the skill still loads.
- **No silent empties.** A policy-caused zero-result carries its reason, on
  both `QueryMemoryResponse.policy_note` and the agent plane's
  `MemoryResponse.policy_note`, and `ExplainAccess` answers "why can/can't
  this principal see this?" without performing the access.

The **surface clamp** (`domain.SurfaceRef`) is the Windows loopback analogue:
the entry point a request arrived through can clamp what may be done
regardless of who is asking. It is derived from the TRANSPORT by a gRPC
interceptor, never from a payload. It is only as trustworthy as the
transport — **do not treat it as a security boundary on a remotely-reachable
deployment until SEC-03 (TLS + operator authn) lands.**

---

## Data-driven development

Cambrian's product is developed through a data-driven development (DDD) loop anchored in [the benchmark harness](../cambrian-benchmarks/context.md). The harness is the black-box test suite that measures the product's behavior.

**Rule: every feature change MUST be measured.**

When you add, remove, or change a feature in this sub-repo:

1. **Identify the affected part** (memory, retrieval, tool use, planning, agent selection, LLM tracing, etc.).
2. **Map to benchmarks**: find the existing suite(s) in [cambrian-benchmarks/](../cambrian-benchmarks/) that exercise the affected part. The current suite catalog: `locomo` (memory recall/answer/full), `operator_sweep` (runtime config arms + LoCoMo), `gaia-tool-use` (single-turn tool use), `stress` (concurrency + long-plan), `hello` (protocol smoke).
3. **Baseline first**: run the relevant benchmark(s) BEFORE the change to establish a baseline.
4. **Make the change.**
5. **Measure after**: run the same benchmark(s) AFTER the change.
6. **If no existing benchmark covers the affected part**, you MUST:
   a. Run the closest existing benchmark as a proxy.
   b. **Propose a new benchmark** to add to [cambrian-benchmarks/](../cambrian-benchmarks/) that measures the affected part specifically. The proposal lives in the harness's `docs/` folder as `<feature>-benchmark.md` and includes: a one-paragraph rationale, a one-paragraph task spec, a one-paragraph scoring spec, and a draft `failure_kind` taxonomy. The new suite is implemented in `src/cambrian_bench/suites/<name>/` once approved.
   c. Document the proposed benchmark in the bench CONTEXT.md's "Suites" section.
7. **Update this CONTEXT.md**: Implementation Status table (add/remove/change the row), Module Breakdown (if a new module), Known Gaps (if a deferred item changes), Glossary (if a new term), Core Philosophy (if the principle changes).
8. **Atomic commit**: the feature change AND any benchmark change go in one commit; the Implementation Status update and the code change are inseparable.

**Why this matters:** the product's quality is its benchmark score. Code changes without measurement are not progress; they are hope. The harness is the contract between "I changed the code" and "the product got better."

---

## Terminology addendum (2026-07-31)

**`caller_scope` is populated at session start** (BRAIN-01, 2026-07-31) — the
`Session.CallerScope` / `CreateScopedSession` machinery has existed since ADR-0034 D13
and **nothing in production ever called it**: every session was opened with the unscoped
constructor, so caller_scope was always empty, the premium authorizer's per-session term
contributed nothing, and `effective = caller_scope ∩ agent_scope` reduced to the agent's
own scope. Both operator session-creation paths now resolve the AUTHENTICATED principal's
read predicate server-side and persist it (`app/caller_scope.go`).

Two properties are load-bearing. It is never read from a request field — a caller
restating its own entitlements is not a boundary (INV-5), the same reason `Surface` is
stamped by the kernel. And it never WIDENS: a multi-clause CNF predicate cannot be
represented as a single-OR-set `TagSet`, so `domain.TagSetFromPredicate` refuses rather
than flattening, and the session opens with no term (the agent's scope still governs)
rather than one that grants more than the decision point did. A nil predicate — "no read
authorized" — is likewise refused rather than inverted into "no constraint".

Verified live: a session opened by an operator holding a `sales` grant persists
`{required_tags:[sales], forbidden_tags:[confidential,internal_only,pii,secrets]}` — the
grant plus the ADR-0091 closed-tag closure.

**Note on axis:** this, not `SessionIsolation`, is the confidentiality control. Session
isolation answers *does this belong to the task I am working on* (relevance);
caller_scope ∩ agent_scope, clamped by surface, answers *may this principal see this*.

**Operator-plane transport security** (SEC-03, 2026-07-31) — the plane authenticated but
had no TRANSPORT story: `grpc.NewServer` carried no credentials and the listener bound
`":port"`, which is 0.0.0.0. So bearer tokens crossed every interface in plaintext, and
nothing in the config said so. Three changes: TLS via `server.tls_cert_file` +
`server.tls_key_file` (TLS 1.2 floor, both-or-neither — a half-configured pair is an
error, never a silent downgrade); the default bind moved to **127.0.0.1**; and a
fail-closed guard that REFUSES to boot when a non-loopback bind has no TLS, unless
`server.insecure_localhost=true` says so explicitly. An unclassifiable hostname is
treated as routable — being wrong there costs a config flag, being wrong the other way
costs an exposed plane. Verified live in all three shapes: loopback boots, routable
plaintext refuses, explicit opt-in boots with a warning. **The client-side connection
matrix (remote CLI over TLS, wrong-token deny) has NOT been run** — only the server side
is proven.

**`RememberService` REMOVED** (2026-07-31) — deleted along with the `MemoryWriter`
interface and server field. It was the raw-store-write path and nothing routed to it.
Its two unique behaviours were moved to the paths that actually run FIRST, then it was
deleted: `MemoryWrittenEvent` publication → `IngestionManager` (one event per DOCUMENT,
keyed on the source-doc entity, chunk count in the summary), and the unknown-principal
fail-closed check → the `IngestMemory` handler, which refuses before any work happens.
Both had been wired to a dead path, so **the operator memory feed had a consumer and no
reachable producer**, and the live ingest path had no "we have never heard of you" check
at all. Verified live: an ingest now emits `memory_written` on the operator feed.

**One ingest path** (2026-07-31) — every memory ingest goes through the chunker. The
agent plane (`IngestMemory`) and the operator ingestor both FAIL with
`FailedPrecondition` when no `IngestionProcessor` is wired, instead of falling back to a
raw `MemoryWriter.Remember` store write. That fallback produced a STRUCTURALLY different
row — un-chunked, single embedding, no source-document entity, no `chunk_relations`, no
structural sections, invisible to `ListDocuments`, and carrying `session_id` where the
chunked path deliberately writes an ingest-thread id. It was also unreachable
(`NewIngestionManager` never returns nil; a zero queue size defaults to 1000) while
still reading as a live second path, which is worse than no code. The same change ports
the authz error mapping onto the chunked path: a denied write or a coined tag now returns
`PermissionDenied`/`InvalidArgument` rather than collapsing into
`ingestion manager: failed to mint source-doc entity [INTERNAL]`.

**Neural-trace session stamp** (BRAIN-01, 2026-07-31) — `storeNeuralTrace` now stamps
`MetaSessionID` from the CONTEXT. Before this a trace carried `trace_id` (a plan or lease
id, session-SHAPED but not a session), so one run's reasoning was retrievable by every
other run forever and no predicate could fence it. Ingested corpus chunks are deliberately
NOT stamped — an ingestion thread is not a task session
(`internal/memory/ingestion_chunks.go`), and stamping them would make every corpus chunk
look like a run's output.

**Session isolation** (`domain.SessionIsolation`, BRAIN-01) — the predicate answering
*does this belong to the conversation I am answering*. Deliberately SEPARATE from
`TagPredicate`, which answers *may this principal see this class of thing*. Two
predicates, one SQL builder: a session id is an identity, and expressing it as a tag
would coin `session:<uuid>` per conversation, which ADR-0099 rejects. It filters on
`metadata->>'session_id'` — a field documents already carried — so it needed no column
and no migration. Unowned material (no session id) stays visible to everyone, which is
what makes it a predicate rather than a store reset. A nil predicate DENIES; an absent
SESSION is a bypass, because denying every unattended kernel read would be a far larger
blast radius than the bleed being fixed.

## Known Gaps

A non-exhaustive list of the load-bearing items that are still partial,
unwired, or guarded. Each entry cites the file or ADR where the work lives.

| Item | Detail |
|---|---|
| **JS agent runtimes are kernel-side only** (ADR-0125) | The kernel can discover, spawn, supervise and sandbox Bun/Node sidecars, but there is no TypeScript SDK yet — a JS agent must hand-implement the process contract (argv flags, UDS bind, `grpc.health.v1`, `AgentService`). Further deliberate residuals: node is probed but never downloaded by setup (bun is the batteries-included path); Linux JS agents run memory-uncapped (D8 — revisit with cgroup v2 `memory.max` in the SEC-01 Unix follow-up); no `js-agent-boot` CI smoke until a JS agent ships in-tree (needs bun in CI); kernel *tools* (`internal/tool/proc`) remain Python-only. |
| **The evidence outbox had no consumer** (ADR-0105 — CLOSED by ADR-0108's `internal/evidence.Consumer`; kept for the flag rationale) | Phase 1 lands the archive half of the substrate: with `execution.ingestion.evidence_capture_enabled` on, every ingest atomically records evidence + an outbox item, but nothing consumes the outbox until Phase 2's transformation stage exists. Deliberate — the flag is default-off precisely so an unconsumed backlog cannot accumulate anywhere real — but a deployment that turns it on early gets a monotonically growing `evidence_outbox` with `processed_at IS NULL`. Also deliberate: `namespace_id` is constant `'default'`; agent-plane ingests carry content-derived source keys, so their replay dedup is content-based, while keyed revision tracking (`same key, new revision, revises_id`) engages for sources that set `SourceURI`/`ThreadID`; and no `KnowledgeItem`/`Assessment` types exist yet, so the God Object risk remains contained-not-fixed per the memo's scorecard. |
| **Evidence Receipts are emitted but nothing records them** (ADR-0103) | The OSS seam is live: every completed retrieval is handed to registered `domain.DecisionObserver`s with chunk ids, scores, the primary-vs-injected flag and a `RetrievalFingerprint`. The consumer now exists in premium (`cambrian-premium/receipts`, `plugins/receiptsplugin`) and all of it passes under `-race`. **2026-08-06: it has now run against the real Postgres** — and the first live `VerifyChain` exposed a real defect (hashes over nanosecond timestamps that TIMESTAMPTZ cannot round-trip; every row reported hash_mismatch), fixed by hashing at microsecond precision in `Chain.Append`; rows written before the fix stay unverifiable and are reported honestly. The lane also gained the AUDIT CHAIN (ADR-0103 addendum): the ADR-0085 D13 decision journal now rides `<chain>/audit` as signed `access_decision` receipts (live: 49 chained+signed, verification fully green). **Benchmarked 2026-08-06** (three-run A/B/A, locomo recall-only ×200, arm knob `CAMBRIAN_RECEIPTS_DISABLED`): the full lane (receipts + audit chain) costs ≈ +210–250 ms mean (~3%) and ≈ +530 ms p95 (~5%) per retrieval question on an LLM-bound (~7.2 s), CPU-shared deployment; drift ruled out (control-vs-control −44 ms, CI spans zero), no tail amplification (on-arm p99 lower), recall unchanged. Kept ON; DECISIONS 2026-08-06. Three further gaps are deliberate and named in the ADR: **stage attribution is absent** (D8 — recording WHICH stage admitted an injected chunk means threading provenance through the non-displacing two-pass truncation, whose invariant exists because graph injection once nearly halved MuSiQue support-recall, so it earns its own arm); **the buffer policy is the consumer's problem** (D2 — the kernel calls observers synchronously and a wedged one slows retrieval, so the premium consumer must hand off to a bounded channel and record gap markers rather than lose events silently); and **the hot-path cost is unmeasured** (the ADR's benchmark obligation — receipts on every retrieval has not yet been run as an arm against `current-kernel`, and p95 latency must be checked before the lane is enabled anywhere real). A fourth, easy to miss: the receipt schema carries `model_id`, `prompt_hash`, `output_hash`, `policy_decisions` and `verifier`, but the retrieval seam cannot see any of them, so nothing populates those fields yet — a receipt today proves what was retrieved and how it was ranked, not what was answered or which rule allowed it. |
| **Policy cannot be authored against a human principal from the UI** (ADR-0085 / ADR-0091) | Deferred deliberately on 2026-07-27. A `principal:<operator>` link RESOLVES correctly today — `applicable()` matches a principal link by id regardless of principal kind — so per-user operator policy works on the wire. What is missing is authoring: the console's principal suggestions come from the AGENT list, so a human account cannot be selected as a link target. Consequence: per-operator permissions are API-only. Related and also open: operators are not exempt from closed tags (ADR-0091), so a newly closed tag denies operators until a grant is linked at `surface: operator` — correct as a failure direction, but nothing surfaces which closed tags are ungranted. |
| **The surface clamp engages only for registered ingresses** (ADR-0085 D7 / ADR-0090 D2/D3) | `domain.Session.Surface` documents itself as "the entry point this session was OPENED on, decided ONCE by the kernel", and `SessionManager.SessionSurface` + `authz.ResolveSurface` read it. It now HAS a writer (ADR-0090 D3): `SessionManager` stamps the surface of a **registered ingress** onto sessions that ingress opens, through the `domain.IngressResolver` port in OSS and the `authz_ingress` Postgres registry in premium (write-through + LISTEN/NOTIFY, same machinery as the policy store). **Only registered ingresses stamp**, and that is load-bearing: `ResolveSurface` prefers a session's stored surface on the assumption it is NARROWER, which is true for an outsider ingress and false for an operator-created session — so stamping every session would silently widen ordinary ones. Residual: a deployment that registers no ingress resolves every surface from the transport, so the clamp does nothing until something is registered; and whoever can write `authz_ingress` can mint a surface, hence it is policy-grade data. |
| **`cli/` is not vendored past contract 0047** (deliberate) — **fix staged in the working tree 2026-08-08, NOT yet committed** | **The committed CLI proto really is far behind, and this row was right.** `git show HEAD:proto/operator.proto` in `cli/` is a **358-line, 16-RPC** file whose header reads *"Pinned to kernel/contract version: 0047 (kernel 0.6.9-alpha)"*; the kernel serves **73** RPCs at contract **0093**. The gap is **57 RPCs**, so the widely-repeated "roughly 49 behind" figure understated it, and "the kernel serves 0076" was the one genuinely stale number. Survivable by design: the handshake exists and clients degrade on it — an RPC the CLI does not know is simply never called. `ui/proto/operator.proto` is a separate story and is **current** (its HEAD is the full file; the only diff is today's content). **Working-tree state:** both `ui/proto` and `cli/proto` now hold byte-identical copies of `api/proto/operator.proto` in LF, so committing `cli/` closes the gap — its diff is legitimately ~2,550 lines because it is catching up 57 RPCs, not line-ending noise. **Correction to a correction, worth keeping as the lesson:** on 2026-08-08 this row was rewritten to claim it had "fabricated a defect" and that "all three numbers were wrong". That rewrite was itself wrong. It measured `cli/proto/operator.proto` in the **working tree** — which an earlier session had already updated and left uncommitted — and mistook it for the committed artifact. A vendored-file claim must be checked against `git show HEAD:`, because a fresh clone gets HEAD, not somebody's uncommitted tree. Verifying against the wrong copy is the same error class as trusting the doc instead of the code, one level down. |
| **Per-generator token accounting is not tracked** (contract 0072) | `GeneratorOp.tokens_in_today` / `tokens_out_today` / `calls_today` report **-1** ("not tracked"), distinct from 0. Nothing in the kernel accumulates per-generator daily usage; the `PriceLedger` holds configured rates, not consumption. Reporting 0 would make a busy generator look idle, which is the direction that misleads. |
| **Only the blend weights hot-apply; everything else needs a restart** (ADR-0054 / ADR-0101 D3) | Durable writes now cover the whole catalogue — `SetConfig` stores any valid tunable and it survives a restart — but only the seven `blend_weight_*` params have a LIVE apply route, because `SetRuntimeConfig`'s effect handler is the only thing in the kernel that mutates a running tunable. Everything else reports `restart_required`, which is honest rather than broken: the value is stored and takes effect on the next boot. Widening it means giving each consuming component (`ToolRetriever.Floor`, the Gatekeeper's candidate cap, the executor's timeout multiplier, the query service's relevance threshold) an atomically-swappable field instead of a value captured at construction — per-field work with its own data-race surface, which is why it was not bundled into the write path. The blend weights themselves have no config-file key at all, so their `value_source` is `"runtime"` or `"default"`, never a file. |
| **Identity tags were rejected as classification coinage** (ADR-0099) — **CLOSED 2026-08-07** | A document could not be written because it carried its own name. `ExternalDocument.Tags` carries classifications AND identity; six benchmark suites and the operator upload lane pass `source_document` + a unique id deliberately, because the id-less fallback once collapsed N documents onto one chunk id and silently destroyed data. Both write chokepoints handed that raw list to `Authorizer.ClassifyWrite`, so under a configured `CAMBRIAN_CLASSIFICATION_VOCABULARY` the premium coinage check judged an identity term as a classification and DENIED the whole write. Symptom: ingest failed with *"failed to mint source-doc entity"* / `authz: write denied by policy`, and **the orchestration suite could not run at all on any deployment with a vocabulary configured** — which is every real one, so the instrument ADR-0100 needed for its gate was itself unrunnable. **Fixed:** `domain.ClassificationHint` (with `domain.SourceDocumentMarker` as the single definition of the convention, which `internal/memory`'s local const now aliases) strips the marker and the id it introduces; applied at `internal/authz/enforcing_store_writer.go` and `internal/memory/document_entity_enforcing.go`. Mutation-checked tests in `domain/classification_hint_test.go` plus a cross-seam pair in premium `authz/write_classification_identity_test.go` that asserts the PREMISE (unstripped identity IS refused) before asserting the fix, so it cannot pass vacuously. The refusal is also diagnosable now: `authz.UnknownClassificationError` names the rejected tag AND the configured vocabulary, wrapping the sentinel so `errors.Is` still holds. **Residual, and deliberate:** a genuinely classification-shaped term that is not in the vocabulary is still refused, and that is correct (ADR-0085 D11 — a typo silently narrowing a write to an unreadable document is the failure this prevents). The orchestration suite therefore stopped sending its own invented labels rather than the kernel accepting them. |
| **Per-step model allocation never reaches the lease** (ADR-0018 pending) | `Dispatcher` computes a `StepAllocation` (winner + 2 TraitModel fallbacks) and returns it on `DispatchResult` — and nothing binds it to the LLM lease, so no per-step or per-organ model choice reaches an agent. The ordering is the obstacle, not an oversight: the lease is minted at step DISPATCH (`server.go`), *before* selection picks a winner, so the allocation does not exist yet at the point the token is created and injected into `handoff.Context["_session_token_id"]`. **Half-fixed 2026-08-07:** both mint sites hardcoded `sa.Winner = "llm:ollama:qwen3:8b"` whenever an Ollama router was present, on the stated reason that `StreamChunks` needed a non-empty Winner. It does not — the gateway's configured-default fallback (`SetDefaultModelID("llm:" + cfg.LLMProvider.Default)`) has covered that case since it was added, and logs when it fires. So the literal did not fill a gap, it OVERRODE the operator's configured default with a model named in Go, which is why "which model runs my agents" was unanswerable from configuration; and because only one of the two sites is the ADR-0050 control arm, the control streamed through a different model than the arm it controls. Both literals removed; configuration now decides. Guarded by `internal/substrate/network/no_hardcoded_model_test.go`, which also asserts its own pattern so it cannot pass by not matching. **Two candidate fixes for the real gap, neither taken:** (a) mint the lease inside the Dispatcher after selection and before `CallAgent`, which moves token injection across a seam; (b) add a gateway `RebindAllocation` mirroring the existing `BindLease` (precedent for a second write to an issued lease) and call it from the Dispatcher before `CallAgent`. **Not benchmarked** — the DDD gate for a routing/model change is the `orchestration` suite, and it could not run: LLM provider quota exhausted (HTTP 429, "weekly usage limit"). Exemption recorded here rather than skipped silently, per the change-control rule. |
| **`proto-check` was red on main; bindings regenerated 2026-08-07** | `api/proto/operator.proto` gained `[deprecated = true]` + a rationale comment on `PlanStepOp.recommended_model` ("Model choice is the LLM Provider's, resolved from the declared need (ADR-0042 D1); a per-step model name on an authored plan overrode that decision. Field number retained, never reused"), and the generated bindings were never regenerated. Net effect: `make proto-check` failed on a CLEAN tree, so `make per-pr` — the minimum bar for every kernel change — was red for **everyone**, for a reason unrelated to whatever they were working on. This is the second instance of that exact shape; the first was the buf-vs-protoc header drift recorded below. **Fixed** by `make proto` with the canonical toolchain (verified `protoc 34.1 · protoc-gen-go v1.36.11 · protoc-gen-go-grpc v1.6.1` — a mismatch here would have committed toolchain drift instead of a fix). Diff is confined to `operator.pb.go`: the deprecation comment, the `Deprecated:` marker, and the descriptor bytes. `proto-breaking` passes. **Contract deliberately NOT bumped, still `0093`:** the wire is unchanged, no RPC/message/field/capability was added or removed, the field was already unread before this (ADR-0100 recorded "the contract stays 0093"), and the UI's pin is an exact-equality check — a bump would fire a skew banner over a comment and force a re-vendor plus `cargo check` to clear it. **`ui/` RE-VENDORED and verified 2026-08-07** — `ui/proto/operator.proto` is byte-identical to core again (one hunk, 4 insertions; `.gitattributes` `* text=auto eol=lf` normalises it, so no CRLF churn — git reports one modified file, not the whole tree). `PINNED_CONTRACT_VERSION` stays `0093`, matching the handshake, so no skew banner. The predicted risk was real in mechanism and benign in consequence: prost DOES emit `#[deprecated]` and the UI DOES read the field, so `cargo check` reported 2 warnings at `src/transport.rs` — but nothing in the repo denies warnings, so it was never a build break. Verified: `cargo check` rc=0 with **zero** warnings after annotation, `bun run test` **830 passed / 79 files**, `bun run build` (tsc + vite) rc=0. **The round trip is kept deliberately** (`#[allow(deprecated)]` on `authored_step_from_pb`/`_to_pb`): dropping the field in transport would silently discard a value the operator typed, which is worse than carrying one the kernel ignores. **The real defect this exposed, now CLOSED:** `ui/src/screens/plan/DagBuilder.tsx` offered `recommended_model` as an editable input under the heading "Which model it uses", with help text promising *"Overriding writes `recommended_model` on this step only. Worth doing when a step is unusually hard"* — a user-facing control for an override the kernel had stopped honouring. An operator typed a model, the UI sent it, and nothing happened. **Marked ADVISORY rather than deleted** (2026-08-07): the field stays editable so an authored plan carrying a value round-trips instead of silently losing it — which is exactly why the screen now has to say so. It carries an `Advisory` / `recorded, not applied` tag, the copy states plainly that a value is *saved with the plan but never applied* and points at the router-role generator as the thing that actually decides, and the caveat is in the accessible name too (`aria-label`), not visual-only. The inherited router-role model — the truthful answer to the heading — is unchanged, and the thinking-off caution moved onto it, where it describes the generator instead of justifying an override that does nothing. Test rewritten from *"describes the model field as an override on an inherited value"* to assert the advisory framing, including that the old `/override/i` placeholder is GONE. Verified: `bun run test` 830 passed / 79 files, `bun run build` rc=0, `cargo check` rc=0 zero warnings. |
| **A provider refusal reached nobody: silent to the log, invisible to chat** — **FIXED 2026-08-08** | A chat turn logged `Sending full prompt to LLM` and then nothing at all, for minutes, while the provider had already answered `{"type":"error","error":{"type":"GoUsageLimitError","message":"Weekly usage limit reached. Resets in 2 days."}}` — HTTP 429, in **0.56s** to curl. Three independent faults turned a fast, explicit refusal into a slow, silent one. **(1) The retry ladder could not tell a RATE limit from a QUOTA wall.** `rateLimitRetryTransport` retried the 429 five times with capped backoff; a weekly quota cannot clear inside a retry ladder, so the only effect was to convert 0.56s into a context deadline. `quotaExhausted` now sniffs the body for non-transient markers (`usage limit`, `quota`, `credit balance`, `billing`, …), **restores `resp.Body`** so the caller can still read it, logs the provider's own message, and returns immediately. Live-verified: `attempt=0`. Markers are deliberately specific — matching bare `limit` would make the kernel give up on ordinary rate limits it used to ride out, which `TestOrdinaryRateLimitStillRetries` pins by asserting the full ladder still runs. **(2) The planner swallowed it.** `Generate`'s error went out as a bare `return nil, err`, so nothing recorded that a turn died. It now logs at ERROR before returning, unchanged. **(3) The console was never told.** `SendFn` returns immediately and the operator watches the feed, so a failed turn delivered no reply, no error, and a progress line still reading "working on it" — the exact "looks like a hang" outcome ADR-0098 D3's terminal update exists to prevent, whose `Note` field is documented as "the closing line a FINAL update leaves on screen instead of clearing — used to say what went wrong". That path simply never emitted it. It now publishes a final `ProgressUpdate` carrying `chatFailureNote(err)` (`app/chat_failure_note.go`), which CLASSIFIES rather than forwarding `err.Error()`: these notes ride a channel whose `Text()` contract says the string reaches "surfaces we do not control the formatting of" — Telegram among them — so a raw provider error would leak workspace ids, billing URLs and JSON to a stranger's phone. Quota is matched before deadline on purpose: a quota refusal often ALSO surfaces as a timeout once the ladder has burned the caller's budget, and reporting that as a timeout sends the operator to look at latency instead of at their bill. **Live end-to-end:** the feed delivered `conversation_progress{conversation_id=df5b2933557bfab7, final=true, text="the language model provider refused this request because its usage limit is reached"}`. The UI half is in `ui/` — `ProgressLine` renders a final note as `role=alert` rather than a pulsing live status. |
| `DialAgent` context timeout | `grpc.WithBlock()` with no deadline in `AgentConnector`; the `bootAgent` UDS poll is safe, but a long-lived hang on a stuck agent process has no upper bound. |
| Agent liveness | `grpc.health.v1` is wired; a proactive liveness probe (reap zombie processes) is not. A crashed agent with a still-open stream will not be detected until the next request fails. |
| Consolidation atomicity race | `consolidateCluster` does non-atomic `Ingest` + `DeleteBatch`. A crash between the two leaves duplicates; a crash before leaves the old memory plus a new one. Lives in `internal/memory/`. |
| Anomaly detection | Stub in `memory_agent.go`; the hooks are present, the logic is partial. Memory pressure events are emitted but not yet acted on. |
| `RunGrantOverlay.Clear` on session-end | Unwired. The session-keyed overlay is single-use, so there is no cross-run leak, but there is no clean session-completion hook either. Per-run grants accumulate as ephemeral state until the session ends. (ADR-0046) |
| Unary `Execute` HITL | The `PauseController` is only active on the streaming path. A unary `Execute` call cannot pause for human approval. |
| Tool sandbox depth | OS process + caps only (the Wasm layer was cancelled). rlimit / cgroup / seccomp is a Unix follow-up. |
| ADR-0037 `CapabilityBelief` loop — **row corrected 2026-08-08** | Unwired by design, and now unwirable as specified. Phase-1 modules live in `internal/centralexec/` but the active-inference resource binding is not connected. This used to read "the EFE-vs-auction A/B spike has not run", which now reads as *pending* when it is *void*: ADR-0100 P3 deleted both the auction and the EFE selector, so there is no longer either arm of that comparison. Reviving it means a new spike against capability-typed dispatch — a fresh decision, not a queued one. |
| Access-policy persistence (ADR-0087) — **CLOSED; row corrected 2026-08-07** | This read as an open gap long after it was closed, and it understated the product to a reader working from this table. **Policy IS durable**: premium `authz/policy_store_pg.go` persists `authz_groups`/`authz_policies`/`authz_links` write-through (durable write first, wholesale reload, cross-replica `LISTEN/NOTIFY` on `authz_policy_changed`); the in-memory map is the read model because a decision fronts every retrieval. Agent scopes and write tags were always durable (`agent_scopes`). Residual, and the only true gap: **no authoring-history table** — the current policy is durable, the sequence of edits that produced it is not. |
| Surface clamp depends on SEC-03 (ADR-0085 D7) | The surface is derived from the gRPC service, which is sound on localhost (the transport is the process boundary) but only as trustworthy as the transport on a remote deployment. Not a security boundary off-localhost until TLS + operator authn land. |
| Tool effect migration (ADR-0086) | Shipped `tools/*tool.py` manifests live outside this repo, so effects are INFERRED for un-migrated tools and flagged `effects_inferred` on `ToolOp`. Flip `execution.tool_effects_strict` once the catalog reports none. |
| No access-policy benchmark — **row corrected 2026-08-07** | Stale in the pessimistic direction: an `access_policy` suite exists in the harness registry (`cambrian-benchmarks/src/cambrian_bench/suites/access_policy`, spec in `docs/access-policy-benchmark.md`). What remains open is narrower than "no suite": the escalation corpus and the explanation-completeness check the DDD mandate's step 6 asks for. |
| Ownership ACL removed (ADR-0085) | The hardcoded `source_agent_id == callerID` filter is deleted. A deployment that relied on per-agent ownership isolation must express it as a policy over the `provenance:source=<agent>` tag the write path already stamps. |
| `embedder` 768→1024 dim migration | Resolved for recall (`bge-large`@1024 + query prefix, LoCoMo recall@100 0.47→0.94). Residual: the ADR-0044 tool menu is not re-embedded; `tool_retrieval_floor>0` would let the tool path benefit too. Stays sub-1B so it co-resides with `qwen3:8b` on 12 GB. |
| Stage-A blend weights (ADR-0054) | Tuned and adopted in `config.json` (cosine 0.40, lexical 0.20, coherence 0.05, confidence 0.10, pagerank/recency/activation 0.0). Coordinate-descent sweep over the operator plane (`SetRuntimeConfig` hot-swap), conv-split train/test: baseline @10 0.673/0.662 → 0.763/0.766. Takes effect on restart. |
| Stage-B reranker (ADR-0054) | Built and DEFERRED (`reranker_enabled=false`). bge cross-encoder as a warm system organ (`reranker_agent`, mirrors `kg_extractor`) regressed recall on LoCoMo (recall@1 0.168→0.089): the Stage-A blend already orders the top-50 well. Ollama can't serve cross-encoders; GPU revisit = CUDA torch + `bge-reranker-v2-m3`. Re-tested 2026-07-06 on a 2-PDF corpus: still net-negative (specific-fact top-5 0.50→0.30, the cross-encoder disagreeing with the correct chunk) and ~8 s/query on CPU. Two latent bugs were fixed while there and kept: `sentence-transformers` was uninstalled (agent crashed → silent fail-soft, never ran), and the model loaded in `__init__` blew the 10 s agent-boot socket timeout (`instance_manager.go`) → re-spawn per query; now **lazy-loaded** (`_ensure_model`), so it boots instantly and stays warm. Enable only with a GPU. |
| pgvector dim-migration is destructive | An embedding-dimension change recreates every EMBEDDING table — `chunks`, `tools`, `skills`, `agent_profiles` (drop+recreate; pgvector can't `ALTER` a VECTOR dim). ADR-0093: `documents` and `document_sections` are untouched, having no vector. The boot path now refuses to start if memory docs are present unless `ALLOW_DESTRUCTIVE_DIM_MIGRATION=1` is set. The guard only protects inside the running binary; rebuild before launch. |
| Model-failover requires ≥2 generators | Automatic model failover via `selectModelCandidates` only fills `Fallbacks` when `FindModelCandidates` returns ≥2. A single-generator config has one candidate and no rung to fall back to. |
| MCP tool OAuth / per-tenant budget | `mcp` tool price in the menu and per-tenant OAuth/budget are deferred. The `mcp:<server>/<tool>` shape is wired and injection-scanned. |
| Re-chunking backfill (ADR-0060) | When the operator changes the `chunker.*` config, existing documents are not re-chunked in v1. A backfill path is future work; operators re-ingest the affected sources to apply a new strategy. |
| Source-document entity size (ADR-0060) | Source-doc entities are GC-exempt (ADR-0060 D8) and accumulate; operators must size the `ContentStore` (CAS) and the entity table accordingly. A dedicated size-cap / tiered-storage ADR is a follow-up. |
| Late-chunker gate is implicit (ADR-0060) | Setting only one of `chunker.late.enabled` / `embedder.supports_long_context` silently routes to `OptionCChunker` (logged warn at resolve time, not a hard error). Intent is "fail to the known-good default," but it means the gate is *implicit* for any operator who only sets one of the two flags. |
| Future chunkers (ADR-0060) | **Implemented 2026-07-06 (default on) — see *Structure-aware ingestion pipeline* above:** PDF structure-aware parsing (via **Docling**, not Marker/LayoutLMv3), hierarchical chunk relations (`section_path` / `section_ltree` / `parent_section_id`), and chunk-level graph edges in `document_edges` (`part_of` / `next`). Still deferred: tree-sitter multi-language AST chunker (cgo-blocked; the v1 AST path is `go/ast`-only), `sibling_index` chunk relations, LLM-driven chunkers (Contextual Retrieval / Propositions / Small-to-big — explicitly rejected: no per-chunk LLM call), auto-router (LLM picks chunker per document), and re-chunking backfill when `chunker.*` config changes. |
| Reactive journal GC unscheduled (ADR-0061) | **CLOSED 2026-08-01 by GOV-02.** `Prune` was implemented, unit-tested, and called by NOTHING — the fifth instance in this codebase of a subsystem that is correct, covered by its own tests, and wired to no caller (after `SetCallerScope`, `MemoryWrittenEvent`, the unknown-principal check and the `RememberService` route). Its tests passed the whole time, because a test supplies the caller the product does not. Now: `reactive.JournalGC` runs bounded passes on a timer from the reactive plugin's `Start`, floored at the **lowest cursor across every registered watch** — a watch registered but never advanced sits at 0 and correctly holds the floor there, where a minimum over only the watches that HAVE acked would drop signals it never saw. `Prune` gained a `limit` (an unbounded first pass over a months-old journal holds bbolt's write lock for the whole scan and is itself the outage) and reports whether more remained. Config `GCInterval`/`GCBatch` (1h / 5000), 0 disables. Passes publish REC-03's source-agnostic `RetentionRunEvent` with `source="reactive_journal"` — no second contract bump, one answer shape for "what did this system delete". 13 tests. |
| Backtest could silently answer over a shortened history (GOV-02) | `Backtest` replays journalled history to answer "would this watch have fired?", and journal GC shortens what it can reach — so "fired twice in six months" and "fired twice in the four days still retained" were the same reply. `WatchBacktester.Backtest` now returns `domain.WatchBacktestResult` carrying the retained window (oldest/newest seq + count) with the verdicts, surfaced on `BacktestWatchOpResponse` (contract `0084`→`0085`). The window is part of the RESULT rather than a separate query on purpose: one a caller must remember to ask for is one most callers will not ask for. |
| Reactive replay drop-on-overload (ADR-0061) | Startup replay re-enqueues onto the same bounded fast/slow channels; a replay backlog larger than the queue dead-letters the overflow (`queue_full`) rather than blocking. Fine for the expected small since-cursor backlog; a blocking/paced replay feed is a follow-up. |
| Operator contract skew — **RESOLVED 2026-08-01** (batch re-vendor) | All consumers now vendor kernel contract `0085`. `ui/proto/operator.proto` + `PINNED_CONTRACT_VERSION` `0079`→`0085`, verified by a clean `cargo check`; `cli/proto/{operator,cambrian}.proto` re-vendored (it had **16 of 65 RPCs** — genuinely 49 behind) and `src/proto-embed.ts` regenerated via `bun run scripts/embed-proto.ts` (that file is gitignored and build-generated); the benchmark harness stubs regenerated to `0085`. **Two findings.** (1) The UI's *pin* was far staler than its *proto* — the vendored schema already had all 65 RPCs and 172/174 messages, so the skew banner had been firing on a stale number, not a stale contract. (2) A naive `grpc_tools.protoc` run emits `import operator_pb2` (absolute), which is unimportable inside a vendored package; every existing harness stub had that hand-edited to `from . import`. That undocumented manual step is a large part of why re-vendoring kept being deferred, so it is now scripted: `cambrian-benchmarks/scripts/revendor_stubs.py` regenerates and applies the fix, with `--check` as a staleness gate. The two pre-existing `cli` test failures (`winKeyPath` keychain-path encoding, file untouched and 4 weeks old) are unrelated and still failing. |
| Operator contract skew (ADR-0061 → 0079) | Kernel now serves contract `0059` (ADR-0079 adds `AgentLLMExchangeOp` + conditional cap `llm-exchange` — the LLM-exchange provider tap for benchmark loop review, gated `execution.capture_llm_exchanges`; the benchmark harness stub is re-vendored to it). Prior baseline `0058` added `AgentStepOp` + cap `agent-steps` — per-memory_query agent-loop observability. Prior baseline was `0057` (accreted caps `watch-deadletter`/`reactive-backpressure`/`watch-condition-guard`/`watch-observability`/`watch-schedule` over REACT-01→06, + `route-preview` for the ROUTE-07 gatekeeper `PreviewRoute` RPC, ADR-0077, + `memory-ingest-binary` for the operator document-upload lane). **`ui/` re-vendored to `0057`** (2026-07-17). `cli/` still trails at `0047`, and the benchmark harness stub is at `0055` (needs re-vendor to call PreviewRoute). Recorded skew — the handshake degrades clients gracefully; the remaining batch re-vendor is a follow-up. |
| DB migration runner residuals (ADR-0064) | The runner is forward-only (no down-migrations, no checksums). `storage.auto_migrate=false` means boot runs only the dimension guard and creates no schema — the operator must `migrate up` first. `migrate` is not yet wired into CI (no committed CI exists). |
| Reactive injection-guard residuals (ADR-0063) | The registration risk gate is a coarse boolean (`Approved`), not a per-*fire* HITL nor a content-risk score. Per-fire approval via the `ApprovalController` and the "deterministic co-condition" alternative are follow-ups. Prompt-injection defense is structural (payload confined to a JSON-encoded nonce fence, fail-closed true/false) — strong against delimiter/tag forgery, but not a total guarantee against an adversarially-reasoning model. Contract `0053` not re-vendored to ui/cli. |
| Reactive backpressure residuals (ADR-0062) | Debounce coalescing buffers are ephemeral (a crash mid-window loses the buffered burst — only the coalesced fire is journaled). The global budget is a single plane-wide bucket, not per-owner/tenant — a noisy watch can spend the plane's whole allowance (per-owner quotas = REACT-07). Circadian/energy is the "natural home" for the budget but that machinery doesn't exist yet, so it is a premium-side token bucket for v1. Contract `0052` (+ cap `reactive-backpressure`, `debounce_seconds` field, `ReactiveBudgetOp` feed op) not re-vendored to ui/cli. |
| Experiential memory removed (2026-07-18) — **SUPERSEDED by ADR-0094/0095; row corrected 2026-07-31** | This row read as an open gap long after the redesign it was waiting for had landed, and was twice mistaken for "experiential memory is unwired" by readers working from this table rather than the ADR status lines. **Current state: the replacement tier is built.** ADR-0094's procedural tier is *"implemented behind a default-off arm; first exercised on real data 2026-07-28"* — D1 record shape, D2 capability-tagging, D3 deterministic clustering, D4 lifecycle, D5 the fourth recall lane, D7 population-wide attribution, D8's erosion guard (whose outcome loop amendment **A2** closed — until then no routine's confidence ever moved), plus the offline scheduler ADR-0049 §A2.5 permits. 8 clusters and 12–15 routines have been induced. Code: `internal/memory/procedure_inducer.go`, `procedure_induction.go`, `hippocampus.go`, exercised by `procedure_realdata_test.go`. **Still outstanding, and this is the narrow true gap:** ADR-0095's status is *"schema implemented (migration `0010_experiences`), writers not built"* — the experience write path is unwired and the table lands empty **deliberately**, because the record shape is still moving and inventing parents now would put soon-to-be-wrong rows in the store the benchmarks measure against. Also unbuilt: ADR-0095 D3's single-writer retag TRANSACTION, D6's retention beyond the cascade, and ADR-0094 D6's per-fire authority clipping. So the tier CAN learn but is not yet being fed — see the "Experiential lane cannot yet be MEASURED" row, which is the honest reading of what that means. **History:** the 2026-07-18 removal unwired all agent-EXECUTION write-back — step results (`RecordExecution`), plan scenes (`WritePlanScene`), procedural (`Hippocampus.Store`), negative edges (`IngestNegativeEdge`), episodic (`EpisodicExtractor` + `MemoryLifecycleManager`, both removed), and ADR-0034 scope promotion (`Promoter`); tool-output recording was already inert (no caller) — explicitly pending the redesign that ADR-0094/0095 then delivered. Document INGESTION (the corpus) was unchanged throughout, and agents always kept READ access. |
| By-ID reads bypass read enforcement — **CLOSED 2026-07-27** | **Fixed:** `EnforcingVectorStore` no longer embeds `domain.VectorStore`; all 11 methods are explicit and labelled ENFORCED or PASS-THROUGH, so adding a method to the interface now breaks the file until someone decides which it is. `GetByID`/`GetBatch`/`QueryByMetadata` are enforced (filter against the ctx predicate; no predicate ⇒ `ErrScopeMissing`); `GetStaleMemories` is a pinned pass-through (kernel maintenance); writes pass through to `EnforcingStoreWriter` (ADR-0035). Required wiring: `searchByType` seeds `ctx = domain.WithScope(ctx, eff)` so by-id enrichment stages inherit the predicate, and kernel-internal readers (`MemoryManager.GetByID`/`GetBatch`, `upsertEntity`) seed the explicit `domain.ScopeSystem` bypass. Residual: enforcement filters in Go post-fetch rather than pushing into SQL; components holding the raw adapter on purpose (GraphStore, spreading engine, profile store) are out of scope by design. **History:** it previously embedded and overrode `Search` only — its type comment states *"Non-Search methods pass through unchanged (Search is the single SQL-building chokepoint)."* So **`GetByID`/`GetBatch` are unguarded reads of the raw adapter**, and "Search is the chokepoint" is an assumption that holds only while every read goes through Search. **kgExpand was the live instance and is FIXED** (2026-07-27): it reached chunks by ID via an unscoped `ChunksMentioningEntity` and returned restricted chunk **content** (for ADR-0049 entity descriptors: endpoints, auth, schema), plus a `Document{ID: id}` stub on failure whose id encodes the source document. `authorizedDoc` now applies the caller's `TagPredicate` to the materialised doc and drops on missing/unreadable/forbidden; `kgExpand` takes `scope` as a required positional param (nil ⇒ fail-closed); mutation-checked regression tests `TestKGExpand_DropsForbiddenChunk` / `TestKGExpand_NilPredicateFailsClosed`. `ChunksMentioningEntity` is now **scoped too** — it takes the predicate and applies it by joining `chunks` (which carries the ADR-0093 D4 tag copy), reusing `scopeExpressionsOn(eff, alias)`; nil fails closed, deliberately departing from `scopeExpressions`' nil convention because nothing sits in front of this lookup. Making `scope` a **required parameter** surfaced two further unscoped call sites, both production defaults: `applyAnchorConstraint` (`anchor_constraint_enabled`, default true) and `injectQueryEntitySeeds` (`query_entity_seeding_enabled`, default false) — all three fixed, with `TestKGExpand_ThreadsScopeToTripletStore` asserting the wiring. **Residual:** every OTHER by-ID read path is still unenforced — enforcing `GetByID`/`GetBatch` on the decorator would close the class but changes read semantics kernel-wide (system components hold the raw adapter on purpose) and needs its own decision. See ADR-0095 D9. |
| Derived artifacts carry no classification — **exploitable read paths CLOSED 2026-07-27; row corrected 2026-07-31** | As written, this row read as a live leak channel and was mistaken for one by readers working from this table rather than from ADR-0095 D9's audit section, which records the fix. **What is actually true.** The structural fact stands: `chunk_triplets` (`sources TEXT[]`), `CosineThemeClusterer.Cluster(docs)` and scene generation distil across multiple sources into artifacts with **no classification column**, so restrictions do not survive derivation *in the schema*. **What is no longer true is that this is reachable.** The laundering path — `kgExpand` materialising restricted chunk **content** through unscoped by-ID reads — was found and closed on 2026-07-27 (`internal/memory/kg_expand.go`): `mustGetDoc` became `authorizedDoc`, which applies the caller's `TagPredicate` to the materialised document and DROPS (never returns an ID-bearing stub) for missing, unreadable or forbidden chunks; `ChunksMentioningEntity` now takes the predicate and applies it by JOINing `chunks` (which carries the ADR-0093 D4 tag copy), so restricted IDs are never returned rather than returned-then-dropped; and `scope` became a **required positional parameter**, which surfaced two further unscoped call sites — `applyAnchorConstraint` (`anchor_constraint_enabled`, default **true**) and `injectQueryEntitySeeds` (`query_entity_seeding_enabled`, default false) — both fixed. The class was then closed at the decorator: `EnforcingVectorStore` no longer embeds `domain.VectorStore`, so all eleven methods are explicit and labelled, and adding a method breaks the file until someone decides which it is. Regression tests `TestKGExpand_DropsForbiddenChunk` and `TestKGExpand_NilPredicateFailsClosed` are mutation-checked. **Residual, and why it is a watch item rather than a defect:** triplet *text* is never surfaced (`kgExpand` consumes only `t.H`/`t.T` as counting keys; relation labels are read nowhere at query time) and the ID lookups are now scoped — so the missing column is contained by the read paths, not by the schema. The exposure returns the moment any derivation starts surfacing its own text or a new by-ID reader appears, which is exactly why **ADR-0095 D9 must be applied to every new distillation**: inherit the sources' restrictions; refuse across a closed-tag boundary; union only for open tags; decide compatibility on the sources' `classification_tags`, never on the artifact's content. See also the "By-ID reads bypass read enforcement — CLOSED 2026-07-27" row, whose residual (every OTHER by-ID read path) is the live half of this concern. |
| Planner can invent capability tags — GUARDED 2026-07-28 | The planner emitted `required_capabilities: ["file_write"]` when nothing declared it and the tag was absent from the vocabulary it was shown, despite the explicit "NEVER invent a capability tag" instruction; with L1 enforcing, that filtered every candidate (`no candidates found`) and killed the step. **Both halves fixed:** (a) `terminal_agent` and `code_generator_agent` now declare `file_write` — the tag was legitimate, just undeclared; (b) a vocabulary guard in `Planner.GetExecutionPlan` drops emitted tags that were not in the rendered CAPABILITY CLUSTERS set, so a hallucination degrades the step to unconstrained routing (the pre-ROUTE-03 path) instead of killing it. The guard is deliberately fail-OPEN and only engages on a NON-EMPTY vocabulary — an empty one means no manifest declared anything, which is absence of knowledge, not evidence of invention. This is a routing refinement, not an authorization boundary: scope (0034/0035), the policy PDP (0085/0087) and approval gates do not consult this field. Live: `no_candidates` 1 → 0, suite 4/6 → 5/6, and the guard did not need to fire because (a) removed the cause. |
| Non-action model output — GUARDED 2026-07-28, root cause remains | `parse_action` still coerces output without a parseable `{"action": ...}` into a `final_answer`, but it now marks it `_inferred` and the loop re-prompts once ("emit the next action, or declare the answer") before accepting — LangChain-shaped bounded retry, and Anthropic's rule that only an explicit finish means finished. A declared final_answer is untouched. Accepted inferred answers log at WARN (`react_inferred_answer_accepted`) instead of passing as silent success. Live: fired once in `runs/rt_vocab1`, converting a hollow success into a visible failure. **This is compensation, not a fix** — the loop still infers intent from parseability because the agent-plane proto carries text only. ADR-0097 Phase B is the actual fix. |
| Agents pick an EDIT tool to create a file that does not exist | Observed 2026-07-28 (`runs/rt_budget1`): an agent repeatedly called `mcp:fast-filesystem/fast_edit_block` to produce a new file, failed every time, and was stopped by the recurrence veto. Tool-selection, not a loop bug — the veto worked. |
| CLI vendored `cambrian.proto` is stale (skew recorded, not fixed) | `cli/proto/cambrian.proto` predates the module rename (`option go_package = "cambrian-runtime/api/proto"`, untranslated comments) and is ~70 lines behind core — it did NOT gain ADR-0097's `GenerateWithTools`. Deliberate: the CLI is an admin companion that never calls the agent-plane generate path, and a wholesale copy would change its `go_package` with build risk in a repo outside this change. The UI is unaffected (it vendors only `operator.proto`, which is byte-identical at 0068) but its `PINNED_CONTRACT_VERSION` was bumped 0067→0068 because the skew check is exact equality and would otherwise raise a permanent false banner. |
| Local `resource_selector: "efe"` silently voids routing measurements — **CLOSED 2026-08-07; row kept as the lesson** | **The trap can no longer be set.** ADR-0100 P3 deleted the EFE selector, `AssignVariant` and the `ResourceSelector` port; `resource_selector` and `efe_*` are retired keys, `configs/tuning.local.json` no longer sets them, and dispatch is unconditional — there is no setting that can silently take over selection. **What happened, because it is the most expensive class of bug this project has hit:** the gitignored local tuning file set `resource_selector: "efe"`, `AssignVariant` treated that as absolute, so **`efe_traffic_percent: 0` did not mean 0% EFE** — every session took the EFE path and never reached the auction. ROUTE-03 was consequently measured as inert for weeks (ADR-0096 Context). The config key was validated, accepted, and did something nobody intended, which is exactly why retired keys are now REMOVED with a recorded reason rather than left declared-but-unread (`retiredExecutionFields`). Two residual cautions: an ARCHIVED routing run is only interpretable against the config it captured, so check for `resource_selector` there before trusting the number; and a gitignored local override is invisible to everyone but its author. |
| A hard agent pin has no runner-ups | `Gatekeeper.FindCandidates` returns a single candidate for `PinHard`, so the inter-step fallback loop (`runnerUps`) has nothing to fall back to and a failed pinned step goes straight to replan. Observed live: hard-pinned `terminal_agent` failed a file write on one run, succeeded on the next, where unpinned selection chose `code_generator_agent` and succeeded. Accepted as the semantics of an explicit directive (ADR-0096 Consequences), not yet measured at scale. |
| Experiential lane cannot yet be MEASURED | Everything in ADR-0049 §A2 / ADR-0094 is implemented and default-off, and none of it can be validated on data yet. The retrievable store is ~4435 corpus chunks against 2 experiential rows, so `memory-guard`'s `mean_foreign_share` — the defect-4 displacement metric — is structurally unable to move: a knowledge query's competition is corpus-vs-corpus, which is same-lane. The lane guard was measured as COSTING nothing (foreign_share identical to 4dp pre/post) but its BENEFIT is unmeasurable until experience is a meaningful share of the retrievable set. Likewise no procedure has ever been induced: that needs plans running under the `capability_contract` arm. The instrument, the guard and the writers are in place waiting for volume — do not read "default-off" as "unfinished", nor "tests pass" as "validated". Updated 2026-07-28: routines HAVE now been induced on real data (8 clusters, 12–15 routines) and the D8 outcome loop is closed, so the tier can finally learn; what remains unmeasured is whether it helps. A LoCoMo recall smoke found ZERO experiential chunks among 160 retrieved, so displacement shows no sign at current volume. |
| ADR-0037 belief loop still gated (ADR-0049 §A2.7 Phase 4 half-open) | Phase 4's prediction-error write gate and the PROCEDURE co-evolution half are done; "precedent outcomes feed `belief.Store`" is NOT, deliberately. `internal/centralexec/belief` is wired nowhere and is unwired by design pending ADR-0037's EFE-vs-auction spike. Wiring it as a side effect of a memory phase would smuggle a gated routing decision through an unrelated ADR — it belongs to ADR-0037's gate. |
| Planner lost its absolute-path rule when the Scout was deleted — **CLOSED 2026-08-08, split across the two components** | The Scout was the sole producer of `<DiscoveryLTM>` and the `<environment>` block inside it, so after its retirement the planner was instructed about sections that could never appear — and the prompt's ONLY host-path guidance (*"on os=windows use backslash paths … NEVER `~`, `~/Desktop`"*) lived in that dead branch, leaving the `~/Desktop`-on-Windows failure unguarded. **Fixed by putting each half where it belongs, rather than restoring the old shape:** the planner cannot see the host it plans for, but the agent executing the step runs *on* that host. *Planner half* (`buildHostBlock`, a **dynamic** constraint): the OS and nothing else, plus a static rule in `plannerDecisionRules` that paths are the executor's job — describe the target, never construct an absolute path, never write `~`. *Agent half* (`sdk/cambrian_agent_sdk/host.py`, `host_facts`/`host_block`, injected once into the ReAct prompt's cacheable prefix): the absolute `os`/`home`/`desktop`/`cwd`/`path_separator` **discovered on the machine**, with the path rules that make them usable. Three things worth knowing. (1) The host block must stay OUT of `plannerStaticText`: the hash gates plan-cache reuse via `PlannerPromptVersion`, so a per-host value in it would give every host a different hash and silently invalidate cached plans across a fleet — `TestPlannerPromptHash_ExcludesTheHost` is the mutation-checked guard. (2) The planner is deliberately NOT told `home`/`desktop`, because plans are replayed as `<PlanLTM>` structural references — an absolute `C:\Users\<someone>\Desktop\…` baked into a step is wrong under another account and writes a username into memory. (3) Facts are discovered, never assumed: a value that cannot be determined is omitted, because the model treats what it is given as authoritative. That last rule earned itself immediately — on the dev machine the real desktop is `C:\Users\<user>\OneDrive\Desktop` (OneDrive Known-Folder redirection, read from the Explorer Shell Folders registry value) while `home/Desktop` also exists as a shadow folder, so the naive implementation would have written files somewhere the user never sees. Linux gets the same treatment via `XDG_DESKTOP_DIR` (a localised desktop is `~/Escritorio`, not `~/Desktop`). |
| Test gate was missing `-race` and both proto gates (race gate now LIVE-validated) | **ADDRESSED 2026-07-31; row corrected.** `per-pr` is now `proto-check proto-breaking test-race separability integration chaos bench-micro`. Two separate holes, same shape. (1) QA-01: `-race` was not in any gate; the races it then reported were all in TEST FAKES (`captureAllStore`, `captureSignalReceiver`, an unguarded slice), never in `IngestionManager` — adding locks to production code would have been the wrong repair to a real signal. (2) `proto-check` and `proto-breaking` existed as Makefile targets wired into **no pipeline at all** (the only workflow is `publish-image.yml`, which references neither). This is the actual explanation for the incident the change-control rules cite: `SetRuntimeConfig` reached main without a contract bump because nothing ran the check, not because someone bypassed it.  **Live-validated 2026-08-01 (QA-01 closed):** a `go build -race` kernel under two CONCURRENT DEMO-01 sustained runs — 120 messages, 26 record versions, 124 signals, 120 memory ingests, four active watches, `GORACE=halt_on_error=0` so every race would be reported rather than just the first — gave **0 data races and 0 errors**. The zero was falsified BEFORE being believed: a deliberate 4-goroutine unsynchronised counter compiled with the same toolchain reported `WARNING: DATA RACE` immediately, so the detector is armed and the kernel result is a measurement rather than absent instrumentation — the empty-denominator trap that cost three retracted numbers earlier in this campaign. It also re-tests QA-01's diagnosis from the other side: those races were all in TEST FAKES, and real concurrent ingest would have surfaced a production race here if the fakes had merely been the only thing under concurrent access. |
| `make proto` was non-deterministic across machines | **FIXED 2026-07-31.** The target preferred `buf generate` when buf was on PATH and fell back to protoc otherwise, on the *stated* assumption that both reproduce the committed bindings. They do not: `buf.gen.yaml`'s remote plugins were unpinned, so buf emitted `protoc-gen-go-grpc v1.6.2` / `protoc (unknown)` against the committed `v1.6.1` / `protoc v7.34.1`. The embedded descriptor is toolchain-independent; the generated file *header* is not. Net effect: `make proto-check` failed on a CLEAN tree the moment a developer installed buf, for reasons unrelated to their change. protoc is now the named canonical generator (it produced the committed bindings and reproduces them byte-for-byte); buf runs the schema gates only, and its plugins are pinned. Canonical toolchain: `protoc v7.34.1 · protoc-gen-go v1.36.11 · protoc-gen-go-grpc v1.6.1` — a mismatch is what `proto-check` reports. |
| `CAMBRIAN_CONFIG_STORE=off` panicked — the escape hatch the kernel's own error recommends (FIXED 2026-08-01) | `OpenConfigStore` returns `(*storage.BoltConfigStore, error)` and yields a **typed nil** for `off`. Passed into `config.LoadConfigWithStore(path, store Store)` it boxed into an interface that is **not** nil — non-nil type descriptor, nil value — so the loader's `if store != nil` guard passed and `Overrides()` dereferenced `s.db` on a nil receiver. The guard was never wrong; the value lied to it. The sharp part is WHICH path this was: the boot error on a corrupt store says *"set CAMBRIAN_CONFIG_STORE=off to boot from the config files alone"* — so the documented recovery from a broken config store was itself a crash. Fixed at the boundary with `configStoreOrNil` (app/config_store.go) rather than by adding nil-receiver guards to each store method: guards would make the mistake SURVIVABLE at every new call site instead of IMPOSSIBLE at the one place the conversion happens. Three tests, one of which asserts the premise (that a typed nil really does box non-nil) so it cannot pass vacuously; live-verified — the kernel now reaches "Cambrian Substrate Active" with the store disabled. Found by the DRIFT-03 deceptive-corpus run, which needed the hatch. |
| `proto-check` conflated two different failures (FIXED 2026-08-01) | The gate ran `make proto` then `git diff --exit-code -- api/proto`. Two faults, both invisible until it was wired into `per-pr`. (1) `api/proto` holds the hand-edited `.proto` SOURCES as well as the generated `.pb.go`, so any uncommitted proto edit failed the gate reporting *"generated bindings are stale"* when the bindings were perfectly in sync — meaning `per-pr` could never pass while you were editing a contract, which is exactly when you would run it. It passed in CI only because the edit is committed by then. (2) Regenerating in place rewrote four files every run, leaving the tree stat-dirty on Windows (`core.autocrlf=true`, no `.gitattributes`) even when byte-identical — the same root cause as the CRLF incident that converted 120 files during a stash round-trip. Now generates to a temp dir and compares generated-vs-fresh, which is the property actually wanted, and leaves the working tree untouched. Verified both ways: passes with an uncommitted proto edit, still fails on a hand-corrupted binding. |
| Retrieval context poisoning | **ADDRESSED 2026-07-31 by BRAIN-01; row corrected.** A second predicate, `domain.SessionIsolation`, now rides beside the tag predicate — pushed into SQL over `metadata->>'session_id'` (no column, no migration; documents already carried it) and re-asserted in-memory where every candidate converges. Unowned corpus material stays visible, so it is a predicate and not a store reset. **The root cause was not a missing feature.** `Session.CallerScope`, `SessionManager.CreateScopedSession`/`SetCallerScope`, `app.Options.SessionScopes` and the premium authorizer's per-session term were all built — and **neither writer had a single production caller**, so every session's caller scope was empty and the term contributed nothing. The only session-aware read filter did the OPPOSITE of isolation: it dropped the current session's own step records and kept every other session's. That machinery is deliberately left in place (it answers a classification question); isolation is separate because a session id is an identity, not a classification (ADR-0099). **History:** measured `cross_session_retrieval_rate` ~1.0 in the orchestration suite; fix named as scope-isolated retrieval (ADR-0034), NOT a store reset (rule: noise is intentional). |
| `go.mod` module rename | `cambrian-core/go.mod` still declares `module github.com/cambrian-sh/cambrian-runtime` even though the directory is `cambrian-core/`. The rename cascades through every internal Go import, the premium `go.mod` `require` block, and `go.work`. Follow-up ticket, not part of this docs plan. |

---

## Terminology Glossary

The kernel's domain language. Bold terms are the canonical names used in
`domain/` and the ADRs. Each entry is one or two sentences.

**Decision provenance (ADR-0103)**

**DecisionObserver** — the OSS seam that receives a value copy of every completed
retrieval. Observes after assembly; cannot influence, delay or fail a query.

**RetrievalDecision** — that value copy: query id, principal, a HASH of the query text
(never the text), the returned chunks with scores and their primary-vs-injected flag,
and the `ConfigFingerprint`. Chunks are referenced by id, never copied, so deleting a
chunk later never forces a choice between honouring the deletion and keeping a
verifiable history.

**RetrievalFingerprint** — a hash over exactly the configuration that can change a
ranking (window, floor, stage flags, blend weights, reranker settings, embedder). Two
runs sharing a fingerprint over the same corpus must rank identically. The field list is
explicit, not reflected, so an unrelated config change does not invalidate every
historical fingerprint.

**Evidence Receipt** — the premium artifact built FROM a `RetrievalDecision`: signed,
hash-chained, and joined to the policy verdict and verifier outcome. A *decision* record
("what did it know, ranked how, under which config, permitted by which rule") as opposed
to an *action* record ("this tool was called and allowed"). Not built yet.

**Core orchestration**

- **Substrate**, the Go kernel/runtime; the OSS composition root that owns agent lifecycle, dispatch, and the gRPC surface.
- **Handoff**, an inter-agent message envelope (`domain.Handoff{ID, FromAgent, ToAgent, Payload, Confidence, Uncertainties, Context}`); the proto `pb.Handoff` only exists at the gRPC boundary.
- **Payload**, a Handoff's data body (`domain.Payload{ID, Type, Data, Metadata}`).
- **Dispatch / Dispatcher / StepDispatcher**, the act of binding a step to an agent, the unit that does it (`internal/metabolism/dispatch`), and the port it satisfies. Selection is eligibility (L1 capability contract) → ranking (`ScoreMerit`) → argmax → call, with only the winner booted. A losing candidate is a `CandidateEntry` — a scored candidate, not a bidder.
- **~~Auction / Proposal / Auctioneer~~**, RETIRED 2026-08-07 (ADR-0100 P3) — listed here only so an older document, comment or run archive can be read. It was the bidding round, an agent's bid (Confidence + Rationale + Requirements + Latency), and the unit that collected bids and picked a winner. `domain.Auctioneer` → `StepDispatcher`, `AuctionTask/Result` → `DispatchTask/Result`, `AuctionEventPayload` → `SelectionEventPayload`, `BidEntry` → `CandidateEntry`; `domain.AgentProposal` is deleted. **The one place the old name is still correct is the wire**: `AuctionEventOp` and the `auction` feed class kept their names deliberately, so the operator contract holds at `0093`.
- **Planner**, the LLM component in `internal/awareness/` that decomposes a request into an `ExecutionPlan`; the Zero-Hardcode rule lives here.
- **DAGExecutor**, traverses the plan as a DAG with concurrent independent steps; tiered recovery (SelfHealer → fallback → replan → `PartialPlanError`); value-copy plan freeze.
- **Zero-Hardcode Rule**, agent-to-task routing lives in the Awareness (LLM) layer, never as Go `if/else`/`switch`. Three exceptions: system-shell, reflexive path (latency), and security gates (scope/approval/budget).
- **Agent pin** (`Step.PreferredAgent` + `Step.AgentPin`, ADR-0096), a named executor carried as DATA on a plan step, emitted only when the USER named an agent. **Soft** exempts it from the L2 semantic gate and adds `SoftPinBoost` (0.25) to its merit, but never carries it past the L1 capability contract and never strands a step; **hard** binds it directly and errors if it is unregistered or undispatchable. Anything not recognisably `"hard"` degrades to soft. Not a Zero-Hardcode exception: the rule forbids *authored* routing tables in Go, not honouring a runtime user directive.

**Gatekeeper, merit, and trust**

- **Gatekeeper**, the 3-layer filter (Declaration → Interview → Merit) that narrows the candidate pool to 3-5 entries by ANN semantic match + cognitive fingerprint + weighted performance.
- **GatekeeperScore**, the winner-selection score, `w1·SuccessRate + w2·TrustScore + w3·(1/NormLatency) [− w4·NormalizedCost]` (ADR-0011 adds the cost term).
- **TrustScore**, the agent's reliability, EWMA-over-task-verifier-score ratio. Defense against **Confidence Inflation** (an agent over-bidding with low real success).
- **Verifier Pool / Verification**, high-Merit agents that independently score a ~10% sample (FNV-1a hash on plan id) of completions; Surveillance mode flags serial failures for re-verification.
- **ProfileAggregator**, a background worker that recomputes Merit EWMA metrics from `TaskEvent`s.

**Reactive / durable execution** (ADR-0032 + ADR-0061)

- **Signal**, the canonical event envelope (`domain.Signal{StreamID, FromAgent, Payload, RawText, Timestamp}`) a daemon or filesystem watcher emits and the `ReactiveEngine` evaluates against `WatchConfig`s.
- **Plugin / Registry / Lifecycle** (ADR-0074, extended by ADR-0082), the compile-time extension system: a `Plugin` (`Manifest()` + `Register(*Registry)`, plus an OPTIONAL `Build(KernelServices)`) declares contributions to curated extension points — **replace-one** (`SetSignalReceiver`, `SetAuthorizer`, `SetIngressResolver`, `SetIdentityResolver`, `SetSubstrateConsultant`; conflict = startup error) or **add-many** (gRPC services, trace wrappers, lifecycles, capabilities). Three explicit phases: **Register** (declare — no kernel exists) → **Build** (construct runtime objects from the capability bundle) → **Lifecycle Start/Stop** (run). Tiered: mechanism is pluggable, the security kernel + auction integrity are NOT. Not Go `.so` (CGO/Windows/version-lock); compiled into a distribution binary.
- **ReactiveEngine**, the premium rule engine (`cambrian-premium/reactive/`) that fans a signal out to matching watches, evaluates each condition, and executes the action; it implements `domain.SignalReceiver`, wired as the first **plugin** (ADR-0074) via `Registry.SetSignalReceiver` + a `Lifecycle` (start/stop) + the ADR-0073 `ReactiveControl` gRPC plane.
- **Reactive journal**, the durable append-before-eval log (ADR-0061); one entry per signal, keyed by a monotonic seq, so a signal survives a crash between receipt and action.
- **Ack cursor**, a per-watch high-water mark of processed journal seqs; conservative (never advances past an unprocessed signal) so replay never skips one. The cursor is an optimization — idempotency is the correctness primitive.
- **Idempotency key / exactly-once**, `sha256(watch_id | signal_fingerprint | time-bucket)`; `MarkExecutedOnce` is an atomic bbolt check-and-set, so a redelivered signal executes its action exactly once, surviving restart.
- **Dead-letter**, a durable record of an action that failed or a signal that expired/dropped, with a reason; surfaced by the `OperatorConsole.ListWatchDeadLetters` read RPC ("what did my watch fail to do").
- **Debounce / coalescing** (ADR-0062), a per-watch window (`WatchConfig.DebounceSeconds`) that collapses a signal storm into one fire per T seconds, carrying the coalesced batch in the fired signal's `Payload["_batch"]` + `["_coalesced_count"]`.
- **Reactive budget / shed order** (ADR-0062), plane-wide hourly token buckets for `llm`-condition evaluations and `start_plan` actions; on exhaustion those (expensive) lanes are shed first — skipped, dead-lettered, and surfaced by a throttled `ReactiveBudgetEvent` — while deterministic/pattern conditions keep flowing.
- **Payload-as-data / condition guard** (ADR-0063), the `llm`-condition injection defenses: the evaluator prompt fences the untrusted signal payload in a nonce-delimited, JSON-encoded data block separate from the trusted operator condition; `WatchConfig.ConditionPayloadKeys` allowlists which payload keys reach the prompt; and `WatchConfig.Approved` is the required operator acknowledgement for a high-risk `llm`→`start_plan`/`dispatch_agent` watch (enforced at `RegisterWatch`).

**Memory and context**

- **VectorStore**, the `domain.VectorStore` port over pgvector (HNSW cosine, query-time `TemporalDecay`, three-set/CNF scope predicate over `metadata.tags`).
- **Document**, a single LTM row typed by `DocType*`: `MnemonicFact` (knowledge), `MnemonicAction` (events), `MnemonicScene` (conditions), `MnemonicEntity` (world-model things), `EpisodicMemory` (session narrative), `AgentProfile` (cognitive fingerprint), `ProceduralTemplate` (Hippocampus replay), `NegativeEdge` (failure memory), `Tool` (vectorised menu), `Skill` (vectorised menu).
- **activation_strength**, the `[0,1]` lifecycle metric per Document (0.1 default, +0.05/retrieval to 0.8); replaces `ImportanceScore`. Effective relevance at read = `cosine × (α + (1−α)·activation) × e^(−λ·age)`.
- **WorkspaceStage**, the pre-planning enrichment: SCENE+FACT fetch + pairwise contradiction guard (cosine>0.85 → `[CONFLICT]`) + cold-start fallback; the single LTM-to-Planner gate.
- **SpreadingEngine**, the GraphRAG BFS over `document_edges`, `0.75^depth` attenuation; the Graph Coverage Guard is the 3-band gate before BFS enrichment.
- **Conversation / Message** (ADR-0084 D1), the first-class chat entities. A `Conversation` is a durable, owned, ordered exchange (`OwnerID`, `Status` open|closed, `ConversationProfile`); a `Message` is one turn with a server-assigned monotonic `Seq` and an optional `ClientID` idempotency key. A Conversation is **not** a Session: a Session is goal-scoped work that completes, and a turn that orders work *references* one (1:N), never *is* one. Persisted by the kernel (`domain.ConversationStore`) so state survives a restart of whatever drives the chat.
- **ConversationProfile** (ADR-0084 D7), the recall + scope posture of a conversation: `operator` (recall, no narrowing), `employee` (recall, caller_scope-narrowed), `customer` (**no shared-memory recall** — tools only). Deliberately a property of the conversation rather than an agent default, so an external user cannot pull on tenant knowledge because someone flipped `seed_recall`.
- **EpisodicMemory**, the session-level narrative index (`Goal`, `Decisions`, `ActionItems`, no `Outcome`) as `DocTypeEpisodicMemory`; produced by the ConsolidatorAgent on `SessionCompletedEvent` (ADR-0029).
- **LTMEnrichment**, the typed `PrimeForPlanning` result: `Facts` + `Negatives` + `Episodes`; carries `<PrecedentLTM>` from ADR-0049. It used to also carry `<DiscoveryLTM>` from the Scout (ADR-0051); the Scout was retired 2026-08-07, so the planner is primed from memory alone.
- **Tier-1 / Tier-2**, the in-memory channel immediately queryable in-run, and the background LLM-as-Judge that commits FULL / FACT_ONLY / DROP with a heuristic fallback on timeout (ADR-0015).
- **failure_kind**, the typed result of a failed step (`retryable` / `non_retryable` / `verification_failed` / `partial` / `replan_needed`); consumed by the DAGExecutor's tiered recovery (ADR-0005/0010/0013).
- **Chunker**, the `domain.Chunker` hexagonal port (`Name() / Supports() / Chunk()`); the `Chunk` value carries `Body` and a free-form `Metadata` map. Five implementations live in `internal/memory/chunkers/` (`option_c` / `recursive_character` / `ast_go` / `markdown_header` / `late`); routing is data-driven via the `chunker_registry` (Zero-Hardcode Rule, ADR-0060).
- **chunker_registry**, the data-driven switchboard at `internal/memory/chunker_registry.go` that routes `(sourceType, ext)` to a registered `Chunker` in strict precedence `match(SourceType) → match(ext) → default`; the default name is a config value, not a Go constant. An unknown route or default is a startup error, not a silent fallback.
- **chunk_relations**, the per-chunk JSON-marshaled metadata payload (set by the IngestionManager in `Chunk.Metadata["chunk_relations"]`): a `ChunkRelations` value carrying `ParentEntityID`, `PrecedingChunkID`, `FollowingChunkID`, and a `SiblingContext`. The retriever follows `parent_entity_id` to drill down to the source document (ADR-0060).
- **SiblingContext**, the content half of `chunk_relations`: parent title, parent summary, parent scene, preceding snippet, following snippet. Per-field caps 80 / 120 / 120 / 96 / 96 bytes; strict 512B total budget enforced by `SiblingContext.MarshalJSON` (over-budget fields are trimmed at the right, never mid-rune). JSON keys and identifier fields are not in scope.
- **source_document**, the `DocTypeMnemonicEntity` discriminator used for ingested external documents (ADR-0060 D1). The entity row carries `SourceURI`, `SourceType`, `Title`, `Author`, `Timestamp`, and `ContentCID` (the CAS handle returned by `domain.ContentStore.Put`). **GC-exempt** (ADR-0060 D8) because source-doc entities are drill-down targets for chunk recall, not chunk-level recall targets themselves.
- **LateChunker**, the gated long-context encoder chunker (Günther et al., arXiv:2409.04701). Selected only when `chunker.late.enabled = true` AND `embedder.supports_long_context = true` (default `false` until the embedder-selection ADR resolves) AND the body is within `chunker.late.max_doc_tokens` (default `8192`, matching `nomic-embed-text`); over-budget docs fall back to `OptionCChunker` with a `late_fallback` log + run-manifest metric.

**Scope and access (ADR-0034/0035/0085/0086/0087)**

- **Authorizer**, the port the kernel asks before it acts (`Authorize` / `Filter` / `ReadFilter` / `ClassifyWrite`). OSS ships `AllowAllAuthorizer`; the premium plugin supplies the real one.
- **TagSet**, an AUTHORED three-set term (`RequiredTags` / `AnyOfTags` / `ForbiddenTags`) the kernel carries without interpreting.
- **TagPredicate**, the COMPUTED, ready-to-apply form the kernel applies (CNF any-of clauses + bypass). The kernel applies a predicate; it never composes two — composition is the decision point's job.
- **AccessDecision**, the explainable outcome: reason, the SPECIFIC tag/clause/effect responsible, which policy linked where contributed it, and the policy version.
- **ToolEffect**, the closed verb set `read|write|egress|spend|admin`. A tag says what a resource is about; an effect says what you are doing to it (ADR-0086).
- **DefaultWriteTags**, the operator-set ceiling the decision point derives a write's classification from. A principal may only narrow, never broaden.
- **ScopeSystem**, the explicit kernel-internal bypass sentinel; one grep enumerates every use (INV-7). Never produced by composition.
- **Surface**, the entry point a request arrived through (operator / agent / chat / reactive / mcp / internal). Established by the KERNEL from the transport and clamps what may be done regardless of identity — the loopback analogue (ADR-0085 D7). `mcp` is the inbound Cambrian MCP endpoint (ADR-0126 D4) — never the outbound MCP *connector*, which dials foreign servers and carries no surface of its own.
- **PolicyObject / Link / Group**, the Group-Policy-shaped authoring model: policy is authored once and LINKED to a container, never assigned to a person. Precedence is organisation → group (outermost to innermost) → principal → surface, every fold an intersection (ADR-0087).
- **Report-only**, a policy evaluated and journalled but not enforced; the decision carries `would_have_denied` so the blast radius is visible before the switch is flipped.

**Tools and skills (ADR-0039/0043/0044/0045/0046)**

- **ToolGrant**, the operator-set per-tool grant + resource bounds (filesystem roots, egress SSRF guard, command allowlist; empty = fail-closed).
- **ToolExecutor**, the single reference monitor: grant → resource policy → data scope → approval → budget → dispatch → audit. Anything that runs a tool goes through here.
- **Skill**, an authored procedural capability (`SKILL.md` with `{Name, Description, Instructions, ToolGrants, ScopeTags}`); distinct from a procedural template (authored vs learned).
- **SkillRegistry / SkillRetriever**, the system-skill index (`DocTypeSkill`) and the scope-gated retriever that serves `ListSkills`. Agent skills are SDK-local and bypass the registry.
- **RunGrantOverlay**, the session-keyed, ephemeral overlay that grants a system skill's bundled tools for the run. Operator authority; dangerous tools are still approval-gated; agent skills are narrow-only by construction (ADR-0046).
- **Two-tier tool disclosure**, the "short to choose, full to call" pattern: deterministic `toolSummary` (first sentence-or-line, capped) is embedded and served as Tier-1 by `ListTools`; `describe_tool(name)` fetches Tier-2 (full spec) grant-gated, fail-closed (ADR-0045).
- **`find_tools` / `find_skills`**, the agent-side pull that complements the kernel-side push of `ListTools` / `ListSkills`. Same hybrid pattern as `memory_query`.
- **Published Tool Surface** (ADR-0126 D1/D2), the OUTBOUND registry of tools Cambrian OFFERS to external callers — `domain.PublishedTool` (declaration) + `PublishedToolHandler` (one method, principal taken from the context, never from the arguments) + `PublishedToolResult`, contributed by plugins through `Registry.PublishTool` (Tier-2 add-many, duplicate names fatal) and composed into a `PublishedToolSurface` snapshot a transport renders. Protocol-agnostic on purpose: MCP is one renderer, so a second transport is additive. Deliberately NOT the `SystemTool` registry, which faces the other way — fusing them would put published tools in internal agents' menus and re-publish every foreign `mcp:<server>/<tool>` outward as an open proxy. Names match `^[a-z][a-z0-9_]{0,47}$`, never dotted (D7 / ADR-0097 D7).

**LLM and prompts (ADR-0042)**

- **LLMProvider**, the health-guarded model broker: id-keyed registry, circuit breaker (failure = transport err, HTTP≠200, timeout, empty response), price ledger, failover ladder. Organs stay blind to model id and health.
- **Generator**, the consumer-side LLM interface; the only type the organs see. Streaming and non-streaming variants are registered for every generator via `llm.NewStreamersFromGenerators` so any provider can serve cognitive agents and `StepAllocation` fallback can span providers.
- **PromptBuilder**, 8 pure functions for the canonical `<System>` / `<Context>` / `<Task>` / `<OutputSchema>` prompt sections; the `PromptRegistry` is the compile-time prompt catalog, threaded to `PlanEvent` via the 8-char `PlannerPromptVersion` hash.

**Operator transport (ADR-0047)**

- **OperatorConsole**, the kernel gRPC service the `cambrian-ui` console speaks to. The only UI→kernel API. Carries the sequenced feed+spool, cursor resume / RESYNC, projection + snapshot, auth + role gate, audit + idempotent commands, control hub / HITL, chat/inject, token lane, and capability handshake.
- **AgentManager** (reused), the internal lifecycle owner that the OperatorConsole projects, not a separate component.

---

## Cross-repo pointer

This file is a sub-repo manual. The monorepo-level map, the four cross-repo
invariants, and the open-core boundary live one level up.

- Monorepo context: [../../CONTEXT.md](../../CONTEXT.md)
- Monorepo agent guide: [../../AGENTS.md](../../AGENTS.md)
- Open-core boundary ADR: [../../0057-open-core-boundary.md](../../0057-open-core-boundary.md)
- Operator transport plane (the UI surface): [docs/adr/0047-operator-transport-plane.md](docs/adr/0047-operator-transport-plane.md)
- Multi-signal ranking (the bench-driven ranking ADR): [docs/adr/0054-multi-signal-ranking.md](docs/adr/0054-multi-signal-ranking.md)
- Chunking pipeline (pluggable Chunker port + 5 chunkers + `chunk_relations` + `source_document` entity): [docs/adr/0060-chunking-pipeline.md](docs/adr/0060-chunking-pipeline.md)
- Kernel agent guide (the rule book, complementary to this manual): [AGENTS.md](AGENTS.md)
- Kernel architecture orientation: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)
- ADR index and status vocabulary: [docs/adr/README.md](docs/adr/README.md)
